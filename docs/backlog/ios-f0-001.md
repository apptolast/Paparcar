# IOS-F0-001 — Fase 0 del port iOS: habilitadores en común

**Fecha:** 2026-08-13 · **Estado:** ✅ **FASE 0 COMPLETA en Windows** (los 9 tickets, mismo día).
Falta: push de las ramas + verificación Mac de la lista [VERIFICAR-MAC] de abajo.
Detalle: completados IOS-F0-01 ✅
(`fix/IOS-F0-01-bt-receiver-kdoc`), IOS-F0-02 ✅ (`feature/IOS-F0-02-trace-ingestion`),
IOS-F0-05 ✅ (`feature/IOS-F0-05-ar-query-pull`), IOS-F0-03 ✅
(`feature/IOS-F0-03-device-capabilities`), IOS-F0-04 ✅
(`feature/IOS-F0-04-geofence-contract`; el bus resultó semivivo en Android — emitía sin
consumidor — y la unificación fue una impl común SharedFlow broadcast) y IOS-F0-06 ✅
(`feature/IOS-F0-06-side-stores`; contratos tipados PendingArmRecords /
ExitDeliveryRecords / ArrivalResolutionRecord, Android delega en el código existente —
claves intactas por construcción — y esqueletos iOS NSUserDefaults + IosDetectionStepAnchors).
Ramas apiladas en ese orden sobre master; suite mock+prod verde en Windows tras cada una.
**Lista [VERIFICAR-MAC]** para el compañero (`gradlew :app:compileKotlinIosArm64`):
`IosPlatformModule.kt` (DeviceCapabilities false/false, F0-03) ·
`IosGeofenceManagerImpl.kt` + `IosDetectionModule.kt` + borrado de
`IosGeofenceEventBusImpl.kt` (F0-04) ·
`IosDetectionSideRecords.kt` + `IosDetectionStepAnchors.kt` + `IosDetectionModule.kt` (F0-06) ·
`IosAppPreferences.kt` (toggles eliminados, F0-08).
Y los tres pequeños: IOS-F0-07 ✅ (reglas de backup, rama `fix/IOS-F0-07-backup-rules`),
IOS-F0-08 ⛔ SUPERADO por SETTINGS-AUDIT-REMEDIATION-001 (ver el recompute al final) y IOS-F0-09 ✅
(heartbeat inexacto aceptado y documentado, pieza CTA special-access descartada; misma rama 08).
**Origen:** `docs/ANDROID-DETECTION-AUDIT.md` §8 + `docs/IOS-IMPLEMENTATION-PLAN.md` §8-F0.
**Principio:** TODO lo de esta fase compila y se testea en **Windows** (commonMain + androidMain
+ tests unitarios). Los targets iOS de Kotlin/Native NO compilan en Windows — cualquier cambio
en `iosMain` queda marcado `[VERIFICAR-MAC]` y lo valida el compañero con Mac tras el push.
Regla de trabajo: rama por ticket, tests verdes (`gradlew :app:testDebugUnitTest`)
antes de merge, nunca a main directo.

---

## Decisiones resueltas (2026-08-13, usuario)

| # | Tema | Resolución |
|---|------|-----------|
| 1 | Alarma exacta (`SCHEDULE_EXACT_ALARM`) | Aceptar modo inexacto y documentarlo; NO pedir permiso al usuario. La malla safety-net de 15 min ya cubre. |
| 2 | Toggles de notificación muertos (P3) | Eliminarlos, no honrarlos. |
| 3 | `allowBackup` | Excluir el estado de detección con `dataExtractionRules`. |
| 4 | Momento del escalado Always (iOS) | Contextual tras el primer aparcado con éxito, con pantalla de valor previa. |
| 5 | Naming de tiers iOS | Reutilizar "Asistido +" (con Always) / "Asistido" (sin Always); Automático oculto en iOS. |
| 6 | Radio mínimo de región iOS | Mantener 100 m, pero explícito en el contrato común (no override silencioso en la impl). |
| 7 | Heurística de audio del coche | Aparcada como experimento post-lanzamiento (F5+). Fuera del MVP. |
| 8 | `Kind.ACTIVITY` en TraceEvent | SÍ, se añade en F0.1. Beneficia también a los fixtures Android. |
| 9 | Nudge retroactivo sin Always | SÍ, incluido en el tier Asistido iOS (consulta histórica al abrir la app). |

---

## Tickets

### IOS-F0-01 — Corregir el KDoc de BluetoothConnectionReceiver (D1) ⚠️ PRIMERO
**Rama:** `fix/IOS-F0-01-bt-receiver-kdoc`
El KDoc (`BluetoothConnectionReceiver.kt:40-41`) afirma `exported=false`; el manifest declara
`true` y ese `true` es vital (revive el proceso muerto en ACL disconnect). Riesgo real de que
una "corrección" futura mate la estrategia BT en silencio.
- Corregir el comentario para que documente el `true` y POR QUÉ es load-bearing.
- **Aceptación:** solo comentario; ningún cambio de comportamiento. 15 min.

### IOS-F0-02 — Promover el replay a commonMain como puerto de ingesta
**Rama:** `feature/IOS-F0-02-trace-ingestion`
La pieza clave del port. Hoy `DetectionTraceReplayer.kt` + `TraceEvent` viven en
`shared/src/commonTest/kotlin/com/rndeveloper/paparcar/domain/coordinator/replay/`.
1. Mover `TraceEvent` (+ `Kind`) y la lógica de reproducción a `commonMain`
   (`domain/coordinator/ingestion/`), detrás de un puerto `DetectionTraceIngestion`
   (entrada: `List<TraceEvent>`; salida: el coordinator evaluado con reloj virtual
   = timestamps reales).
2. Añadir `Kind.ACTIVITY` (decisión 8): transición AR (automotive/walking, enter/exit)
   como tercer tipo de evento; la ingesta alimenta la escalera AR igual que el service.
3. Los 12 tests de regresión y los 9 fixtures `Trace_*.kt` se quedan en `commonTest`
   pero pasan a consumir el puerto (misma semántica, cero cambios de aserciones).
4. Un fixture nuevo mínimo con eventos `ACTIVITY` que pruebe la escalera AR vía ingesta.
5. Follow-up opcional (no bloqueante): `tools/trace2fixture/trace2fixture.py` emite
   eventos ACTIVITY si la traza de Firestore los trae.
- **Aceptación:** tests de regresión verdes sin tocar aserciones; fixture ACTIVITY nuevo
  verde; `commonMain` sigue sin imports de plataforma. _Dep: ninguna._

### IOS-F0-03 — DeviceCapabilities: fiabilidad y tiers conscientes de plataforma (P5)
**Rama:** `feature/IOS-F0-03-device-capabilities`
Hoy `DetectionReliabilityInput` asume las tres patas Android (BT, exención de batería, OEM).
En iOS la pata batería no existe y la BT es estructuralmente imposible — no deben generar
issues con CTA imposible ("empareja tu coche").
1. `DeviceCapabilities(supportsBtStrategy, supportsBatteryExemption)` inyectado a
   `EvaluateDetectionReliabilityUseCase` (`domain/usecase/detection/`).
2. Capacidad no soportada = pata N/A (ni satisfecha ni issue), no `false`.
3. Techo de tier iOS = ASSISTED; naming según decisión 5 (Asistido +/Asistido, Automático
   oculto). `DetectionReliability.kt` (`domain/model/`) y el test de la matriz se amplían:
   matriz Android intacta (6 filas) + filas iOS nuevas.
4. Impl Android del provider de capacidades (`true`/`true`); la impl iOS (`false`/`false`)
   queda declarada en el módulo Koin de iosMain `[VERIFICAR-MAC]`.
- **Aceptación:** matriz Android sin cambios de resultado; tests nuevos de las filas iOS;
  ninguna superficie Android cambia de copy. _Dep: ninguna._

### IOS-F0-04 — Contrato único de eventos de geocerca + suelo de radio explícito (G2 + decisión 6)
**Rama:** `feature/IOS-F0-04-geofence-contract`
Hoy hay dos semánticas: Android entrega EXITs directo al service (el `GeofenceEventBus`
existe pero está muerto en Android) e iOS emite por el bus. Un solo contrato:
1. Decidir en diseño (media página en el PR): bus como contrato único (Android emite y el
   service consume) — recomendado — o eliminar el bus y definir entrega directa común.
2. `GeofenceManager` (commonMain) declara el suelo de radio por plataforma en el contrato
   (Android 60–200 m según precisión; iOS mínimo 100 m — decisión 6). Se elimina el
   override silencioso `MIN_RADIUS_M=100` de `IosGeofenceManagerImpl` `[VERIFICAR-MAC]`.
3. Documentar en el KDoc del contrato la semántica de re-registro (Android re-registra
   tras boot; iOS NO lo necesita — el sistema relanza la app).
- **Aceptación:** un solo camino de entrega de eventos con test unitario; comportamiento
  Android de campo idéntico (mismos RC 9100/9101/9102). _Dep: ninguna._

### IOS-F0-05 — `queryTransitions` (pull) en ActivityRecognitionManager + seals sobre pasos por query
**Rama:** `feature/IOS-F0-05-ar-query-pull`
La reconstrucción wake-and-query necesita preguntar por el pasado.
1. `ActivityRecognitionManager` (`domain/ActivityRecognitionManager.kt`): añadir
   `suspend fun queryTransitions(fromMs: Long, toMs: Long): List<ActivityTransitionEvent>`.
   Android (`ActivityRecognitionManagerImpl`) implementa devolviendo vacío (o su bus
   histórico si existe barato) — en Android el camino push sigue siendo el principal.
2. `DetectionStepAnchors` (`domain/sensor/`): documentar y ajustar el contrato del seal
   para que funcione con pasos por rango de fechas (CMPedometer) además del counter
   acumulado Android. El diseño (qué campos del seal sobreviven: posición y momento sí,
   counter pasa a ser derivable) va en el KDoc del contrato.
3. `StepCounterSource`: anotar en el contrato la semántica pull equivalente.
- **Aceptación:** compila y testea en Windows; fake del puerto con `queryTransitions` en
  los tests; cero cambios de comportamiento Android. _Dep: ninguna (sinergia con F0-02
  por `Kind.ACTIVITY` — hacer después de F0-02)._

### IOS-F0-06 — Portificar los side-stores mínimos (`DetectionSideStore`)
**Rama:** `feature/IOS-F0-06-side-stores`
Los registros durables que la reconstrucción iOS necesita leer/escribir hoy son clases
Android puras sobre SharedPreferences.
1. Puerto KV durable `DetectionSideStore` en commonMain (o contratos específicos por
   registro — decidir en el PR con el criterio: contratos tipados > KV genérico).
2. Migrar detrás del contrato: `pending_arm` (`PendingDetectionStore`), seals/anclas,
   `exit_delivered_*`, `arrival_resolution_*`. `SentryResidenceStore` NO se portifica
   (SENTRY es NO APLICA en iOS).
3. Las claves y formato actuales de SharedPrefs NO cambian (nada de migraciones de datos
   de campo); la impl Android envuelve lo existente.
4. Impl iOS NSUserDefaults: solo esqueleto declarado en Koin `[VERIFICAR-MAC]`.
- **Aceptación:** tests unitarios de los contratos con fake en memoria; los tests
  existentes del safety-net siguen verdes; claves de prefs intactas (verificar con test).
  _Dep: ninguna. Sinergia con higiene 8.2.3 (store tipado del safety-net) — NO mezclar:
  este ticket solo portifica, el split del worker es otro PR._

### IOS-F0-07 — allowBackup: excluir el estado de detección (decisión 3)
**Rama:** `fix/IOS-F0-07-backup-rules`
Auto-backup restaura SharedPrefs de detección en un móvil nuevo → estado fantasma
(anclas/sesiones de hace semanas en un dispositivo virgen).
- `dataExtractionRules` (+ `fullBackupContent` para API < 31): excluir
  `parking_safety_net`, `trip_trail`, `driving_route`, `departure_event_bus`,
  `exact_heartbeat`, `bt_identity` y el resto de prefs de detección; el resto de la app
  no cambia.
- **Aceptación:** manifest apunta a las reglas; lista de ficheros excluidos revisada
  contra la lista real de prefs en el código (no de memoria). 1 h.

### IOS-F0-08 — Eliminar los toggles de notificación muertos (P3, decisión 2)
**Rama:** `fix/IOS-F0-08-dead-toggles`
Los switches de Settings que el código ignora se eliminan (UI + estado + strings en los
9 idiomas en el mismo commit, regla i18n).
- **Aceptación:** ninguna referencia huérfana; galería/Dev Catalog actualizado (MOCKQA-001).

### IOS-F0-09 — Heartbeat: asumir y documentar el modo inexacto (decisión 1)
**Rama:** `fix/IOS-F0-09-heartbeat-inexact`
Con targetSdk 36, `SCHEDULE_EXACT_ALARM` no está concedido por defecto → los
`setExactAndAllowWhileIdle` del `ExactHeartbeatScheduler` son inexactos en campo.
- Documentar la realidad en el KDoc y en PARKING-DETECTION.md; renombrar si procede
  (`HeartbeatScheduler`); eliminar cualquier asunción de exactitud en la lógica que
  dependa del intervalo de 5 min (revisar consumidores).
- NO se pide el permiso al usuario. **Aceptación:** cero promesas de exactitud en código
  ni docs; tests verdes.

---

## Orden recomendado y paralelización

```
IOS-F0-01 (15 min, primero — desactiva la mina)
IOS-F0-02 ──► IOS-F0-05          (ingesta antes que query AR, comparten Kind.ACTIVITY)
IOS-F0-03 ─┐
IOS-F0-04  ├─ independientes, paralelizables entre sí y con la cadena 02→05
IOS-F0-06 ─┘
IOS-F0-07 / 08 / 09 — sueltos, tamaño S, para huecos
```

**Definition of done de la fase:** todo verde en Windows
(`gradlew :app:testDebugUnitTest` + build Android instalable en Redmi/OPPO),
push de todas las ramas mergeadas, y lista `[VERIFICAR-MAC]` consolidada (módulos Koin
iosMain + `IosGeofenceManagerImpl`) para que el compañero con Mac ejecute
`gradlew :app:compileKotlinIosArm64` y reporte errores de iosMain, que en Windows
son invisibles.

---

## Historial de rebases

- **14-08-2026** — la pila de 8 ramas (`fix/IOS-F0-01…08`) se consolidó en una sola rama,
  `feature/IOS-F0-001-fase0`: las 7 intermedias eran punteros dentro de la misma cadena lineal y
  la punta ya contenía los 17 commits. Backup en los tags `backup/ios-f0-prerebase` (copia
  pre-rebase que había en origin) y `backup/ios-f0-stack-preclean`.
- **14-08-2026** — rebase sobre master `1e94142f` (UI-TOPBAR-COLLAPSE-001, UI-SCROLL-TO-TOP-001,
  DEPS-BASELOGIN-110, PARK-DELETE-NO-DECLARE-001, UI-ZONE-MANAGE-001). Un solo conflicto, en
  `SettingsScreen.kt` [IOS-F0-08]: master había reescrito la pantalla con
  `PapCollapsingTopBarScaffold` + `expandKey` mientras el ticket borraba la sección de
  Notificaciones. Resuelto conservando el layout nuevo de master y aplicando encima el borrado del
  ticket. Verde: 1140 tests + `compileMockDebugKotlinAndroid` + `compileProdDebugKotlinAndroid`.
- **21-08-2026** — rebase sobre master `ef61d93b`, 56 commits de detección por delante (entre ellos
  DET-MOTOR-PROOF-001, DET-UNWITNESSED-DISPLACEMENT-001, DET-STOP-BUTTON-001,
  DET-MOTORWAY-TRIP-JUDGED-BICYCLE-001). Dos conflictos:
  - `DetectionTraceReplayer.kt` [IOS-F0-02]: la rama lo borra al promoverlo a
    `commonMain/…/ingestion/DetectionTraceIngestion.kt`, y master lo había ampliado con el carril
    AR (`Kind.VEHICLE_EXIT`, `Kind.BICYCLE_ENTER` + `trueTimeMs`) para el replay del viaje de
    autovía. Resuelto **fundiendo las dos semánticas en el modelo de la rama**: el carril AR sigue
    siendo `Kind.ACTIVITY`, el enum `Activity` gana `BICYCLE_ENTER`, y `emitActivity` pasa a
    `(activity, trueTimeMs)` para que el sello se juzgue por cuándo se OBSERVÓ. Barridos sus
    consumidores: `Trace_MotorwayRedmi001.kt`, los dos tests de motorway,
    `ActivityRecognitionQueryTest.kt` [IOS-F0-05] y el generador `tools/trace2fixture`.
  - `IosDetectionModule.kt` [IOS-F0-06]: conflicto aditivo — master registra
    `ArrivalHandoffDetection` + `DepartureWatchResumer` y el ticket los side-records; se conservan
    los dos bloques.
  Backup en el tag `prerebase/IOS-F0-001` (borrado al verificar). Verde: **1332 tests, 0 fallos** +
  `compileMockDebugKotlinAndroid` + `compileProdDebugKotlinAndroid`. Force-push a origin hecho.

## Rebase 2026-08-27 sobre master `a622c854`

Los 19 commits replicados sobre un master **74 commits por delante**, que por medio traía el
refactor F6 de detección, el módulo de DI de detección, `DET-ASK-STATE-001` y el paso a
`allWarningsAsErrors`. Tres conflictos, ninguno mecánico:

1. **`IOS-F0-02` vs. el refactor F6.** La rama promociona el harness de replays a commonMain como
   `DetectionTraceIngestion` — o sea, borra el fichero que master había **renombrado** de paquete
   (`domain.coordinator` → `domain.detection.coordinator`). Comprobado que master **no le tocó la
   conducta**, solo el paquete, así que la regla de resolución fue: *estructura de la rama,
   nomenclatura de master*. El fichero nuevo vive ahora en
   `domain.detection.coordinator.ingestion`.
2. **`IOS-F0-03` vs. `DET-DI-DETECTION-MODULE-001`.** Master movió los bindings de detección de
   `DomainModule` a `DetectionModule`; la rama cambiaba una línea del bloque movido. Se aplicó el
   movimiento de master y el cambio de la rama se llevó a su sitio nuevo.
3. **⚠️ `IOS-F0-08` vs. `DET-ASK-STATE-001`.** El más peligroso. La rama borra los toggles muertos
   de notificación, pero master había metido `PendingPromptWindow` **en el mismo bloque**. Tomar el
   lado de la rama en bloque habría borrado el "¿Has aparcado?" de Home sin que nada fallara al
   compilar. Resuelto quirúrgicamente: fuera los toggles, intacto el prompt (16 ficheros lo siguen
   usando).

Un commit extra en la punta alinea lo que master añadió DESPUÉS del merge-base: las 4 trazas nuevas
(`CameliasGondola`, `Gondola2508Supersede`, `GondolaCamelias`, `HouseMirage`) y 7 call sites que
seguían llamando al `DetectionTraceReplayer` ya renombrado.

**Verificado:** 1.682 tests, 0 fallos · **22 replays verdes** · 0 warnings con `-Werror`.
⚠️ **Solo Android.** iOS no se puede compilar desde Windows: `iosMain` cambió en este rebase y
sigue **sin verificar**.

---

## Recompute sobre master `b949efa1` (2026-08-29)

Master hizo el split `:app`+`:shared` y renombró el paquete a `com.rndeveloper.paparcar`
[ARCH-HEALTH-001]. Esta rama **no se rebasó: se recomputó**, porque un rebase habría dejado los
ficheros que master añadió bajo `composeApp/` fuera de todo módulo, y git los reporta limpios.
Método: `git diff` de la rama → traducción mecánica de rutas (`composeApp/src/commonMain` →
`shared/src/commonMain`, `androidMain/res` y el manifest → `app/src/main`, `androidMock` →
`app/src/mock`) y del paquete → aplicado fichero a fichero sobre master. 74 de 78 entraron solos.

**Lo que hubo que decidir a mano:**

1. ⛔ **`IOS-F0-08` (borrar los toggles de notificación muertos) se DESCARTA: master lo resolvió
   antes y mejor.** `SETTINGS-AUDIT-REMEDIATION-001` (28-08) auditó los mismos dos toggles y
   concluyó distinto: `notifySpotFreed` se borra entero, pero `notifyParkingDetected` **no estaba
   muerto sino sin cablear**, y lo cableó en su choke point (`AppNotificationManagerImpl.
   showParkingSaved`). La premisa de este ticket —"el código los ignora"— dejó de ser cierta. Los
   17 ficheros en conflicto (prefs, sus 3 impls, fakes y los 9 locales) se devolvieron a master.
   El ticket queda **cerrado como superado**, no como pendiente.
2. `IosDetectionModule` y `AndroidPlatformModule`: sus hunks no casaban porque el split les cambió
   el contexto (los bindings de notificación se fueron a `:app`). Aplicados a mano:
   `SharedFlowGeofenceEventBus` + los 4 ports de IOS-F0-06, y `DeviceCapabilities` de IOS-F0-03.
3. Alineación del harness con lo que master añadió DESPUÉS del merge-base — la misma tarea que ya
   hizo el commit de la punta, ahora con material nuevo: la traza `Trace_Redmi2808RefutedStillness`
   (28-08) usaba el `Kind.VEHICLE_EXIT` viejo → `Kind.ACTIVITY` + `activity=`; 3 llamadas a
   `replay(emitVehicleExit = …)` → `emitActivity`; 2 trazas sin el import de `TraceEvent`; y
   `SettingsViewModelTest` pasando ya las `DeviceCapabilities` de Android.

**Verificado:** 1.778 tests, 0 fallos · prod y mock ensamblan · 35 composeResources en el APK.
⚠️ **Sigue siendo solo Android.** `iosMain` cambió también en este recompute (el módulo de DI) y
**sigue sin compilar ni verificar** — eso es de la Mac.

## Rebase 2026-09-03 sobre master `7f56981f` — parcial, la rama queda ROJA

Tercera vez que el mismo mecanismo muerde: el rebase **no da un solo conflicto** y aun así no
compila. La rama promociona el harness de replay a `commonMain` (`DetectionTraceReplayer` →
`DetectionTraceIngestion`, y las transiciones de AR salen de `TraceEvent.Kind` a un campo
`Activity` con su propio `trueTimeMs`), así que todo lo que master añada encima sigue resolviendo
`TraceEvent` contra el fichero borrado. Git no lo ve: son ficheros nuevos que nadie tocó a la vez.

**Arreglado aquí** — el mismo import de una línea que ya llevan las otras 15 trazas:
- `Trace_CasaGapAnchor3008.kt` (10 errores)
- `Trace_Parafarmacia2908.kt` (6 errores)

**~~Sigue roto, a propósito~~ → ✅ CERRADO el 03-09** por
`TEST-PROMOTED-HARNESS-LEAVES-NEWER-TESTS-BEHIND-001` (ver su doc). Eran `DetectionTraceReplayTest.kt`
con **20 errores** en 4 tests que master añadió después
(`DET-DISPLACEMENT-DRIVE-MUST-SURVIVE-ITS-NEXT-FIX-001`,
`DET-A-USER-YES-DOES-NOT-SHRINK-A-WALK-ENTERED-DOUBT-001`,
`DET-THE-TWO-FPS-THAT-CAUSED-THE-REDESIGN-BECOME-REPLAYS-001`).

⛔ **Se temió que el port no fuese mecánico y NO era así** — dejarlo escrito porque la conjetura era
razonable y aun así falsa. El miedo: la API nueva juzga un evento de AR contra su tiempo observado
en vez de contra el reloj de entrega, y eso es lo que deciden esas aserciones. Leídos los 4:
**ninguno usaba `emitVehicleExit`**; inyectan las transiciones en el coordinator con su tiempo
verdadero explícito (`onVehicleExit(CASA_GAP_3008_FIRST_EXIT_AT_MS)`), o sea ya hacían a mano lo que
la API nueva sistematiza. **4 renombres de clase, cero cambios de lógica**, ni una aserción tocada.
Los 20 errores eran uno repetido: con la clase sin resolver se cae la inferencia de los lambdas.
📌 **Contar errores de compilación sobreestima el trabajo.** Leer los tests antes de dimensionar.

🔴 Y el error de compilación **tapaba un fallo real**: `KoinModuleVerifyTest` caía con
`MissingKoinDefinition` sobre el `Boolean` de `DeviceCapabilities` [IOS-F0-03] — el binding es
correcto (construye con literales), es `verify()` que refleja el constructor sin ver dentro del
lambda. Arreglado en la lista blanca del test, junto a `Function0`/`Function1`, que son el mismo
caso. **Suite: 2178 tests, 0 fallos.** La rama ya está verde en Android.

## Rebase 2026-09-03 (tarde) sobre master `65eff0cd` — limpio, y el guardarraíl nuevo cazó deriva

23 commits de master por delante, 4 propios replicados, **0 conflictos** — y como siempre en esta
rama, el rebase limpio no significó nada: `MockKoinGraphVerifyTest` (recién nacido en
`IOS-DI-A-MOCK-GRAPH-ONLY-PROD-IS-VERIFIED-001`) cayó porque master añadió `LicensesViewModel`
y su `OpenSourceLicenseRepository` solo estaba en `dataModule` — la pantalla de licencias
crasheaba en mock. Arreglado bindeando la impl real (commonMain, sin deps) en los dos mock
modules, amendado en el commit del ticket. Verde: suite + verify mock + ambos compiles.

⛔ **`origin` estaba divergente, no atrasado.** Tenía `cb9edbdb` (20 commits, 27-08) con el árbol
**pre-`ARCH-HEALTH-001`**: `composeApp/` + paquete `io.apptolast.paparcar`. El local `9f77e0f8`
(29-08) es ese mismo trabajo ya arrastrado al split `:app`/`:shared` y al rename, squasheado. Se
verificaron los 79 ficheros del stack remoto uno a uno antes de forzar: 73 aparecen mapeando rutas,
3 se movieron de `androidMain/` a `main/` y 3 los borra la rama a propósito
(`GeofenceEventBusImpl`, `DetectionTraceReplayer`, `IosGeofenceEventBusImpl`). No se perdió nada.
⛔ No fiarse del primer `git diff` entre las dos: da 72 "ficheros que faltan" que son puro cambio
de ruta.
