# 06 — Registro de invariantes del subsistema de detección (síntesis Fase 2)

> 📌 **Citas de línea ancladas a master `2288468e` (2026-08-24), el commit de arranque de F6.**
> A partir de aquí la Fase 1 mueve símbolos y los números se desplazan: son *a fecha de*, no punteros
> vivos. Para resolver cualquiera con exactitud: `git show 2288468e:<fichero>`. [P0.5]

> Refactor de solo-lectura · 2026-08-18 · síntesis de los 4 parciales de cluster (F1 ancla/egress,
> F2 decisión/ciclo de vida, G triggers/sesión, H androidMain/rutas) + `01-inventario-usecases.md`,
> `02-estado-coordinator.md`, `05-cobertura-tests.md`. **133 filas de origen → 132 tags únicos**
> (BUG-SERVICE-109 aparecía en F2 y H; aquí una sola fila). **+1 post-snapshot:** DET-CAR-REST-CLOCK-001
> (fix de campo 19-08, `fbc83847`) añadido tras la síntesis → **133 tags vigentes**; desplaza ~+14 las
> línea-refs de `EvalUS` citadas en las filas GAP/WALK-ENTERED-ZONE. Ningún guard se declara redundante sin
> demostración escrita con su escenario de campo; lo no confirmado queda **NO VERIFICADO**.
>
> Abreviaturas: **CPD** = `commonMain/…/domain/coordinator/CoordinatorParkingDetector.kt` ·
> **EvalPD/EPD** = `domain/usecase/parking/EvaluateParkingDecisionUseCase.kt` ·
> **EvalUS** = `EvaluateUnattendedParkingSaveUseCase.kt` · **EvalHC** = `EvaluateHonestCloseUseCase.kt` ·
> **EvalSNC** = `EvaluateSafetyNetCheckUseCase.kt` · **SHP** = `EvaluateShortHopDriveProofUseCase.kt` ·
> **CPC** = `CalculateParkingConfidenceUseCase.kt` · **CDS** = `androidMain/…/detection/service/CoordinatorDetectionService.kt` ·
> **Config** = `domain/model/ParkingDetectionConfig.kt` · **CPDTest** = `commonTest/…/CoordinatorParkingDetectorTest.kt`.
> Rutas relativas a `composeApp/src/`. 🔴 = ningún test menciona el tag.

---

## 1 · Tabla maestra

### 1.1 Cluster F1 — ancla, congelación, gap, egress, prueba de conducción, cierre honesto (28)

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **ANCHOR-LOCK-001** | 2026-07-04 supermercado (EXIT tardío, ancla derivó dentro de la tienda) + Calle Gavia (2 pasos espurios en semáforo no deben romper la detección) + paseo enérgico Doppler 2.5–3.6 m/s que borró el ancla y re-ancló a 55 m (CPD:2207-2211) | El paseo post-parking arrastraba/borraba el ancla → pin donde el peatón | `isAnchorLocked = bestStopLocation != null && stepCount ≥ anchorLockEgressSteps` (=8). LOCKED ⇒ (a) nunca se re-captura en un stop posterior (`pinnedToOtherStop`, CPD:2101) y (b) solo conducción REAL (`speed ≥ minimumTripSpeedMps && acc ≤ minGpsAccuracyForDriving`) la limpia — la banda ambigua no (CPD:2205-2213, 2267-2276) | CPD:180, 814, 852, 1774-1780, 2056, 2100-2101, 2207-2211, 2297; Config:315 | `bestStopLocation`, `stepCount`, `anchorCapturedAtStop` | ✅ replay `Trace_CalleGavia001` + `Trace_Supermarket001` (DetectionTraceReplayTest:103, 135), CPDTest:1903, 1930 | Con DET-ANCHOR-FREEZE-001: `isAnchorPinned = locked ∨ frozen` (CPD:1786-1787), consumidores idénticos por diseño. Con DET-STEP-SPEED-GATE-001: los pasos fantasma en atasco envenenaban el lock |
| **DET-ANCHOR-EGRESS-001** | 2026-07-15 16:11Z Redmi/MIUI, Camino de los Enamorados — FP 1.11 km: ancla congelada en un semáforo, confirm kinematic+egress EN el semáforo (`Trace_Enamorados001`). Rule A: 2026-07-15 Camelias, cluster in-car acc 3 m dentro de la casa · backlog | Pin en parada intermedia: el gate de desplazamiento solo tenía FLOOR, trivial a 1.11 km | `isEgressBornAtAnchor` (CPD:1998-2009): `d(ancla, egressOriginFix) ≤ max(accA+accO + egressOriginStepCount×anchorStrideMeters + egressBirthMarginMeters, egressBirthFloorMeters)`. FALSE ⇒ decisión = Prompt (EvalPD:223) y timeout desatendido = zona EGRESS_MISMATCH (centro por liveness del contador, radio = d(birth,ancla); EvalUS:207-227). Birth registrado en 2 sabores: parado CPD:2144-2156+2196-2197, móvil CPD:2336-2345+2380-2389. Rule A `refinedParkLocation` (CPD:2011-2047): refina al birth solo si `egressOriginStepCount>0 && acc ≤ egressBirthRefineMaxAccuracyMeters && d ≤ steps×stride+accs` | CPD:204, 212, 1053, 1990-2047, 2144-2156, 2336-2345; EvalPD:62-68, 218; EvalUS:207-227; Config:416-443 | `egressOriginFix`, `egressOriginStepCount`, `bestStopLocation`, `stepCount`, `kinematicEgressFixes` | ✅ replay Enamorados (DetectionTraceReplayTest:237, 289), `Trace_CameliasOppo001` (Rule A), EvaluateUnattendedParkingSaveUseCaseTest:229 | Duplicación INTERNA: los 2 sabores del birth con gates ligeramente distintos (02 §5 máquina 6). Comparte el veredicto Prompt con DET-CREDIBLE-DRIVE y DET-GAP-ANCHOR (EvalPD:218-225: 3 taints, una cláusula) |
| **DET-ANCHOR-FREEZE-001** | 2026-07-11 Redmi — contador mudo: la caminata a casa arrastró el ancla a la puerta (EvalUS:191); refinamiento same-stop: 2026-07-11 Av. Sanlúcar, fix de 9.8 m llegó al s71 y el corte a 30 s retenía uno de 260 m (CPD:2102-2108) | Sin pasos no había lock → la caminata movía el ancla al final de la conducción | Maduración (CPD:2116-2143): `matured = ¬anchorFrozen ∧ hasEverReachedDrivingSpeed ∧ ancla-de-ESTE-stop ∧ walkFixesSinceDriving ≤ anchorFreezeMaxWalkFixes ∧ (restProvenByTime ∨ restProvenByFixes)`. FROZEN ⇒ pinned (CPD:1782-1787) ⇒ solo conducción real la mueve; burst de reposición vetado (CPD:2306-2313). El save desatendido solo confía en ancla PINNED como punto exacto (EvalUS:190-205 → UNPINNED_ANCHOR, `bounded=sessionSawSteps`). F4: re-registro GMS del fence + throttle (EvalSNC:130, 393-420; ParkingSafetyNetWorker:227, 264, 765, 777; DepartureDetectionWorker:49; Config:573) | CPD:185, 193, 852, 1782-1787, 2100-2143, 2195, 2292-2298, 2311, 2352, 2360; Config:338, 361, 573; EvalUS:190; EvalSNC:130, 393, 409; ParkingSafetyNetWorker:227, 264, 765, 777 | `anchorFrozen`, `walkFixesSinceDriving`, `hasEverReachedDrivingSpeed`, `stoppedFixes`, `anchorCapturedAtStop` | ✅ CPDTest:1202+, 1406; EvaluateSafetyNetCheckUseCaseTest:557, 576 | Solape estructural con ANCHOR-LOCK (mismo consumidor `isAnchorPinned`). DET-SHORT-TRIP-FREEZE es una cláusula OR interna de su `matured`. Relaja LOC-001 (§4) |
| **DET-GAP-ANCHOR-001** | 2026-07-29 Redmi, Av. Sanlúcar — hueco MIUI de 100 s acabó en UN fix speed-0 a mitad de ruta; el paseo a casa satisfizo steps+egress y pinó 315 m antes del parking real · backlog | Ancla en punto de paso (drive-past): el reposo nunca fue presenciado | Al ABRIR un stop: si `previousFix.speed ≥ minimumTripSpeedMps ∧ (ts − previousFix.ts) > anchorGapMaxFixGapMs` ⇒ `stopEnteredAfterGapMs = holeMs` (CPD:2076-2084; velocidad-solo en el fix pre-gap A PROPÓSITO — exigir accuracy eximiría a los streams degradados que producen el hueco). Se estampa al rebind (`anchorGapMsAtCapture`, CPD:2193). `anchorGapEntered` ⇒ Prompt, nunca pin silencioso (EvalPD:76-84, 218-225); un "Sí" con gap no usa el ancla (CPD:1053) | CPD:254, 263, 1053, 2064-2094, 2190-2194, 2353-2356; EvalPD:76-84, 218; EvalUS:245-248; Config:369 | `previousFix`, `stopEnteredAfterGapMs`, `anchorGapMsAtCapture` (+`anchorGapEnteredAtCapture` CPD:365-369) | ✅ CPDTest:2134-2248, 2393; EvaluateParkingDecisionUseCaseTest:421; EvaluateUnattendedParkingSaveUseCaseTest:280 | DET-GAP-ANCHOR-ZONE-001 es su corolario (misma condición, cambia la SALIDA en el timeout). Su KDoc lo declara "same class as anchorWalkEntered" (EvalPD:82) |
| **DET-GAP-ANCHOR-ZONE-001** | 2026-08-17 Redmi sesión `1786970028118` — hueco de 126 s a 42 km/h, luego 14.7 min de reposo a 40 m de donde el Oppo pinó el mismo viaje; el parking (25 min medidos) se tiró (`645bb09e`) · backlog | El taint gap era booleano: no distinguía 45 s de una hora → todo ancla gap-born era "no acotable" → Ask → parking perdido | El campo guarda MAGNITUD. En el timeout desatendido, si `anchorGapMs > 0`: `doubt = anchorGapMs/1000 × maxPedestrianSpeedMps`; `bounded = anchorRestMs ≥ sustainedStopForSaveMs` (la hipótesis drive-past muere en un reposo sostenido; testigo cambiado al ancla por DET-CAR-REST-CLOCK-001) ⇒ SaveZone(GAP_ANCHOR, centro=ancla, radio=doubt); si no, Ask (EvalUS:249-270). Precedencia movida por este ticket: `egressExceedsWalkReach` (VEHICULAR_EGRESS) se testea ANTES de toda zona (EvalUS:234-243) | CPD:260, 270, 365, 1162, 2072-2075, 2085-2093; EvalUS:86-93, 234, 249-270, 317 | `anchorGapMsAtCapture`, `stopEnteredAfterGapMs`, `anchorRestMs` (arg), `anchorGapEnteredAtCapture` | ✅ EvaluateUnattendedParkingSaveUseCaseTest:254, 344; CPDTest:2203, 2248 | Con DET-WALK-ENTERED-ANCHOR-ZONE-001: misma doctrina «degradar precisión, nunca perder el parking», mismo `zoneOrAsk` y `sustainedStopForSaveMs`. En los paths de confirm el gap sigue siendo Prompt |
| **DET-WALK-ENTERED-ANCHOR-ZONE-001** | 2026-08-16 22:23Z Redmi sesión `1786918991116` — 25.6 min y 96.7 km/h medidos, 47 fixes de conducción, 0 pins: la maniobra final gastó el walk-fix budget y el MISMO contador mudo cerraba el fallback · backlog | El remedio de la duda estaba cerrado por la propia duda → parking medido perdido | En el timeout, con ancla PINNED walk-entered: `doubt = max(anchorStepEventsAtCapture × anchorStrideMeters, anchorWalkInSpanMeters)` — el 2º término habla AUNQUE el contador esté mudo; `bounded = doubt > 0 ∧ anchorRestMs ≥ sustainedStopForSaveMs` (testigo cambiado al ancla por DET-CAR-REST-CLOCK-001) ⇒ SaveZone(WALK_ENTERED_ANCHOR, centro=ancla) (EvalUS:279-294). `anchorWalkInSpanMeters` = d(walkRunOriginFix, ancla) sellado al rebind (CPD:2177-2189); `walkRunOriginFix` = primer fix del run peatonal vigente (CPD:2367-2371). El helper `zoneOrAsk` (EvalUS:300-314) es el ÚNICO sitio de la regla «área = centro + duda acotada; si falta algo → Ask» | CPD:125, 242, 246, 1124, 1162, 1523, 2174-2189, 2364-2371; EvalUS:9, 32, 99-104, 123, 275-294, 300-314; Config:545 | `walkRunOriginFix`, `anchorWalkInSpanMeters`, `anchorStepEventsAtCapture`, `anchorRestMs` (arg) | ✅ EvaluateUnattendedParkingSaveUseCaseTest (cabecera :11 + casos) | Deliberadamente NO relaja UNPINNED_ANCHOR (EvalUS:197). El taint que gestiona lo CREA DET-CREDIBLE-DRIVE-001 — dos tags, un objeto |
| **DET-SHORT-TRIP-FREEZE-001** | 2026-07-12 Oppo, Durango→Glorieta ~2 min (CPDTest:1327); Enamorados: 4 fixes densos (16.6 m) maduran el freeze (`Trace_Enamorados001`:177) | El stop destino de un viaje corto no dura 60 s antes de que el usuario se vaya → el freeze por tiempo nunca maduraba | El reposo se prueba por TIEMPO **o** por EVIDENCIA: `restProvenByTime = (now−startedAt) ≥ anchorFreezeStopMs` ∨ `restProvenByFixes = stoppedFixes.size ≥ anchorFreezeStableFixes` (CPD:2125-2134) | CPD:2125-2130, 2141; Config:351 (única superficie propia) | `stoppedFixes`, `stoppedSince`, `anchorFrozen` | ✅ CPDTest:1327; replay Enamorados:177 | Es un PREDICADO interno de DET-ANCHOR-FREEZE-001 (cláusula OR de `matured`), no un veredicto independiente — §4 |
| **DET-FROZEN-COUNTER-001** | 2026-07-25 22:29 Redmi/Jerez — contador cumulativo MIUI CONGELADO: delta ≈0 sobre un paseo de 150 m → «ride» → pin aproximado dentro del restaurante liberando el pin correcto de 6 min. + «no mute zones» (zona inexplicable sin logcat) · backlog | La inferencia por pasos AUSENTES se creía a un contador congelado; y las zonas eran inauditables desde Firestore | (a) Escalera honest-close: `sessionStepEvents > 0 ∧ stepsSinceStalePin < sessionStepEvents ⇒ FROZEN ⇒ KeepSilent(frozen_counter)` (EvalHC:257-267); `sessionMaxSpeedMps ≥ minimumTripSpeedMps ⇒ la conducción medida ES el ride` (EvalHC:206-222). (b) Save desatendido: zona solo con duda ACOTABLE — ancla unpinned exige contador vivo (EvalUS:192-197); radio con techo `unattendedZoneMaxRadius` (Config:539, CPD:1466-1495). (c) Telemetría completa: `HonestCloseVerdict` + `DetectionEvent.HonestClose` (DetectionEvent.kt:113, 125; CDS:860-882; DetectionEventDto.kt:89), PROMPT_SHOWN en trace remoto (CPD:2451, 2493), snapshots `lastFinished*` (CPD:413-428, finally :1319) | DetectionEvent.kt:113, 125; CPD:413-428, 1319, 1466, 1495, 1566, 2451, 2493; EvalHC:103, 175-177, 206, 257; RunHonestCloseUseCase:11, 63-65; EvalUS:48, 192; CDS:828-834, 860-882; Config:539 | `lastFinishedStepEvents`, `lastFinishedMaxSpeedMps`, `lastFinishedSessionId` (@Volatile), `sessionSawSteps` | ✅ EvaluateHonestCloseUseCaseTest:23, 186; RunHonestCloseUseCaseTest:138; EvaluateUnattendedParkingSaveUseCaseTest:212; CPDTest:1367, 1385, 1406, 2838; replay Enamorados | Tríada del step-budget con DET-TRIP-WITNESS y DET-STEP-BUDGET-ORIGIN (misma escalera, orden fijo). El MISMO walked-vs-rode con guards mute/frozen duplicado en EvalSNC (01:136-138). Rama `REASON_SESSION_MEASURED_DRIVING` defensivamente inalcanzable (§4) |
| **DET-STEP-BUDGET-ORIGIN-001** | 2026-07-22 01:47 Redmi/Glorieta — sello a mitad de egress ~159 m del pin: el paseo restante (~83 m, ~110 pasos) quedó bajo los 129 pasos que exigía la distancia AL PIN → paseo leído como ride → pin dentro de casa · backlog | Presupuesto de pasos comparado contra una distancia con OTRO origen | El sello graba `sealPoint` = posición del CUERPO al sellar; el budget se compara contra `haversine(sealPoint, abortFix)`, nunca contra la distancia al pin. Sin `sealPoint` ⇒ KeepSilent(no_seal_origin) (EvalHC:269-298). Selladores barridos: confirm (ConfirmParkingUseCase:337-339; CPD:1585), manual (SaveManualParkingUseCase:88), BT (BluetoothParkingDetector:141, 167), backfill (ParkingBackfillWorker:119), re-sello safety net (ParkingSafetyNetWorker:259, 632, 745) | CPD:1585; ConfirmParkingUseCase:106, 337-339; EvalHC:167, 269; RunHonestCloseUseCase:57, 110; DetectionStepAnchors.kt:28, 45; AndroidDetectionStepAnchors.kt:18, 49; ParkingSafetyNetWorker:259, 632, 745; BluetoothParkingDetector:141, 167; ParkingBackfillWorker:119; SaveManualParkingUseCase:88; CDS:825 | Fuera del state: seal `{steps, sealPoint, sealedAtMs}` de `DetectionStepAnchors` | ✅ EvaluateHonestCloseUseCaseTest:18, 146; RunHonestCloseUseCaseTest:90 | Precondición del mismo veredicto que FROZEN-COUNTER/TRIP-WITNESS/WALK-FLOOR; lenguaje step-budget duplicado en el safety net (01:136-138) |
| **DET-STEP-SPEED-GATE-001** | FP Avenida de los Mástiles, field 2026-07-12 — confirm EN MARCHA en tráfico stop-and-go: la vibración acumuló pasos fantasma a velocidad de conducción | Pasos fantasma → falso steps+egress + `movementOutrunsSteps` envenenado sosteniendo el ancla a mitad de ruta | (a) gate de CONTEO — con ancla puesta y GPS móvil el paso solo cuenta si `lastSpeedMps < egressStepMaxSpeedMps` (stepJob, CPD:633-641); (b) veto de DECISIÓN — `isRolling = lastSpeedMps > egressStepMaxSpeedMps` veta TODO auto-confirm, y con proofs presentes ⇒ Rejected decisivo (EvalPD:155-160, 162-163, 234-237) | CPD:349, 635, 821; EvalPD:57-61, 155, 234; Config:604 | `lastSpeedMps`, `stepCount`, `bestStopLocation` | ✅ EvaluateParkingDecisionUseCaseTest:65 | Mismo incidente cerró las dos mitades. El mismo FP genérico motiva DET-EGRESS-PEDESTRIAN-CEILING (Rejected paralelo en EvalPD:237-244) |
| **DET-KINEMATIC-EGRESS-001** | Field 2026-07-11 (Redmi) — el outcome que ese viaje debió tener (CPDTest:1252) · backlog | Hardware con contador MUDO no podía confirmar nunca por pasos | `kinematicEgressFixes` acumula fixes banda-peatón de calidad (`speed < minimumTripSpeedMps ∧ acc ≤ minGpsAccuracyForDriving`) SOLO con `anchorFrozen` (CPD:2329-2335). Señal: `anchorFrozen ∧ bestStop≠null ∧ kinematicEgressFixes ≥ kinematicEgressMinWalkFixes` (CPD:1813-1815). En la decisión: `hasKinematicProof = hasKinematicEgress ∧ hasEgressDisplacement ∧ sessionSawDriving` (EvalPD:143-147). Reliability propia (Config:410). Dispara el fast-confirm (CPD:1241-1269) | CPD:198, 1241, 1269, 1809-1815, 2327-2335, 2379; EvalPD:50-56, 143; Config:401, 410 | `kinematicEgressFixes`, `anchorFrozen`, `bestStopLocation` | ✅ CPDTest:1252-1286; EvaluateParkingDecisionUseCaseTest:46 | Exento del ceiling peatonal A PROPÓSITO (EvalPD:126-127, 170). Depende de DET-ANCHOR-FREEZE: sin freeze no hay kinemático |
| **DET-EGRESS-PEDESTRIAN-CEILING-001** | 2026-07-18 Calle Abeto — recogida de pasajero: ~26 pasos fantasma + el coche alejándose ~500 m leyó como steps+egress → pin falso y el parking real posterior perdido | `hasEgressDisplacement` es solo un FLOOR: el coche saliendo de una parada breve también lo satisface | `egressExceedsWalkReach` (CPD:1880-1889): `d(ancla,fix) > stepCount×anchorStrideMeters + accA + accF + egressBirthFloorMeters` (floor GENEROSO: Gavia caminó 68 m con 8 pasos loggeados — solo descarta escala vehicular). Veta los paths step/window (kinemático exento) y con steps+floor presentes ⇒ Rejected (EvalPD:85-98, 121-128, 167-170, 238-244). En el timeout ⇒ Ask(VEHICULAR_EGRESS) por ENCIMA de toda zona — evidencia de AUSENCIA, no duda de precisión (EvalUS:238-243) | CPD:1870-1889; EvalPD:85-98, 121, 167, 238; EvalUS:107, 238-243 | `bestStopLocation`, `stepCount` | ✅ EvaluateParkingDecisionUseCaseTest:294 | Miembro de la familia geométrica ×4 (§3-b). Su precedencia sobre GAP/WALK-ENTERED en EvalUS fue movida por DET-GAP-ANCHOR-ZONE-001 |
| **DET-SENTRY-ARM-PEDESTRIAN-CLOCK-001** | 2026-08-16 23:52 Oppo sesión `1786917152243` — 990 m ANDADOS desde el pin previo; arm sentry-wake (reloj arranca con la caminata ya banked); 3 fixes ESTACIONARIOS a esa distancia dieron drive-proof → espejismo Doppler 42 km/h cold-start desbloqueó `maxSpeed` → 220 pasos plantaron pin en la playa. Variante zona: un pico solo compró zona (EvalUS KDoc :65-72) · backlog | En un arm que no acota cuándo salió el coche, «lejos» certificaba «conducido» | (a) Short-hop: cada fix del run debe MEDIR conducción — `isCredibleDrivingSpeed(fix.speed, fix.acc)` fix a fix (SHP:79-94). (b) Señal vehicular cruda exige un RUN, no un pico: `pendingMax ≥ minimumTripSpeedMps ∧ credibleDrivingFixes ≥ rawDriveSignalMinFixes` (EvalUS:170-176); acumulado en CPD:775-777 | CPD:330, 775-777; EvalUS:65-72, 170-176; SHP:79-94; Config:476-478; CPDTest:1700-1740 | `credibleDrivingFixes`, `pendingMaxSpeedMps`, `shortHopQualifyingFixes` | ✅ CPDTest:1700, EvaluateUnattendedParkingSaveUseCaseTest:171, EvaluateShortHopDriveProofUseCaseTest:18, 68 | Parche transversal SOBRE DET-SHORT-HOP-PROOF y DET-NODRIVE-ZONE: la misma física «un pico no es una conducción» que DET-DRIVE-PROOF aplica al track. Usa la tríada `isCredibleDrivingSpeed`/`isBeyondPedestrianReach` triplicada (01:131-135) |
| **DET-WALK-FLOOR-001** | 2026-07-26 20:28 Oppo/Glorieta — pin colocado A MANO 12 min antes, exacto sobre el coche, liberado por margen de UN paso (16 contados vs 17 requeridos sobre 31.8 m) y re-plantado a 100 m · backlog | El veredicto del budget volcaba por 1 paso en distancias dentro del ruido; y una inferencia deponía una aserción del usuario | (a) Pin user-asserted: `pin.detectionReliability ≥ reliabilityUserConfirmed ⇒ KeepSilent(user_asserted_pin)` — solo conducción MEDIDA (rama :209) libera ese pin (EvalHC:224-234). (b) Walk floor: `walkDistanceMeters ≤ acc_origen + acc_abort + honestCloseMinTripMeters ⇒ KeepSilent(walk_too_short)` (EvalHC:300-312); sobre el desplazamiento del SELLO, no solo del pin | EvalHC:128-143, 224-234, 300-312 | Ninguno del state (lee `pin.detectionReliability` + seal) | ✅ EvaluateHonestCloseUseCaseTest:27, 227 | Miembro de la tríada del step-budget. El floor reutiliza EXACTAMENTE la fórmula del guard `too_close` (EvalHC:199) |
| **DET-TRIP-WITNESS-001** | 2026-07-30 17:53 Redmi/Glorieta — eco de EXIT con el móvil EN CASA; sello de 16 h: delta 0 sobre el paseo de 200 m de anoche → «198 m sin pasos» = trip proven → pin correcto liberado y re-plantado en el sofá · backlog | Delta cumulativo ininterpretable a través de horas de sueño / process death / batching MIUI | `sealAgeMs == null ∨ sealAgeMs > honestCloseMaxSealAgeMs ⇒ KeepSilent(stale_seal)` — el budget solo es interpretable mientras el contador es fiable en TODO el intervalo; sello sin timestamp = viejo (EvalHC:236-247). `sealedAtMs` persistido (DetectionStepAnchors.kt:48; AndroidDetectionStepAnchors.kt:49, 68; ParkingSafetyNetWorker:632, 749); edad calculada en CDS:835-837 | EvalHC:115-126, 172, 236-247; RunHonestCloseUseCase:61, 166; CDS:835-837; DetectionStepAnchors.kt:48; AndroidDetectionStepAnchors.kt:49, 68; ParkingSafetyNetWorker:749; Config:533 | seal `sealedAtMs` | ✅ EvaluateHonestCloseUseCaseTest:289; RunHonestCloseUseCaseTest:35, 166 | Tríada del step-budget. PRECEDE en la escalera a mute/frozen/no_seal_origin (EvalHC:242 < 251 < 262 < 275) — precedencia, no redundancia (§4) |
| **DET-HONEST-CLOSE-001** | FN Camelias hop 2026-07-14 19:19Z Redmi (hop 300 m, EXIT entregado con el viaje acabado → `aborted_false_enter` silencioso, pin rancio, nada preguntado) + contraejemplo 2026-07-15 02:11Z Oppo (EXIT tardío con el usuario a 1.1 km A PIE: el silencio era CORRECTO) · backlog | El abort silencioso descartaba la evidencia de salida que la sesión ya tenía | Doctrina: *ninguna sesión armada por trigger real acaba en SILENCIO si el coche provablemente se alejó de su pin*. Tras `outcome ∈ {aborted_false_enter, aborted_no_movement}` (CDS:1482, gate :818) el servicio corre la escalera desde el FGS vivo, `NonCancellable` (CDS:816-884, disparo :1289-1296). Escalera pura (EvalHC): rung 1 pin aproximado (`acc ≤ minGpsAccuracyForDriving`), rung 2 zona (`radio = max(acc, honestCloseMinZoneRadiusMeters)`), rung 3 silencio (9 razones tipadas). Infra: snapshots `lastFinished*` sellados en el finally ANTES del reset (CPD:398-428, 1314); sello baseline al confirmar (ConfirmParkingUseCase:333-340); persistencia zona (UserParkingEntity.kt:51, Migrations.kt:159, AppDatabase.kt:31, UserParking.kt:61, 92, ParkingSessionMapper.kt:105); DI (DomainModule.kt:129-135, AndroidDetectionModule.kt:43) | CDS:107, 810-884, 1289-1296, 1482; CPD:398-428, 1314; EvalHC (todo); RunHonestCloseUseCase:23; + Room/mapper/DI | `sessionOutcome`, `lastFinishedFix`, `lastFinishedSessionId`, `lastFinishedStepEvents`, `lastFinishedMaxSpeedMps` (@Volatile, canal post-invoke) | ✅ replay `Trace_CameliasHop001` + `Trace_LateExitOnFoot001`; EvaluateHonestCloseUseCaseTest (18); RunHonestCloseUseCaseTest (5) | Espejo DECLARADO de EvalSNC — mismo walked-vs-rode duplicado (01:136-138). BUG-WALK-DEPART-001 es el límite de su rung 3. Con DET-NODRIVE-ZONE: dos salidas honestas para dos momentos (abort inmediato vs timeout) |
| **DET-NODRIVE-ZONE-001** | 2026-07-27 18:36Z Redmi 2201117TY — EXIT retenido 4 110 m por MIUI: la sesión nace TRAS el viaje, en el bordillo; 176 pasos de egress + AR exit → `confirmed_unattended_zone_no_drive_egress`. El espejismo del mismo día (14:56, indoor) debía seguir muerto · backlog | Sesión nacida en destino no puede corroborar la conducción → parking real perdido; pero el mismo camino no debía resucitar el mirage indoor | En el timeout con `¬measuredDriving`: SaveZone(NO_DRIVE_EGRESS, centro=ancla, `doubt=max(d(ancla,fix), steps×stride)`) ⟺ `ancla≠null ∧ liveEgress ∧ vehicularSignal`, con `liveEgress = sessionSawSteps ∧ stepCount ≥ anchorLockEgressSteps ∧ d ≥ minEgressDisplacementMeters` y `vehicularSignal = vehicleExitConfirmed ∨ (pendingMax ≥ trip ∧ credibleDrivingFixes ≥ rawDriveSignalMinFixes)`; si no ⇒ Ask(NO_DRIVE) (EvalUS:157-186) | CPD:335; EvalUS:71, 162-186; Config:478 | `maxSpeedMps`, `pendingMaxSpeedMps`, `credibleDrivingFixes`, `sessionSawSteps`, `stepCount`, `vehicleExitConfirmed` | ✅ replay `Trace_RedmiLateExitHome001` (DetectionTraceReplayTest:477) + anti-resurrección CPDTest:1023-1133; EvaluateUnattendedParkingSaveUseCaseTest:152 | Comparte `vehicularSignal` con DET-SENTRY-ARM-PEDESTRIAN-CLOCK. DET-UNVERIFIED-ARM-DRIVE-PROOF reduce su población |
| **DET-CREDIBLE-DRIVE-001** | 2026-07-15 Camelias-Oppo (pin DENTRO de la casa: contador mudo dejó que el walk-back lavara como conducción; ancla en la puerta a 37 m del coche) + 2026-07-16 Galeote 31 (FN: la deceleración final tiñó el ancla walk-entered) + 2026-07-15 Enamorados (MIUI mató la accuracy de cada fix — el unfreeze necesitaba desplazamiento) · backlog | La banda ambigua Doppler con contador mudo lavaba caminatas como coche (FP) o teñía deceleraciones como caminata (FN) | Tres piezas: (a) `isSustainedDepartureFromAnchor` (CPD:1902-1923): `speed ≥ clearBestStopSpeedMps ∧ d > accs + sustainedDepartureFloorMeters ∧ rate ∈ [minimumTripSpeedMps, sustainedDepartureMaxRateMps]` ⇒ CAR aunque ningún fix sea creíble. (b) `isCorroboratedVehicleHop` (CPD:1934-1944): hop provable rescata la banda ambigua con contador mudo (CPD:2250-2251). (c) Taint `isAnchorWalkEntered` (CPD:1801-1807): `anchorWalkFixesAtCapture > anchorFreezeMaxWalkFixes` salvo EXENCIÓN DE MANIOBRA (`anchorStepEventsAtCapture==0 ∧ anchorSawStepsAtCapture ∧ walkFixes ≤ maneuverEntryMaxWalkFixes`) ⇒ Prompt, nunca pin (EvalPD:69-75, 223) | CPD:216, 297, 1789-1807, 1891-1944, 2163-2166, 2226-2251, 2272, 2285-2290; EvalPD:69, 218; EvalUS:272-278; Config:446+ | `anchorWalkFixesAtCapture`, `anchorStepEventsAtCapture`, `anchorSawStepsAtCapture`, `previousFix`, `walkFixesSinceDriving` | ✅ replay ×3 (`Trace_Enamorados001`:36, `Trace_CameliasOppo001`, `Trace_GaleoteOppo001`); CPDTest:2918 | `isSustainedDepartureFromAnchor` vs `corroboratesDrive` (DET-DRIVE-PROOF): dos corroboraciones por desplazamiento con umbrales compartidos y destinos distintos. El taint que crea lo consume DET-WALK-ENTERED-ANCHOR-ZONE en el timeout |
| **DET-DRIVE-PROOF-001** | 2026-07-27 — espejismo Doppler: 45 m/s a acc 5 m con el móvil en el SALÓN encendió `maxSpeed` de sesión → `sessionSawDriving` → el path kinemático pinó el salón · backlog | Un solo pico creíble encendía «esta sesión midió conducción» | `maxSpeedMps = pendingMax` SOLO si `driveProven` (CPD:815); `driveProven = latch ∨ shortHopProven ∨ (credibleSpeedFix ∧ speed ≥ trip ∧ corroboratesDrive)` (CPD:799-801). `corroboratesDrive` (CPD:1961-1982): contra un look-back de edad `[driveProofWindowMinMs..MaxMs]`: `d > accs + margen ∧ d ≥ minimumTripDistanceMeters ∧ rate ≤ sustainedDepartureMaxRateMps ∧` los fixes de la mitad tardía han DEJADO el look-back (`≥ d × DRIVE_PROOF_PROGRESS_FRACTION`, CPD:2529) — un drive progresa por su ventana, un mirage es plano-y-salto. Ring acotado `recentFixes` (CPD:1984-1988, 2535-2539) | CPD:324-344, 764-818, 1946-1988, 2529-2539; EvalUS:60-63; DetectParkingDepartureUseCase:145; SHP KDoc:11; Config:485+ | `driveProven`, `maxSpeedMps`, `pendingMaxSpeedMps`, `recentFixes` | ✅ CPDTest:1842-1890, 2975; replay `Trace_RedmiLateExitHome001`:13, DetectionTraceReplayTest:480 | Par con DET-SHORT-HOP-PROOF: DOS pruebas independientes del MISMO latch `driveProven`. Física hermana de `isSustainedDepartureFromAnchor` (DET-CREDIBLE-DRIVE) |
| **DET-SHORT-HOP-PROOF-001** | 2026-08-14 22:56 Oppo sesión `1786740987649` — hop 900 m, 303 fixes, pico 30 km/h, 104 pasos de egress, `drive 3/303` → `maxSpeedMps=0` → «no measured driving» → parking PERDIDO (nudge sin respuesta) (`94267552`) · backlog | Un hop urbano stop-and-go jamás sostiene la forma de ventana que `corroboratesDrive` exige | Prueba por DESPLAZAMIENTO anclada al PIN QUE EL COCHE DEJÓ (nunca al primer fix de la sesión — eso regalaría el origen al mirage): `consecutiveQualifyingFixes ≥ shortHopProofFixes` con cada fix cumpliendo `acc ≤ minGpsAccuracyForDriving ∧ isCredibleDrivingSpeed ∧ d(pin,fix) ≥ shortHopProofFloorMeters ∧ d > accA+accF+fenceRadius ∧ isBeyondPedestrianReach(d, elapsed, fence, accs)` (SHP:54-109). Alimenta `driveProven` (CPD:778-799); `departureAnchor` viaja del servicio (CDS:1276-1286; CPD:501-508) | SHP (todo); CPD:121, 344, 501-508, 778-799, 803, 820; CDS:1276-1286; Config:465+ | `shortHopQualifyingFixes`, `driveProven` | ✅ EvaluateShortHopDriveProofUseCaseTest (12); CPDTest:1564-1645, 2932 | Par de DET-DRIVE-PROOF (mismo veredicto). DET-SENTRY-ARM-PEDESTRIAN-CLOCK es cláusula interna suya (SHP:79-94). DET-UNVERIFIED-ARM-DRIVE-PROOF es esta física sobre arms sin verificar (CPDTest:1620) |
| **DET-BIKE-NOT-A-CAR-001** | Field 2026-08-16 — FP bici: perfil CAR sobre una bicicleta re-pinó el coche a 4.8 km (EvalPD:99-104); variante 11:08Z Samsung: 59 min de bici a Los Toruños acabó en `unattended_zone_unpinned_anchor` a 4.8 km de un Mercedes que nunca se movió (EvalUS:112-118) · backlog | El perfil responde «qué conduces», no «en qué vas AHORA» | AR `ON_BICYCLE` ENTER estampa `bicycleRideAtMs` (carril de EVIDENCIA, jamás arma — ActivityRecognitionManagerImpl:118-137; ActivityTransitionReceiver:49-83); un boarding posterior SUPERSEDE la bici (`vehicleRideAtMs`; timestamps comparables). `humanPoweredRide = isHumanPoweredRide(...)` (función pura `domain/detection/HumanPoweredRide.kt`; CPD:1341-1375; ventana Config:555). Veta el auto-confirm (EvalPD:206-210 → Prompt) Y el save desatendido como PRIMERA cláusula — evidencia de que el COCHE no se movió: Ask(HUMAN_POWERED) (EvalUS:145-149) | CPD:279, 285, 1341-1375; HumanPoweredRide.kt:7-13; EvalPD:99-104, 206-210; EvalUS:112-118, 145-149; ActivityTransitionReceiver:49, 55, 83; ActivityRecognitionManagerImpl:118, 137; Config:555 | `bicycleRideAtMs`, `vehicleRideAtMs` | ✅ HumanPoweredRideTest (7); CPDTest:1770-1836 (incl. bici→coche = viaje de COCHE) | Precede a TODO en EvalUS. Complementa BUG-SCOOTER-001 (perfil humano) — misma pregunta, eje distinto (garaje vs viaje) |
| **DET-G-04** | 2026-07-01 El Puerto — GEOFENCE_EXIT armó una sesión que nunca re-vio velocidad en un short hop → el guard false-enter descartaba el parking real (CPDTest:674). Su seed incondicional causó luego BUG-REPARK-WALK-001 | Arm a MITAD de viaje: el stream se calienta con la conducción ya acabada | `armEvidence.isVerifiedDeparture ⇒ seed hasEverReachedDrivingSpeed=true` al entrar en invoke (CPD:522-530; doc :480-485). SOLO arms verificados: Manual/Unverified conservan todos los guards anti-walking (regresión guardada en CPDTest:720-742) | CPD:485, 522-530; CDS:1255-1260; VerifyDepartureEvidenceUseCaseTest:47 | `hasEverReachedDrivingSpeed` | ✅ CPDTest:668-742; DetectionTraceReplayTest:72 | DET-G-05 es su corrección (gate del seed). El enter-arm step veto (CPD:615-624) puede DESHACER el seed en 2 escrituras no atómicas desde el stepJob (02 §7.1) |
| **DET-G-05** | BUG-REPARK-WALK-001, 2026-07-03 22:13Z El Puerto — parkeo real, paseo, EXIT armó SEEDED sobre el peatón → steps+egress re-confirmó un parking falso ~120 m del coche (`Trace_BugReparkWalk001`:11) | El EXIT solo prueba que el TELÉFONO salió del radio; caminar también lo dispara | Pre-arm: `VerifyDepartureEvidenceUseCase` decide el `ArmEvidence` (velocidad creíble de un fix FRESCO — `freshFixMaxAgeMs` — o ENTER en ventana + admisibilidad session-birth + corroboración `isBeyondPedestrianReach`) (CDS:587-634; VerifyDepartureEvidenceUseCase:9-16). Upgrade tardío: `DepartureDetectionWorker` → `notifyDepartureConfirmed()` siembra SOLO la sesión viva y marca `LABEL_VERIFIED_LATE` (CPD:450-463; RunDepartureCheckUseCase:132; ArmEvidence.kt:58; DepartureConfirmationListener.kt:8). `verified_late` es evidencia DÉBIL (no salva silencioso sin `sessionSawDriving`, EvalPD:177-200). Un veredicto entre sesiones NO se filtra a la siguiente (CPDTest:1944-1966) | CPD:450-463; CDS:94, 587-634, 1153, 1261-1265; VerifyDepartureEvidenceUseCase:9; ArmEvidence.kt:58; DepartureConfirmationListener.kt:8; RunDepartureCheckUseCase:132; DomainModule.kt:108 | `hasEverReachedDrivingSpeed`, `currentArmEvidence` (@Volatile) | ✅ CPDTest:1897-1966; VerifyDepartureEvidenceUseCaseTest (10) | SOLAPE mayor: la tríada de evidencia (velocidad creíble + ENTER-ventana + `isBeyondPedestrianReach`) TRIPLICADA con `DetectParkingDepartureUseCase` y `EvalSNC` (01:131-135) — §3-a |
| **BUG-WALK-DEPART-001** | Histórico (backlog `detection-departure-bugs-2026-06-05.md`; fecha exacta NO VERIFICADO): caminar cerca del coche dentro de `vehicleEnterWindowMs` confirmaba una salida falsa | Distancia+velocidad solas no distinguen «el coche se fue» de «el dueño pasea» — el nag afirmaría que el coche está donde el peatón | (a) `departureEventBus.reset()` al confirmar un parking: la detección de salida solo confía en un IN_VEHICLE_ENTER NUEVO posterior al pin (ConfirmParkingUseCase:284-288; re-seed al mover el pin, UpdateParkingLocationUseCase:71). (b) Doctrina replicada como límite: safety net PREGUNTA en vez de liberar (EvalSNC:55; ParkingSafetyNetWorker:75), honest-close rung 3 = silencio (EvalHC:42), departure-check inconclusive-forever sin ENTER (RunDepartureCheckUseCase:22, 112), espejo BT (EvaluateBtParkUseCase:39) | ConfirmParkingUseCase:287; UpdateParkingLocationUseCase:71; RunDepartureCheckUseCase:22, 112; EvalSNC:55; EvalHC:42; EvaluateBtParkUseCase:39; ParkingSafetyNetWorker:75 | Fuera del state: `departureEventBus` | ✅ ConfirmParkingUseCaseTest:417; RunDepartureCheckUseCaseTest:77; `Trace_LateExitOnFoot001`:14 | Doctrina-MADRE de la asimetría en salidas: DET-HONEST-CLOSE (rung 3), safety net y BT la citan como límite — es el techo que los demás no cruzan |
| **DET-UNVERIFIED-ARM-DRIVE-PROOF-001** (solo-test) | Field 2026-08-15 21:26 Redmi — MIUI se comió el EXIT; hop late-armed que descansa ~1.13 km del pin (`e9186a52`) · backlog | Un arm sin verificar cuyo stream SÍ midió la salida perdía el parking | Fijada SOLO por test (sin comentario-tag en producción — 05 §Inversos): un arm unverified cuyo stream mide la salida vía short-hop drive proof PRUEBA la conducción → el parking sobrevive (CPDTest:1686); el complemento sigue: desplazamiento sin salida medida jamás pina en silencio (CPDTest:1620-1645). El mecanismo de producción ES DET-SHORT-HOP-PROOF operando sobre arms no verificados | Producción: sin tag (código `e9186a52` = maquinaria SHP/CPD:778-799). Tests: CPDTest:57, 1620, 1686, 2937, 2953 (fixture inline del field 15-08) | `shortHopQualifyingFixes`, `driveProven`, `hasEverReachedDrivingSpeed` | ✅ (solo-test, fixture de campo inline) | SOLAPE de etiqueta: dos tags (este + DET-SHORT-HOP-PROOF) sobre UNA física — candidato a fundirse en la ficha del futuro dueño `DriveProof` |
| **LOC-001** | NO VERIFICADO (guard histórico pre-diagnósticos; sin fecha/dispositivo) | El ancla se re-capturaba con fixes tardíos del mismo stop → deriva del pin | `bestStopLocation` se congela tras la ventana inicial del stop: `mayCapture = ¬pinnedToOtherStop ∧ (withinInitialWindow ∨ sameStopPreEgress)` con `withinInitialWindow = (now−startedAt) < initialStopWindowMs` (30 s) (CPD:2095-2115); el spot es el fix de menor accuracy de `stoppedFixes` (`bestFix`, CPD:356) | CPD:2095 (única ocurrencia del tag); mecanismo :2095-2115; campos en 02 fila 2 | `stoppedFixes`, `bestStopLocation`, `stoppedSince`, `stepCount` | ⚠ 05 lo lista 🔴, pero CPDTest:399 cita «the original LOC-001 contract» — cobertura indirecta comentada (corrección a 05) | PARCIALMENTE DOMINADO por DET-ANCHOR-FREEZE-001 (§4): el corte a 30 s solo decide ya en la franja «mismo stop, tras el primer paso contado» |
| **LOC-002** | NO VERIFICADO fecha/dispositivo; escenario fijado en test: alucinación GPS con velocidad de conducción aparente y MALA accuracy no debe limpiar `bestStopLocation` (CPDTest:344-368) | Un fix-ruido «a velocidad de coche» borraba el ancla del parking real | Toda decisión «driving» exige accuracy creíble: `isDriving = speed ≥ clearBestStopSpeedMps ∧ acc ≤ minGpsAccuracyForDriving` (CPD:2205-2206, descarte :2216-2223); el MISMO gate de 50 m protege el flip de `hasEverReachedDrivingSpeed` (`credibleSpeedFix`, CPD:745-753) | CPD:749 (tag), 745-753, 2205-2223; Config:617 | `bestStopLocation`, `hasEverReachedDrivingSpeed`, `lastSpeedMps` | ⚠ 05 lo lista 🔴, pero CPDTest:344, 362, 368, 427, 442, 469 lo citan por nombre (corrección a 05) | PARKING-001 existe PORQUE LOC-002 preservaba de más (ruido a velocidad de maniobra necesitó su propio camino, CPDTest:427-469). DET-CREDIBLE-DRIVE lo puentea deliberadamente cuando NINGÚN fix es creíble |

### 1.2 Cluster F2 — decisión de confirmación, ciclo de vida del coordinator, tags históricos (26 + 1 anexo solo-test; BUG-SERVICE-109 fusionado aquí con su reflejo androidMain)

Leyenda extra de este cluster: **GUARDA-CÓDIGO** = el tag anota una condición ejecutable viva ·
**SOLO-COMENTARIO** = marca histórica/doctrinal sin condición propia. «rama N» = ramas de
precedencia del collect según 02 §4.

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-C-01** | 2026-06-25 (doctrina tras FP de EXIT espurio en parada larga; PARKING-DETECTION.md:1184) | Un `IN_VEHICLE_EXIT` espurio en un atasco publicaba plaza fantasma: AR-exit + dwell confirmaban solos | `!input.hasEgressDisplacement -> false` en confirmNow: el egress (desplazamiento ≥ `minEgressDisplacementMeters` desde el ancla) es OBLIGATORIO para TODO `ParkingDecision.Confirmed`; STILL/dwell/AR-exit solos mantienen `Inconclusive` | EPD:111 (KDoc), EPD:165-166 (gate); Config:710 (por DET-C-01 se eliminó `reliabilitySlowPath`) | `hasEgressDisplacement` (derivado de `bestStopLocation`) | ✅ EvaluateParkingDecisionUseCaseTest:136, :365 | SOLAPE conceptual con DET-EGRESS-PEDESTRIAN-CEILING-001: C-01 es el SUELO del egress, el ceiling su techo (EPD:66-67). GUARDA-CÓDIGO |
| **DET-C-02** | 2026-06-26 "buy tobacco" (errand stop; PARKING-DETECTION.md:251) | Parar, andar al kiosco y seguir conduciendo pinneaba la parada del recado | Todo egress-confirm automático es TENTATIVO: `beginConfirm` (CPD:1445-1463) registra `PendingConfirm` si `confirmHoldMs>0` (default 2 min, Config:680); rama 1 del collect (CPD:845-921): (1b) `userConfirmedParking ∨ heldMs ≥ confirmHoldMs` → finaliza EN el pin holdeado; (1c) `drivingResumed` → descarta y re-ancla | CPD:118-120, 139-148, 158-161, 845-921, 1437-1463, 1716; Config:669-680; watchdog por reloj CPD:684-719 [DET-AUDIT-002 T7] | `pendingConfirm`, `userConfirmedParking`, `isAnchorPinned` | ✅ CPDTest:2487-2727 (5 tests), replay DetectionTraceReplayTest:390 (hold real Camelias-Oppo) | Con DET-CONFIRM-FRESHNESS-001: 1a y 1c son dos mitades de la física del errand. Con BUG-COORD-115: el hold 1b resuelve el "Sí" ANTES de la rama 5 (§4). GUARDA-CÓDIGO |
| **DET-D-01** | — (doctrina 2026-06-25) | Las dos mitades (parking/salida) hablaban vocabularios de corroboración distintos | `ParkingDecision` es espejo de `DepartureDecision` — mismo lenguaje Confirmed/Rejected/Inconclusive | EPD:9 (única ocurrencia) | — | 🔴 ninguno menciona el tag | SOLO-COMENTARIO (doctrinal), sin condición ejecutable propia |
| **DET-D-02** | — (refactor 2026-06-25; PARKING-DETECTION.md:1199) | La precedencia de 9 paths vivía inline en el coordinator, intesteable | La decisión de candidate es FUNCIÓN PURA de primitivas (`ParkingDecisionInput`), replayable sin corrutinas; el orquestador solo conserva side effects | EPD:30, :109; CPD:1685-1686 (seam de delegación) | ninguno (esa es la regla) | 🔴 ninguno menciona el tag (los 33 tests de EPDTest lo ejercitan sin nombrarlo) | SOLO-COMENTARIO (marcador arquitectónico; la "condición" es la frontera pura misma) |
| **DET-D-03** | Traza 2026-06-26: el confirm esperó ~16 s un AR EXIT ya redundante (PARKING-DETECTION.md:242, :1122) | En hardware con EXIT tardío/ausente la detección era frágil; STILL retirado como señal | Rama 9 del collect (CPD:1232-1289): `stepCount ≥ minStepsToConfirm ∨ hasKinematicEgressSignal` → `evaluateParkingDecision(elapsedSinceHighMs=0)` → confirm SIN esperar AR EXIT ni CANDIDATE; AR EXIT queda como hint no decisivo | CPD:74 (KDoc path 4, supersede BUG-OPPO-LATE-CONFIRM), 1232-1240, 1269; ActivityTransitionReceiver.kt:36; iosMain IosActivityRecognitionManagerImpl.kt:29 | `stepCount`, `kinematicEgressFixes`, `vehicleExitConfirmed` | ✅ CPDTest:2101-2123 | Supersede explícitamente a BUG-OPPO-LATE-CONFIRM. GUARDA-CÓDIGO |
| **DET-D-04** (anexo solo-test) | — | — | Etiqueta de cobertura: "unit coverage for the pure candidate-phase decision" | SOLO commonTest EvaluateParkingDecisionUseCaseTest.kt:11 — cero ocurrencias en producción | — | ✅ (es el header del test, 33 @Test) | SOLO-COMENTARIO; nombra la contrapartida de DET-D-02 |
| **DET-CONFIRM-ANCHOR-001** | 2026-08-11 16:08 (backlog): 32 driving fixes, contador casi mudo, "Sí" tardío | El pin se plantó en el DESTINO peatonal — el usuario respondió tras irse andando | Rama 5 (user-confirm), sub-rama else: si la parada fue PRESENCIADA (`bestStopLocation` no gap-entered) y el "Sí" llega a >`USER_CONFIRM_NEAR_CAR_MAX_METERS` (100 m) del ancla Y del egress-birth → re-anclar en el witnessed stop, no en el fix actual; responder cerca del birth conserva el comportamiento previo (Enamorados: ahí el birth ES el coche) | CPD:1062-1098, 2553-2558 | `bestStopLocation`, `egressOriginFix`, `anchorGapMsAtCapture` (veto del gap), `userConfirmedParking` | ✅ CPDTest:2253-2384 (3 asserts) | Convive en la MISMA rama con DET-ANCHOR-EGRESS-001/DET-GAP-ANCHOR-001 (CPD:1053-1059): aquellos deciden SI se usa el ancla; este decide QUÉ hacer cuando no. GUARDA-CÓDIGO |
| **DET-CONFIRM-FRESHNESS-001** | Field 23/24-07-2026, Redmi (backlog): FP "Bodegas Osborne" (semáforo, 160 m de creep con contador vivo callado), FP Calle Abeto 22:52 (hold liquidó con el coche a 570 m) | El hold finalizaba con el snapshot de hace 2 min sin mirar dónde está el coche; el silencio de un contador VIVO se trataba igual que uno muerto | Dos guards: (a) settle-time re-validation, rama 1a (CPD:864-886): `!userConfirmedParking && heldMs ≥ confirmHoldMs && heldConfirmOutrunByVehicle(...)` → descarte `HOLD_STALE_DISCARDED`; (b) salida stepless (CPD:2252-2283, máquina 8): `anchorPinned ∧ sessionSawSteps ∧ stepCount==0 ∧ speed ≥ clearBestStopSpeedMps ∧ escapesAnchorEnvelope` durante ≥ `frozenAnchorSteplessDepartureFixes` fixes → CAR, des-pinnea. Más la re-validación del save desatendido (EvalUS:229) | CPD:224-292, 642-650, 691-693 (watchdog NO re-valida, asimetría documentada — §5), 864-886, 1836-1868, 2167-2173, 2252-2283, 2373-2377; Config:381, 393 | `stepEventsSinceDriving`, `anchorStepEventsAtCapture`, `anchorSawStepsAtCapture`, `sessionSawSteps`, `pinnedSteplessMovingFixes` | ✅ CPDTest:2703-2878 (4 tests), EvaluateUnattendedParkingSaveUseCaseTest:331 | SOLAPE de física con DET-EGRESS-PEDESTRIAN-CEILING-001 (familia geométrica ×4, §3-b). GUARDA-CÓDIGO |
| **DET-UNVERIFIED-CONFIRM-001** | 2026-08-13 20:56, Oppo, Calle Góndola (backlog): espejismo Doppler 24,8 km/h acc 2,9 m en el PRIMER fix frío de un sentry-wake | Salió de casa ANDANDO y la app re-pinneó el coche a 7 m del parking anterior (270 pasos + egress confirmaron en silencio) | `LABEL_SELF_OBSERVED ∈ weakLabels` (EPD:193-197): con `autoConfirmRequiresStrongEvidence` y `!sessionSawDriving` (drive-proof, no el latch del espejismo) → `weakEvidenceOnly` → `Prompt`, nunca Confirmed | EPD:185-200 (una línea en el set + comentario) | `evidenceLabel` (de `currentArmEvidence` @Volatile CPD:467), `maxSpeedKmh` gated por `driveProven` | ✅ EvaluateParkingDecisionUseCaseTest:195 (fixture del field 13-08) | Es UNA ADICIÓN al predicado `weakEvidenceOnly` de DET-SOLID-001 — dos tags, una condición (§3-a). GUARDA-CÓDIGO |
| **DET-JAM-WINDOW-001** | Plan Driversnote pieza 3, impl. 2026-07-30 (backlog) | Salir de la plaza y clavarse en atasco sin llegar a 18 km/h mataba la sesión a los 4 min → viaje sin cobertura | Rama 3 (CPD:943-1001): al vencer `maxNoMovementMs`, si `!staleExitDelivery && recentCreep ≥ jamCreepMinMeters` (en `creepWindow` sobre `jamCreepWindowMs`=2 min, fixes acc ≤ 50) y edad ≤ `jamExtendedNoMovementMs` → NO abortar; si aún vence → outcome `aborted_no_movement_jam` + `NO_MOVEMENT_JAM_FOLD` | CPD:538-543, 943-1001, 2524-2527; Config:227-243 | locals `creepWindow`, `jamExtensionLogged` (fuera del StateFlow, 02 §1c); `hasEverReachedDrivingSpeed` | ✅ CPDTest:771 | Colisión con DET-ZOMBIE-PROBE-001 RESUELTA explícitamente: `!staleExitDelivery` (CPD:966) — un zombi jamás gana la extensión. GUARDA-CÓDIGO |
| **DET-ZOMBIE-PROBE-001** | Field noche 24→25-07-2026 (backlog): Oppo 3 sesiones + Samsung 1, todas 4,1 min de GPS con vmax ≤3 km/h y 0 pasos, armadas far-delivered (d=1,9-3,8 km) | Cada EXIT zombi quemaba 4 min de GPS/FGS → alimentaba el scoring de "abuso de batería" MIUI/ColorOS | Presupuesto corto para arms stale-lane: `noMovementBudgetMs = if (staleExitDelivery) staleExitNoMovementMs (75 s) else maxNoMovementMs` (CPD:956-957). Física: un EXIT real entregado lejos muestra fixes de conducción en segundos; solo aplica a evidencia unverified | CPD:492-500, 940-957; Config:213; CDS:643, 1156, 1275 (clasificación stale-lane) | param `staleExitDelivery`; `hasEverReachedDrivingSpeed` | ✅ CPDTest:876 | La clasificación stale-lane la produce DET-EXIT-TRUST-001; este tag consume el booleano. Exclusión mutua explícita con DET-JAM-WINDOW-001. GUARDA-CÓDIGO |
| **DET-RECONCILE-001** | Field 2026-07-06 (backlog): ambos móviles perdieron parkings reales; Redmi perdió uno por notificación no vista 15 min | Sistema edge-triggered: si el evento no llega, fallo silencioso permanente; el response-timeout DESCARTABA la sesión | Doctrina level-triggered (el estado se re-deriva periódicamente). En este cluster: rama 7 — response-timeout SAVE-don't-discard (CPD:1115-1121): prompt sin respuesta tras `confirmationResponseTimeoutMs` → `EvaluateUnattendedParkingSave` → SaveExact con `reliabilityUnattendedSave=0.5` (asimetría de costes: perder ≪ corregir con un tap) | CPD:1115-1121, 1191-1194; Config:278, 286, 703-708; resto de sus ×27 en EvalSNC, RunDepartureCheckUseCase:55/65/137, ProcessConfirmedDeparture:46, GetOneLocation:25, StepCounterSource:4 (cluster safety-net) | `phase.promptShownAt`, veredicto puro externo | ✅ 4 ficheros (matriz 05) | Tag SISTÉMICO; su rama 7 delega ya en las zonas de DET-WALK-ENTERED/GAP-ANCHOR-ZONE. GUARDA-CÓDIGO |
| **DET-PHASE-001** | — (UX: el chip leía "Aparcando…" durante la mayor parte de un viaje normal) | LowReached/Notified saltan en CUALQUIER semáforo → teñían toda la conducción | Solo `ConfirmationPhase.Candidate` mapea a `DetectionPhase.Candidate`; el resto es Driving (`toDetectionPhase`, CPD:2570-2573). Emisión reactiva vía `phaseSink` con `distinctUntilChanged` (CPD:674-682); reset a Idle al conducir | CPD:114-117, 674-682, 2570-2573; DetectionRuntimeState.kt:28/42/92/146; HomeTripController.kt:64/114/207; DrivingPuck.kt:26, DetectionReadiness.kt:36; mock StateGallery/DevCatalog | `phase` → sink externo | ✅ ConfirmationPhaseMappingTest (1), HomeTripControllerTest:166 | Sin colisión — MAPEO de solo lectura sobre la máquina de REFACTOR-200. GUARDA-CÓDIGO |
| **DET-SOLID-001** | Auditoría SOLID + BUG-REPARK-WALK-001 (2026-07-03, El Puerto) | Paseos confirmaban parks; evidencia ENTER-only (falsificable por bus/taxi) salvaba en silencio; señal STILL inalcanzable | Familia con sub-índices; en este cluster: (C1) STILL eliminado → fast path techo Medium (Config:25/47/709); (C2+B) `weakEvidenceOnly` y `humanPowered` → `Prompt` (EPD:138-141, 177-210); (B4) enter-arm step veto degrada evidencia (CPD:610-623); repark implausible → degrade a prompt (CPD:1617-1634); `degradeToPrompt` (CPD:1748-1767); doctrina "cada bug de campo → fixture" (harness de replay) | ×54 en producción (DetectionEvent.kt:38 y 20+ ficheros); aquí CPD:610-623, 1617-1634, 1748-1755, 2543-2547; EPD:23, 48, 138, 177, 202 | `currentArmEvidence` (@Volatile :467), `maxSpeedMps`, `stepCount`, `hasEverReachedDrivingSpeed` | ✅ 13 ficheros + todo el harness de replay | Tag PARAGUAS: DET-UNVERIFIED-CONFIRM-001 es una enmienda a sus weakLabels; BUG-DETECT-310503 quedó absorbido por su C1 (§4). GUARDA-CÓDIGO |
| **DET-VERDICT-NOT-PREDICATE-001** | — (doctrina `e5df4cc6`, 2026-08, solo doc: 45 casos de uso + coordinator de 2.573 líneas = el mismo problema) | Predicados puros atrapados como métodos privados del coordinator (11) y veredictos triviales ascendidos a caso de uso | Un caso de uso por VEREDICTO (citable en diagnóstico); predicado compartido → función pura top-level en `domain/detection/` (patrón `HumanPoweredRide`, `SentryWakeCooldown`…) | HumanPoweredRide.kt:13 (ejemplo canónico); CLAUDE.md y docs/architecture; 02 §6 nombra los futuros dueños `AnchorTrust`/`DriveProof` | — | ✅ HumanPoweredRideTest, EvaluateShortHopDriveProofUseCaseTest mencionan el tag | SOLO-COMENTARIO (doctrina organizativa; ⛔ su código no empieza hasta validar los 4 fixes de campo) |
| **PARKING-001** | Field Redmi Note 11 2026-05-30 (Config:640-642): tormentas de 5 ráfagas acc=22-48 m limpiaban el ancla estando aparcado | «Espera + maniobra a la plaza»: el ancla se congelaba 10-15 m antes de la plaza real; y el ruido GPS limpiaba anclas buenas | Burst de reposición: ≥`repositionFixCount`(3) fixes CONSECUTIVOS a speed ≥ `repositionSpeedMps`(1.7) con acc ≤ `repositionMaxAccuracyMeters`(15) → `isRepositionBurst` → `shouldClearBestStop` (CPD:2306-2322); reset en cada fix parado (CPD:2199-2200); HOY además vetado por `anchorPinned` y exige `outruns` (CPD:2312-2313, aportes posteriores de ANCHOR-LOCK/FREEZE) | CPD:2199-2200, 2214-2215, 2306-2322 (máquina 9); Config:620-643 | `consecutiveRepositionFixes`, `bestStopLocation` | ✅ CPDTest (matriz 05) | Territorio RECORTADO por ANCHOR-LOCK/DET-ANCHOR-FREEZE — sigue siendo la única vía de clear a velocidad de maniobra PRE-lock; NO dominado (§4). GUARDA-CÓDIGO |
| **BUG-COORD-112** | — (refactor REFACTOR-201; sin fecha/dispositivo en repo ni PARKING-DETECTION.md — NO VERIFICADO) | Un throwable del `StepDetectorSource` cascadeaba y CANCELABA el scope padre — moría todo el loop de detección | try/catch en stepJob (CPD:599-668): re-lanza `CancellationException` (cooperativa), loguea el resto → el stepping degrada a los paths window/kinemático | CPD:594-598 (comentario), 664-668 (catch vivo) | stepJob (corrutina hermana) | 🔴 ningún test lo menciona ni inyecta un StepDetectorSource que lance | GUARDA-CÓDIGO vivo, pero sedimento DOCUMENTAL: cero rastro del incidente; anida en un marcador REFACTOR-201 que no existe en otro sitio |
| **BUG-COORD-115** | — (sin fecha/dispositivo; PARKING-DETECTION.md no lo documenta — NO VERIFICADO) | Riesgo de doble-save si un tap del usuario y un auto-confirm aterrizaban en la misma iteración | Precedencia del collect: `state.userConfirmedParking` se evalúa ANTES del árbol candidate/fast-confirm (rama 5, CPD:1050-1108) → "un tap siempre gana"; KDoc del invariante en CPD:81-93 | CPD:81 (KDoc), 1050 | `userConfirmedParking` | 🔴 ningún test lo menciona | ⚠ El KDoc que porta el tag ESTÁ DESFASADO (02 §4): omite que la rama 1 (hold, DET-C-02) va primero y su 1b YA resuelve el Sí (CPD:887-898). El invariante SEMÁNTICO sobrevive por dos rutas; la LISTA es falsa. GUARDA-CÓDIGO con doc podrida (§4) |
| **BUG-DETECT-310502** | 2026-05-31, Redmi Note 11 (PARKING-DETECTION.md:882-888) | La notificación Low/Medium se suprimía indefinidamente esperando STILL/exit; al llegar, el coche ya se movía — confirmación imposible | Timeout de fallback: en `LowReached`, `hasExit ∨ (now - firstReachedAt) ≥ lowNotifTimeoutMs (90 s)` → mostrar la notificación igualmente (CPD:2440-2464, rama 10 vía `advanceLowMedium`) | CPD:2437 (log que porta el tag), 2440-2464; Config:652-659 | `phase` (`LowReached.firstReachedAt` — campo absorbido por REFACTOR-200) | 🔴 ningún test lo menciona | Su campo original (`lowFirstReachedAt`) fue subsumido por REFACTOR-200 — el guard sobrevivió a la migración. La mitad STILL de su condición murió con C1 (quedó `hasExit ∨ timeout`). No dominado: única vía de prompt en paradas que nunca alcanzan High. GUARDA-CÓDIGO |
| **BUG-DETECT-310503** | 2026-05-31, Oppo CPH2371, entrada de hospital (PARKING-DETECTION.md:890-907) | Auto-confirm a los 30 s de una parada breve: exit + GPS excelente + 8 pasos → 0.75 exacto → CANDIDATE → confirm antes de poder descartar | HOY: el fast path TOPA en Medium 0.65 POR ARITMÉTICA (`fastPathBaseScore 0.50 + fastPathSpeedBonus 0.15`, sin bonus de accuracy) → Medium no abre CANDIDATE → nunca auto-confirma; abre prompt (CPC:39-46). El FIX ORIGINAL (gate del accuracy-bonus en STILL) fue ELIMINADO: DET-SOLID-001 C1 borró STILL y `fastPathAccuracyBonus` | CPC:14-16 (KDoc), :41; Config:21, 27 | ninguno (función pura de `ParkingSignals`) | ✅ CalculateParkingConfidenceUseCaseTest:29 ("0.65 → Medium: the fast path's ceiling by design") | SUPERSEDIDO PARCIAL por DET-SOLID-001 C1: la condición que el tag guardaba YA NO EXISTE; queda un techo estructural que el test fija. Frontera GUARDA-CÓDIGO/histórico (§4) |
| **BUG-GARAGE-COLA-001** | Colas de garaje / atascos ≥5 min (PARKING-DETECTION.md:925-942, pre-2026-06) | El slow path confirmaba por TIEMPO con el usuario aún dentro del coche | Los pasos son la señal canónica de "salió del coche": `StepDetectorSource` como corrutina hermana; expiración del CANDIDATE sin prueba de egress → DISCARD (`Rejected` → CPD:1729-1735, phase→Notified + `stepCount=0`); el slow path puro ya no confirma | CPD:305, 608, 1729 (discard — rama 8); Config:713-719; AndroidDetectionModule.kt:35 | `stepCount`, `stoppedSince` | 🔴 ningún test lo menciona (el Rejected-por-ventana lo fijan EPDTest/CPDTest sin nombrarlo — NO VERIFICADO test a test) | Su regla nuclear fue absorbida y endurecida por DET-A (pasos ∧ egress) y DET-C-01; le queda en propiedad el DISCARD :1729 y el linaje del step-detector (§4). GUARDA-CÓDIGO |
| **BUG-SCOOTER-001** | — (2 modos de fallo, PARKING-DETECTION.md:944-990, pre-2026-06; sin fecha/dispositivo — NO VERIFICADO) | (1) Scooter/bici como vehículo → cada llegada publicaba plaza falsa; (2) coche de perfil pero viaje en patinete → park guardado contra el coche | Dos niveles: `ParkingStrategyResolver` → `NONE` si primario ∈ {SCOOTER, BIKE} (nunca arranca detección); mismatch guard: `CAR && sessionDurationMs ≥ 8 min && maxSpeedKmh ≤ 28` → `isMismatch` → nunca auto-confirm, solo prompt (EPD:149-153, 164) | ParkingStrategyResolver.kt:29, 101-104, 127; EPD:150; Config:754; VehicleType.kt:10, Vehicle.kt:35, DetectionReadiness.kt:47; + UI de registro y Room (VehicleEntity:18, Migrations:27) | `vehicleType`, `sessionDurationMs`, `maxSpeedKmh` | 🔴 ningún test lo menciona (ParkingStrategyResolverTest fija las ramas NONE sin nombrarlo — NO VERIFICADO por nombre) | Su heredero moderno es DET-BIKE-NOT-A-CAR-001 (perfil vs viaje ACTUAL): mismo `humanPowered` del veredicto — dos tags alimentan un predicado. GUARDA-CÓDIGO |
| **BUG-SERVICE-109** | — (histórico; sin fecha/dispositivo — NO VERIFICADO; fila fusionada F2+H) | Reutilización del Koin single entre sesiones: la siguiente sesión leía `hasEverReachedDrivingSpeed` rancio | Reset íntegro del estado en entrada de `invoke` (CPD:520) y en el finally bajo `NonCancellable` (CPD:1297-1301, 1327) → entre sesiones `hasDetectedMovement == false` (CPD:444-446). Reflejo androidMain: idempotencia por job activo, sin leer flags cross-sesión (CDS:336-340, "stop relying on stale hasDetectedMovement across sessions") | CPD:97 (KDoc lifecycle), 444-446, 1297-1301; CDS:336 | TODO `ParkingDetectionState` (el reset) + `hasDetectedMovement` | 🔴 ningún test lo menciona | ⚠ ENMENDADO por DET-AUDIT-002 T8 (CPD:1302-1335): el reset del finally ya NO es incondicional — solo la sesión que posee `currentSessionId` lo ejecuta; una superseded no toca el estado del sucesor. El invariante lo garantizan 109+T8 JUNTOS; el KDoc :95-97 describe el contrato pre-T8 (§4). GUARDA-CÓDIGO |
| **REFACTOR-200** | Refactor 2026-06-08 (ConfirmationPhase.kt:4-14; NO documentado en PARKING-DETECTION.md — allí solo existe BT-REFACTOR-200, OTRO refactor, del servicio BT) | 4 flags implícitos (`lowFirstReachedAt`, `confirmationNotificationShownAt`, `highConfidenceReachedAt`, `highCandidateHadVehicleExit`) permitían combinaciones inválidas | Sealed interface `ConfirmationPhase` (Idle/LowReached/Notified/Candidate) hace las combinaciones inválidas IRREPRESENTABLES; transiciones explícitas; reset a Idle al conducir | ConfirmationPhase.kt:4; CPD:134-137, 162, 1728, 2323-2326, 2418 | `phase` | 🔴 ningún test menciona REFACTOR-200 (ConfirmationPhaseMappingTest fija el MAPEO de DET-PHASE-001, no las transiciones) | SOLO-COMENTARIO-HISTÓRICO: nombra la arquitectura (viva y load-bearing), no una condición. Máximo candidato a sedimento nominal. ⚠ Riesgo de confusión con BT-REFACTOR-200 (BluetoothDetectionService.kt:55/74/201), refactor DISTINTO |
| **REFACTOR-300** | 2026-06-08/09 (PARKING-DETECTION.md:1018, 1050): field 09-06 — la card "Vehículo aparcado · Cancelar" desaparecía en ~1-2 s | Doble notificación (prompt → dismiss → "saved") y ventana de revert efectivamente cero | Notificación unificada post-save: el prompt SE MORFEA en la card ACK/REVERT al mismo ID (CPD:1592-1605); receiver + acciones en el service; **REFACTOR-300-FIX**: `savedConfirmPostedAt` @Volatile — el session-start solo descarta la card si `age > confirmationResponseTimeoutMs` (CPD:565-585), protegiéndola de un ENTER espurio mientras caminas | CPD:385-387, 565-585, 1429, 1592-1608; CDS:85, 271-272, 362-371, 1505; AppNotificationManagerImpl.kt:88, 453; AppNotificationManager.kt:30/41; DomainModule.kt:169; AndroidManifest.xml:144; ParkingConfirmationReceiver.kt:15; ConfirmParkingUseCase.kt:76; IosAppNotificationManagerImpl.kt:75 | `savedConfirmPostedAt` (@Volatile, cruza sesiones, perdido en process death — documentado) | ✅ parcial: CPDTest:570 ("[REFACTOR-300 follow-up]"), :2661 (REFACTOR-300-FIX), FakeAppNotificationManager:18 — ⚠ 05 lo lista 🔴 y ES INCORRECTO (corrección a 05) | Flujo VIVO completo (feature, no sedimento). Colinda con DET-C-02 (el hold retrasa el morph). GUARDA-CÓDIGO |
| **DET-LOG-03** | — (infra de diagnóstico, fase T8 de DET-AUDIT-002) | Sin trace remoto por sesión no había forense de los FN/FP de campo | Ciclo de vida del trace: `currentSessionId` reclamado en la entrada (CPD:518, antes del reset — T8), `sessionOutcome` refinado por cada salida, `SessionEnded` emitido en el finally ANTES de limpiar (CPD:1324-1326); decisiones terminales CONFIRMED/CONFIRM_FAILED (CPD:1609-1613, 1646-1650) | CPD:389-396, 549-551, 1324-1326, 1609, 1646; DetectionEvent.kt:24 | `currentSessionId`, `sessionOutcome` (@Volatile — 02 §1c: acoplamiento por prefijo `startsWith("confirmed_")` en :1508) | ✅ CPDTest:216 ("coordinator emits a diagnostics session trace") + FakeDetectionEventLogger.kt:8 — ⚠ 05 lo lista 🔴 y ES INCORRECTO (corrección a 05) | Es la CONDICIÓN DE POSIBILIDAD de DET-SOLID-001 (sin trace no hay fixture) y del vocabulario `sessionOutcome`. GUARDA-CÓDIGO |
| **DET-LOG-04** | — (infra: el stream de entrada del replay) | Sin fixes crudos + transiciones AR edge-logged no se puede reproducir una sesión 1:1 | Cada fix emite `LocationFix`; las señales AR se edge-detectan (`loggedVehicleExit`) para loguear cada transición UNA vez (CPD:545-547, 834-843) | CPD:545-547, 834-843; DetectionEvent.kt:84 ("the replay input stream for the Fase D pure-decision tests") | local `loggedVehicleExit`; `vehicleExitConfirmed` | ✅ CPDTest:235, 251 | Alimenta a DET-D-02 (replayabilidad) y al harness de DET-SOLID-001. GUARDA-CÓDIGO |

### 1.3 Cluster G — triggers/armado, salida+safety net, sesión/nudges/provenance, arbitraje BT, readiness/watch/sentry (38)

#### 1.3-A · Triggers y armado

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-AR-FIRST-001** | Field 2026-07-09/10, Oppo+Redmi (backlog): 6/6 EXITs entregados a 951–2 192 m; AR disparó todo el día con lag pequeño | El EXIT tardío hacía perder el arranque del viaje; el AR-arm legado producía FPs de bus | Escalera pura: `NoSession` / `StaleEnter` (`lag ∉ [0, exitEnterPairWindowMs]` o ENTER predata la sesión) / `NoFix` / `ArmAtCar` (`d ≤ radio+acc`, embarque EN la propia valla) / `ArmMidTrip` (EXIT lejano registrado) / `TickOnly` (bus/taxi → evaluador, nunca arm) | EvaluateArEnterArmUseCase.kt:69-98; CDS:104, 268, 655-804, 1496 (carril `getForegroundService`); ActivityRecognitionManagerImpl.kt:52, 143-156, 183 (registro ENTER-only); ArmEvidence.kt:30, 56; DetectionTrigger.kt:22; F3/F4 en CPD:625, 660, 1763-1766, 1817, 2226-2311; EvalSNC:379-391 (F4); EvalUS:157 (F3) | `ArmEvidence`, `armEvidence` persistido, `stepCount` de egress, `exitEnterPairWindowMs` | ✅ EvaluateArEnterArmUseCaseTest (8) + CPDTest | SOLAPE con DET-CONJUNCTION-001 (`ArmMidTrip` consume el mismo registro de EXIT lejano); con DET-SESSION-BIRTH-001 (línea 83, filtro propio) |
| **DET-AR-REARM-001** | 2026-06-28 (PARKING-DETECTION.md:1251): tras un park el loop serial geofence-gated quedaba PARADO y perdía los parks siguientes | Viajes cortos y EXITs perdidos dejaban la detección muda | (a) Watchdog "¿sigues aparcado?" → tap "me he ido" = `ACTION_DEPARTURE_CONFIRMED`; (b) guard anti-loop: coordinator ya corriendo en la MISMA zona → no re-armar; (c) lecturas de posición pasivas (lastLocation, sin provocar la valla) | CDS:86, 273, 390, 537-554, 694-704, 1354, 1510; AppNotificationManagerImpl.kt:457; AppNotificationManager.kt:186; Config:97; GetLastKnownLocationUseCase.kt:13 + DomainModule.kt:92 + LocationDataSource.kt:37 + AndroidLocationDataSourceImpl.kt:115; SessionSupersede.kt:12 | trigger armado, anchor de la sesión corriendo, notificación watchdog | ✅ SessionSupersedeTest (indirecto) — el watchdog en sí NO VERIFICADO test propio | SOLAPE estructural con DET-SUPERSEDE-001: supersede es la válvula de escape del guard (b) — mismas líneas de servicio (539-561, 692-711) |
| **DET-EXIT-TRUST-001** | Derivado de los EXITs entregados a >900 m (07-2026); doctrina "evento re-entregado pierde autoridad directa" | Un EXIT entregado lejos/tarde ganaba autoridad de salida instantánea | La distancia de ENTREGA nunca decide SI se mira, solo cuánto vale una entrega: boundary → evento Exited + worker speed-gated; far → SOLO registro para el reconcile + el mismo worker. Partición: `deliveredAtMeters ≤ watchdogFarThresholdMeters` | EvaluateGeofenceExitUseCase.kt:67, 88-98; CDS:411, 501-509; ParkingSafetyNetWorker:220, 315, 714, 790; RunDepartureCheckUseCase:67; DetectParkingDepartureUseCase:102; EvalSNC:80, 121, 346; Config:1078 (`isCredibleDrivingSpeed` canónica) | `staleDepartures`, `exitDeliveredAtMs` (persistido por fence), `watchdogFarThresholdMeters` | ✅ 3 ficheros | SOLAPE: su registro `exitDeliveredAtMs` es la mitad posicional de DET-CONJUNCTION-001; tríada §3-a |
| **DET-CONJUNCTION-001** | Field 2026-07-08 cine, Redmi (fixes speed=0, contador congelado, ancla caducada; EXIT 21:42 + ENTER 21:43 + d=1 470 m) y 2026-07-10 Oppo (4 h a 1,4-1,9 km, 9 ticks silenciosos) | En devices con todos los sentidos degradados la salida real quedaba muda | Dos eventos OS independientes post-sesión de acuerdo: `|exitAtMs − boardingAtMs| ≤ exitEnterPairWindowMs` ∧ `isBeyondPedestrianReach(d, now−boarding)` ∧ contador mudo (`trustedSteps == null`) → DispatchDeparture preconfirmado. La valla rota ES la mitad posicional | EvalSNC:126, 265-298, 387; EvaluateArEnterArmUseCase.kt:67 (misma conjunción para ArmMidTrip); ParkingSafetyNetWorker:224, 650, 706, 753, 793; ActivityTransitionReceiver.kt:71; Config:261 | `exitDeliveredAtMs`, `lastVehicleEnteredAtMs`, `exitEnterPairWindowMs` | ✅ EvaluateSafetyNetCheckUseCaseTest | SOLAPE con DET-EXIT-TRUST-001 (productor del registro) y DET-RIDE-PROOF-001 (el par sigue siendo 2 nominaciones: exige además `isBeyondPedestrianReach`) |
| **DET-SESSION-BIRTH-001** | Field 2026-07-08 18:52, Redmi: MIUI re-entregó el ENTER del viaje de IDA (trueTime 17 min viejo) segundos tras el park; 23 s después "verificó" un EXIT andando y borró un parking correcto | Un parking correcto borrado por evidencia del viaje que lo CREÓ | Toda evidencia con `timestamp < session.location.timestamp` se trata como AUSENTE. UN filtro aplicado a cada fuente | **4 implementaciones**: DetectParkingDepartureUseCase:122-135; VerifyDepartureEvidenceUseCase:51, 72-73; EvalSNC:152-160 (boarding Y exit); EvaluateArEnterArmUseCase:81-83 (StaleEnter); consumo RunDepartureCheckUseCase:112; CDS:605 | `sessionStartMs` (= timestamp del pin), `lastVehicleEnteredAt` del bus | ✅ 5 ficheros | ⚠️ SOLAPE máximo del cluster: el predicado `evidencia ≥ sessionStart` copiado en 4 evaluadores — §3-d |
| **DET-RIDE-PROOF-001** | Field 2026-07-09 11:53/11:55, Redmi: ENTER fantasma andando (14 s antes de un EXIT a pie a 127 m) liberó la plaza real y sembró un park fantasma en la peluquería | Un evento de OS confirmaba una salida sin movimiento medido | *El evento NOMINA, solo el movimiento medido confirma*: un ENTER solo es admisible si la física lo corrobora — `isBeyondPedestrianReach(d, elapsed, radio, acc)`; velocidad solo con accuracy creíble; contador con delta≈0 y desplazamiento andable = sospechoso de congelado y pierde la voz | Chequeo físico único en Config:1092 (`isBeyondPedestrianReach`); aplicado en DetectParkingDepartureUseCase:178-198; VerifyDepartureEvidenceUseCase:29, 54, 76-98; EvalSNC:38, 204-216, 276, 312; EvaluateGeofenceExitUseCase:44; EvaluateArEnterArmUseCase:56; CDS:501-509, 579, 609; ParkingSafetyNetWorker:369, 808; Config:172 | speed+accuracy del fix, `stepsSinceAnchor`, `frozenCounterSuspectSteps`, `maxPedestrianSpeedMps` | ✅ 4 ficheros | Es LA física compartida de la tríada (§3-a); el freno que DET-CONJUNCTION-001 y DET-DEPART-PROOF-001 refinan |
| **DET-DEPART-PROOF-001** | Field 2026-07-27 18:30, Oppo, en casa quieto: un espejismo indoor (14 km/h, acc 21 m, 121 m) rompió la valla y 140 ms después el worker recibió el MISMO fix cacheado → plaza fantasma publicada en el salón | Un solo fix disparaba el EXIT Y "confirmaba" la salida | Puerta de independencia: la muestra confirmatoria debe cumplir `fix.timestamp − exitTimestamp ≥ departureProofMinGapMs` (velocidad creíble pero no independiente → `Inconclusive("exit_echo")`, nunca Confirmed) | DetectParkingDepartureUseCase:43, 89-91, 136-160 (REASON_EXIT_ECHO); RunDepartureCheckUseCase:76-83; Config:273 (retry ~45 s) | `departureProofMinGapMs`, `exitTimestampMs`, ts del fix | ✅ DetectParkingDepartureUseCaseTest + RunDepartureCheckUseCaseTest | Refina la pata "velocidad" de la tríada; solo existe en el worker (el safety net muestrea su propio fix en otro instante — NO duplicado allí) |

#### 1.3-B · Salida diferida y safety net

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-SAFETY-NET-001** | 2026-07-04 Calle Gavia (salida silenciosa tras EXIT-andando descartado: estado INSIDE/OUTSIDE del engine envenenado) + toda la clase "el OS no entregó el EXIT" | Un EXIT perdido paraba la cadena entera | Capa que NO depende de Play Services: wake-ups propios (worker 15 min + significant motion) muestrean UN fix y el evaluador puro mapea → `CureGeofence` / `DispatchDeparture` (lejos + prueba de viaje + ancla) / `PromptStillParked` / `None`. *Nunca libera por distancia sola* | EvalSNC:9-104, 138-405; ParkingSafetyNetWorker:57, 428, 687; DomainModule.kt:109; Config:112; PaparcarApp.kt:94; BootCompletedReceiver.kt:49; AppNotificationManager.kt:95; epílogo CDS:1443 | `lastSeenNearCarAtMs` (ancla), `stepsSinceAnchor`, `watchdogFarThresholdMeters`, `userPresent` | ✅ EvaluateSafetyNetCheckUseCaseTest (49) | Techo de casi todo el cluster; la orquestación androidMain que lo invoca está 🔴 (05 cluster 2) |
| **DET-CURE-FRESH-001** | Field 2026-07-12, Oppo, park manual en la Glorieta: la cura re-registró una valla recién creada y abrió la ventana ciega justo antes de arrancar | Re-registrar una valla sana resetea INSIDE/OUTSIDE → salida sin EXIT | `shouldReregisterCure`: `sessionAgeMs < cureSkipFreshSessionMs → false`; si no, primera cura por proceso siempre, después `now − lastCure ≥ cureReregisterMinIntervalMs` | EvalSNC:408-434 (tag en :420); Config:580; ParkingSafetyNetWorker:281 | `cureSkipFreshSessionMs`, `alreadyCuredThisProcess`, `lastCureAtMs` | ✅ EvaluateSafetyNetCheckUseCaseTest | Complementa DET-SAFETY-NET-001 (frena su propia rama CureGeofence) |
| **DET-BACKFILL-TAINT-001** | Field 2026-07-30 20:42, Redmi/Jerez: el coordinator abortó `aborted_unattended_gap_anchor` (nudge-only, hueco GPS inbounded); 1 min después el net de 15 min plantó el pin igualmente con MENOS información | Dos deciders sobre una misma llegada: el segundo, más ciego, re-decidía lo vetado | El placer del backfill DIFIERE si ∃ resolución nudge-only con `0 ≤ now − resolutionAt ≤ arrivalResolutionWindowMs` ∧ `d(backfillFix, resolutionPoint) ≤ arrivalResolutionMatchRadiusMeters`. Solo difiere la COLOCACIÓN; la liberación de la plaza vieja sigue | EvaluateBackfillDeferralUseCase.kt:9, 34-63; estampado CDS:887-909, 1295, 1489; ParkingBackfillWorker:74-90, 145, 166; ParkingSafetyNetWorker:757; Config:152, 159 | stamp `{resolutionAtMs, resolutionPoint}`, ventanas | ✅ EvaluateBackfillDeferralUseCaseTest (7) | Cierra la cara que DET-ARRIVAL-DOUBLE-PIN-001 dejó abierta — su KDoc lo documenta (:18-20) |

#### 1.3-C · Sesión, nudges y provenance

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-SUPERSEDE-001** | Field 2026-07-12, WA YUKI: una valla espuria a ~100 m del park real siguiente bloqueó su detección ("already running") | Sesión zombie retenía el arm del park verdadero | `shouldSupersedeRunningSession`: supersede sii `d(newPark, runningAnchor) > newFenceRadius + newFix.accuracy`; conservador: `runningAnchor == null → false` | SessionSupersede.kt:7-32; CDS:539-561 (lane EXIT), 692-711 (lane AR); DetectionEvent.kt:207-217 (`SessionSuperseded`); DetectionEventDto.kt:87 | anchor de la sesión corriendo, radio+accuracy del nuevo park | ✅ SessionSupersedeTest (3) | Válvula de escape del guard anti-loop de DET-AR-REARM-001 (misma frontera espacial que `ArmAtCar` de DET-AR-FIRST-001, SessionSupersede.kt:16) |
| **DET-MANUAL-CANCEL-001** | Sin incidente de campo citado (preventivo) — NO VERIFICADO | Un auto-confirm tardío podía sobreescribir el pin manual, o dejar valla huérfana | Al guardar un park manual (CREATE) → `manualParkingDetection.stop()` cancela cualquier sesión coordinator. Un MOVE no lo hace. Idempotente | SaveManualParkingUseCase:22, 91-94; ManualParkingDetection.kt:16-22; ManualParkingDetectionImpl.kt:23 | sesión coordinator viva, `editingParkingId` | ✅ SaveManualParkingUseCaseTest + HomeViewModelTest | Con DET-NUDGE-PIN-PROVENANCE-001 (mismo flujo de guardado); sin conflicto: orden save→stop |
| **DET-NEVER-SILENT-001** | Leg chino→casa, field 2026-07-12: proceso muerto a mitad de viaje = park perdido en silencio | Muerte de proceso mid-trip → ni pin ni pregunta | Al armar se persiste un "pending detection" durable con heartbeat; todo terminal lo limpia. Heartbeat viejo ⇒ proceso MATADO ⇒ el watchdog pregunta. Decisión pura: `shouldNudgeForStalePending = trigger ∈ {GEOFENCE_EXIT, MANUAL} ∨ sawDriving` | PendingNudgeDecision.kt:4-20; PendingDetectionStore.kt:7; CDS:1194-1202, 1304; ParkingSafetyNetWorker:132, 434; Config:588, 592 | `PendingDetection{trigger, sawDriving, heartbeat}`, `pendingDetectionStaleMs` | ✅ PendingNudgeDecisionTest (3) | SOLAPE de señal con DET-FGS-REAPER-001 y DET-NUDGE-PERSIST-001: el MISMO stale-pending alimenta reaper y nudge (§3-otros) |
| **DET-NUDGE-PERSIST-001** | Field 2026-07-25, Redmi, sesión 1784939810210: nudge notificado a las 03:11, el usuario dormía; por la mañana Home decía "no hay coche aparcado" sin vía de recuperación | La pregunta vivía solo como notificación descartable | La pregunta se persiste como ESTADO DE APP (`PendingParkNudge{createdAt, source, vehicleId}`) en el mismo choke point que postea el nudge; banner visible mientras `shouldShowParkNudgeBanner`; un nudge nuevo REEMPLAZA al anterior | PendingParkNudge.kt:6-48; AppNotificationManagerImpl.kt:251 (persistir PRIMERO); AppNotificationManager.kt:128; AppPreferences.kt:36 + AndroidDataStoreAppPreferences.kt:110; ClearParkNudgeUseCase.kt:7 + DomainModule.kt:107; ConfirmParkingUseCase:349; RunHonestCloseUseCase:120; UI HomeViewModel:89, 153, 621, HomeSheetContent:100, 134, HomeState:142, HomeSlices:146, strings ×9 | `PendingParkNudge` en DataStore, sesiones activas | ✅ ParkNudgeUseCasesTest (7) | Consumidor natural de DET-NEVER-SILENT-001; con DET-NUDGE-PIN-PROVENANCE-001 (responder el banner conserva provenance) |
| **DET-NUDGE-PIN-PROVENANCE-001** | Backlog; sin fecha de campo citada en código — NO VERIFICADO | El pin puesto respondiendo un nudge quedaba estampado como reporte manual (TTL 15 min en vez de 2 h) | `fromDetectionNudge=true` → `SpotType.AUTO_DETECTED` + `detectionPath="nudge"`; a mano → `MANUAL_REPORT` + `"manual"`; "Sí" al prompt → `AUTO_DETECTED` + `"user"` | SaveManualParkingUseCase:38-63, 101-104; origen del flag: StartAddParkingEventBus.kt:29, MainActivity.kt:198-199, AppNotificationManagerImpl.kt:196, 267, 375, HomeIntent.kt:106, HomeState.kt:219, HomeViewModel.kt:449, 839, HomeSheetContent.kt:136 | `fromDetectionNudge`, `spotType`, `detectionPath` | ✅ SaveManualParkingUseCaseTest | Sub-caso de DET-PIN-PROVENANCE-001 (mismo vocabulario de paths) |
| **DET-PIN-PROVENANCE-001** | Doctrina de diagnóstico (feedback_detection_trigger_provenance) | Sin provenance, un FP de campo no era atribuible a su trigger | Todo pin persiste `detectionPath` ("steps+egress", "kinematic+egress", "vehicle-exit", "unattended_timeout", "user", "manual", "nudge", "bt", "safety_net_backfill") + `armEvidence`, sincronizados a Firestore; null en filas legacy | CPD:1581-1583; ConfirmParkingUseCase:95; SaveManualParkingUseCase:83-86; RunHonestCloseUseCase:106; BluetoothParkingDetector:207; ParkingBackfillWorker:113, 164; SaveNewParkingSessionWorker:117, 143; EnrichParkingSessionWorker:198; UserParkingEntity.kt:43, 46, UserParking.kt:54, ParkingHistoryDto.kt:28, 31, ParkingSessionMapper.kt:100, 151, 188, AppDatabase.kt:29, Migrations.kt:146, RemoteUserProfileDataSourceImpl.kt:239 | `detectionPath`, `armEvidence`, MIGRATION_12_13 | ✅ ParkingSessionMapperTest (fuera del subsistema) | Paraguas de DET-NUDGE-PIN-PROVENANCE-001; alimenta el criterio de DET-VERDICT-NOT-PREDICATE-001 |
| **DET-SESSION-RELIABILITY-STAMP-001** | Doctrina "la fiabilidad se estampa en cada sesión"; sin incidente puntual — NO VERIFICADO | Una traza que muere en silencio no decía si las exenciones estaban puestas | Al armar, la cabecera de sesión estampa `isBatteryUnrestricted` (en vivo), `requiresAutostartWhitelist`, `requiresOemBatteryFreezeExemption` — el post-mortem distingue OEM-kill de config | DeviceInfoProvider.kt:26-44; AndroidDeviceInfoProvider.kt:12; FirestoreDetectionEventLogger.kt:154; DetectionEventDto.kt:28 | los 3 flags de supervivencia | 🔴 sin test | Insumo del diagnóstico de DET-RELIABILITY-001 (misma física, distinto momento) |
| **DET-BREADCRUMBS-001** | Fields 2026-07-07 22:41 (el net despierta cada pocos segundos en viajes no trackeados) y 2026-07-08 04:41 (sesión guardada en casa con el coche a 200 m) | Los fixes one-shot del net se tiraban tras decidir; el backfill aterrizaba donde estaba el TELÉFONO al despertar | Todo fix one-shot fresco se persiste en un ring buffer en disco (`TripTrail.append`); el reconcile lo lee para (a) colocar el backfill donde PARÓ EL COCHE y (b) probar un ride con contador mudo. NO es tracker continuo | TripTrail.kt:7-28; TripTrailImpl.kt:11-13; DomainModule.kt:90 (null en iOS); GetOneLocationUseCase.kt:35; AndroidDetectionModule.kt:58 | ring de `GpsPoint` acotado | ✅ GetOneLocationUseCaseTest | Insumo del backfill del safety net; sin conflicto |

#### 1.3-D · Arbitraje BT de dominio

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-TIERS-001** | Field 2026-08-01 y 2026-08-10 (lanes armando bajo BLUETOOTH, doble pin) | BT y Coordinator a la vez producían pins duplicados/mal atribuidos | BT NUNCA entra en el scoring (regla dura): un edge ACL del coche emparejado SUPERSEDE la sesión coordinator vía veredicto puro. Eje de producto: tier AUTOMATIC sii BT emparejado; ASSISTED_PLUS con exención; ASSISTED resto (el OEM cambia el level, no el tier) | EvaluateBtArbitrationUseCase.kt:5, 10, 42 + DomainModule.kt:113; BluetoothConnectionReceiver.kt:57, 104; CDS:278, 1477; EvaluateDetectionReliabilityUseCase.kt:35, 59-66 + DetectionReliability.kt:38; UI DetectionTierStatusCard.kt:44, 108, PermissionsContent.kt:209, 289, PermissionRow.kt:74, PermissionsState.kt:37, strings ×9 | verdictos `NoOp/Supersede/Veto/Yield`, `DetectionTier` | ✅ 4 ficheros | Mismo fichero-árbitro que DET-BT-WRONG-CAR-ABORT-001; frontera con DET-STRATEGY-GATE-001 (gate en el arm) |
| **DET-STRATEGY-GATE-001** | Field 2026-08-01: bajo BLUETOOTH los lanes sentry/AR armaron igualmente y pinnearon los viajes del Kamiq sobre el Focus primario | Misatribución: el coordinator armaba cuando la estrategia BT poseía el viaje | `coordinatorMayArm(strategy, trigger) = trigger==MANUAL ∨ strategy==COORDINATOR` — UN choke point (`startParkingDetection`); el lane EXIT corta antes con la misma regla. La residencia sentry también es strategy-aware | ParkingStrategyResolver.kt:48-64; CDS:984, 1160-1170; SentryLifecycleDecision.kt:20; ObserveDepartureWatchGapUseCase.kt:49 (BT conectado NO es gap); DetectionStory.kt:48 | `ParkingStrategy`, `DetectionTrigger` | ✅ 3 ficheros (ParkingStrategyResolverTest 16…) | SOLAPE doctrinal con ARCH-MONITORING-002 (misma física, dos momentos: resolver vs arm) |
| **ARCH-MONITORING-002** | Misma familia field 2026-08-01: correr Coordinator en paralelo al coche conectado atribuía ese viaje al primario no-BT | Viaje del coche BT atribuido al primario | Cuando BLUETOOTH gana (conectado), Coordinator se suprime AUNQUE el primario no tenga BT; al DESCONECTAR se vuelve a COORDINATOR — seguro porque el Coordinator exige conducción medida y un edge BT mid-session lo arbitra `EvaluateBtArbitrationUseCase` | ParkingStrategyResolver.kt:31-36, 57 (rationale + supresión); VehicleRegistrationViewModel.kt:274 | estrategia resuelta, ACL conectado | ✅ ParkingStrategyResolverTest | DOMINADO en runtime por DET-BT-CONNECTED-NOT-PAIRED-001 + DET-STRATEGY-GATE-001 (§4) — NO redundante como doc: carga el porqué |
| **DET-BT-CONNECTED-NOT-PAIRED-001** | Field 2026-08-08 Málaga (PARKING-DETECTION.md:26): Kamiq emparejado + Focus activo sin BT → resolvía BLUETOOTH, el pipeline BT esperaba un disconnect que nunca llegó → **el Focus nunca se detectó** | Mero pairing secuestraba la estrategia | BT posee la detección SOLO mientras el teléfono está CONECTADO a un coche emparejado (`btPairedVehicleIds ≠ ∅ ∧ isBluetoothEnabled ∧ isConnectedToPairedCar`); ground truth ACL en `BtConnectionStore`, limpiado al desconectar | ParkingStrategyResolver.kt:17, 83-95; BtConnectionStore.kt:32 + BluetoothConnectionReceiver.kt:134, 151; AndroidBluetoothScanner.kt:43; BluetoothScanner.kt:28; EvaluateBtArbitrationUseCase.kt:34, 68; ParkedWatchBadge.kt:55; FakeOtherDataSources.kt:183 | `BtConnectionStore` (MAC conectada), adapter on/off | ✅ ParkingStrategyResolverTest | Precondición de DET-BT-WRONG-CAR-ABORT-001 (su KDoc :68 la cita) |
| **DET-BT-WRONG-CAR-ABORT-001** | Field 2026-08-10: doble pin — Kamiq por bt y Focus por steps+egress con 75 s de diferencia | El coordinator confirmó un re-park fantasma del coche equivocado | Identidad refutada: `btVehicleId ≠ null ∧ coordinatorVehicleId ≠ null ∧ distintos` ⇒ DISCONNECT→Supersede; CONNECT en Driving→YieldToConnectedCar; CONNECT en Candidate→NoOp (el pin pendiente se ganó con evidencia medida pre-connect); null en cualquier lado → confiar en el edge del MAC | EvaluateBtArbitrationUseCase.kt:36, 51, 84-94, 107-108 | `btVehicleId`, `coordinatorVehicleId`, `DetectionPhase` | ✅ EvaluateBtArbitrationUseCaseTest (12) | Su rama DISCONNECT-coche-distinto produce el MISMO veredicto que el fallthrough genérico — dominada en comportamiento, carga rationale (§4) |
| **DET-BT-OWNERSHIP-001** | Field 2026-08-11: la valla del Kamiq aparcado nominó 8 viajes del Focus; cada confirm re-vallaba el Kamiq y re-armaba la cadena | Pin tras pin sobre el coche BT que no se movió | Un nominador BT-paired es VETADO en la ATRIBUCIÓN (su identidad solo la establece el MAC; una valla solo prueba que el TELÉFONO salió): `resolveSessionVehicleId = nominating.takeUnless{esBtPaired} ?: activeId`. El armado NO se toca — cualquier valla despierta | VehicleFenceOwnershipPolicy.kt:49-65; CPD:1003-1034 (lock del vehicleId al primer fix a velocidad de conducción); HomeViewModel.kt:482 + VehicleFenceOwnershipPolicy.kt:83-84 (PARK-DELETE-NO-DECLARE) | `nominatingVehicleId`, `bluetoothDeviceId`, vehículo activo | ✅ CPDTest + VehicleFenceOwnershipPolicyTest | Misma asimetría "identidad BT = solo MAC" que DET-BT-IDENTITY-GATE-001 en otra capa (§3-otros) |
| **DET-BT-IDENTITY-GATE-001** | Field 2026-07-18, Redmi: recogido en coche ajeno JUNTO a su coche aparcado; el step budget liberó la plaza real y pinneó un fantasma en el drop-off | Auto-release reconstruido de un viaje en OTRO vehículo | Veto de identidad: si `vehicleBtGated` ∧ (`lastBtConnectedAtMs == null ∨ < sessionStart`) ⇒ todo `DispatchDeparture` reconstruido degrada a `PromptStillParked`. Solo downgrade — nunca inventa un release | EvalSNC:133-136, 162-177 (`releaseOrAsk`); ParkingSafetyNetWorker:102, 188-229; BluetoothConnectionReceiver.kt:146; BtConnectionStore.kt:8 | `vehicleBtGated`, `lastBtConnectedAtMs`, `sessionStartMs` | ✅ EvaluateSafetyNetCheckUseCaseTest | SOLAPE doctrinal con DET-BT-OWNERSHIP-001 (misma asimetría, capas distintas) |

#### 1.3-E · Readiness, watch, sentry, toggles

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-READY-001b** | Reemplaza el boolean fino `HomeState.allPermissionsGranted` (histórico, sin incidente) — NO VERIFICADO | Estados de detección incoherentes entre superficies | UN stream `DetectionReadiness` con precedencia fija: **Disabled → Blocked → Monitoring → Parked → Ready**, combinando fleet + sesiones + permisos + runtime + toggle | ObserveDetectionReadinessUseCase.kt:26, 46-129; DetectionReadiness.kt:12; ParkingStrategyResolver.kt:80 | los 5 flujos combinados | 🔴 (el fichero tiene 16 tests pero ninguno menciona 001b) | Paraguas de DET-READY-001i, DET-READY-TRIP-OVER-PARKED-001 y DET-TOGGLE-001 — las 4 reglas viven en el MISMO `resolve()` |
| **DET-READY-001c** | — (infraestructura) — NO VERIFICADO | El dominio no distinguía "armado e idle" de "siguiendo un viaje" sin depender de Android | `DetectionRuntimeState` (singleton DI): `isRunning/trip/phase/presence` que el servicio muta y el dominio observa; `setRunning(true)` al armar, `false` en el finally | DetectionRuntimeState.kt:77, 106, 154; DomainModule.kt:226; CDS:89, 1182, 1316, 1434 | `isRunning`, `TripContext`, `DetectionPhase`, `ServicePresence` | 🔴 directo (indirecto vía ObserveDetectionReadinessUseCaseTest) | Su flag `isRunning` es un lock de DET-FGS-REAPER-001; sin conflicto |
| **DET-READY-001i** | Rediseño del gate de permisos (backlog det-ready-2026-06-26) | GPS-off forzaba navegar a la pantalla de permisos | GPS toggle off se une al set de permisos CORE que faltan → `Blocked` muestra la fila roja en Home; la pantalla de permisos abre solo la sección relevante (`PermissionsFocus`) | ObserveDetectionReadinessUseCase.kt:86-92; App.kt:129, 360, 563; PermissionsFocus.kt:4; PermissionsEffect/Intent/Content/Screen.android/ViewModel; RequiredPermission.kt:25; AppPermissionState.kt:29, 37; strings ×9 | `isLocationServicesEnabled`, `missingPermissions()`, `PermissionsFocus` | ✅ ObserveDetectionReadinessUseCaseTest + AppPermissionStateTest | Rama Blocked del mismo `resolve()` que 001b |
| **DET-READY-TRIP-OVER-PARKED-001** | Field 2 coches (c55ab8eb): con un 2º coche aparcado la readiness quedaba Parked y mataba toda la UI de viaje en vivo | Trip en vivo invisible con multi-parking | Monitoring gana a Parked SOLO para un viaje cuyo PROPIO coche ya salió: `followingTrip = isRunning ∧ ¬(trackedVehicleId==null ∨ ∃sesión de trackedVehicleId)`. Un arm en el coche deja su sesión activa → sigue Parked; arms manuales sin trip context no atribuyen | ObserveDetectionReadinessUseCase.kt:36, 95-117; DetectionReadiness.kt:17; PreferredParkingSession.kt:19; HomeScreen.kt:730, 742; FakeUserParkingRepository.kt:198 | `trip.departingVehicleId`, sesiones activas por vehículo | ✅ ObserveDetectionReadinessUseCaseTest | Con UI-PREFERRED-SESSION-RECENCY-001; rama Monitoring del mismo `resolve()` |
| **DET-WATCH-REACTIVATE-001** | Field 2026-08-14: en clean install el self-heal leía `first()` de un Room aún vacío → "nada aparcado" → watcher muerto hasta el siguiente arranque | Watcher de salida muerto sin que nadie lo note ni pueda revivirlo | Gap = `presence==Dead ∧ resolvePostDetectionLifecycle(...)==EnterSentry`, como STREAM; resurrección solo en momento foreground (Activity visible o CTA "Reactivar", FGS-start legal en A12+); `force=true` en tap explícito salta el cooldown | ObserveDepartureWatchGapUseCase.kt:18, 29-52; DepartureWatchResumer.kt:5-26; DepartureWatchResumerImpl.kt:17; MainActivity.kt:54, 138; AndroidDetectionModule.kt:70; PaparcarApp.kt:104; CDS:1470-1472; UI HomeViewModel:96, 630, HomeScreen:195, HomeIntent:69, HomeSheetContent:128, HomeDetectionSurface:114, PaparcarError.kt:33; FakeDetectionSources.kt:56 | `ServicePresence`, `resolvePostDetectionLifecycle`, cooldown del resumer | ✅ ObserveDepartureWatchGapUseCaseTest (15) | Misma regla `resolvePostDetectionLifecycle` que el epílogo del servicio (una regla, dos lectores) |
| **DET-WATCH-RESUME-RACE-001** | Backlog; raza descrita en el KDoc | El guard one-shot del resumer leía los repos por su cuenta y respondía "nada aparcado" en frío | `current() = invoke().first()` — el snapshot SUSPENDE hasta que cada input del combine produce valor real | ObserveDepartureWatchGapUseCase.kt:54-63; DepartureWatchResumerImpl.kt:24 | los mismos 4 flujos del gap | ✅ ObserveDepartureWatchGapUseCaseTest | Sub-regla de DET-WATCH-REACTIVATE-001 |
| **DET-WATCH-HONEST-001** | Consecuencia de OEM-kills (Oppo/MIUI); sin fecha única — NO VERIFICADO viaje concreto | El badge "vigilando" mentía en verde con el watcher muerto | La UI refleja el ciclo de vida REAL del FGS (`presence`): fragile / interrupted → CTA honesto (exención o Reactivar), nunca "green lie"; self-heal al lanzar la app (`ACTION_APP_LAUNCH_SELF_HEAL` → epílogo decide sentry vs stop) | CDS:258-265, 1470; ParkedWatchBadge.kt:6, DetectionStory.kt:53, 76, 114, HomeState.kt:160, 254, HomeSlices.kt:143, HomeDetectionSurface.kt:110, 211, 275, 347, HomeSheetContent.kt:126, HomeViewModel.kt:886, BatteryOptimizationRequest.kt:8 (+.android:13), HomeScreen.kt:222, MockScenario.kt:49 | `ServicePresence`, exención de batería | ✅ DetectionStoryTest (solo presentación; el self-heal del servicio 🔴) | Con DET-WATCH-REACTIVATE-001 (interrupted→Reactivar) y DET-RELIABILITY-001 (fragile→exención) |
| **DET-SENTRY-COOLDOWN-001** | Field 2026-08-13, Calle Góndola: sesión armada-y-refutada cada ~18 s durante >1 h ≈130 `aborted_false_enter` en un paseo. ⚠️ Field 17/18-08 Chema→Balsa: el cooldown CUBRIÓ el viaje real del Oppo (FN, con EXIT entregado 11 h tarde) | Tormenta de wakes del sensor al andar cerca del coche: batería, telemetría y ruleta de espejismos GPS | Streak puro: `nextSentryWakeAbortStreak` — solo un arm de sentry-wake refutado como abort andando lo extiende; cualquier otra sesión lo resetea. `sentryWakeRearmCooldownMs`: 0 bajo el umbral; después base×2^exceso, cap en `sentryWakeCooldownMaxMs`. SOLO duerme el nominador significant-motion; EXIT, AR y net periódico siguen | SentryWakeCooldown.kt:6-59 (+`DetectionSessionOutcomes`); CDS:152-159, 957-975, 1176; SignificantMotionMonitor.kt:93-129; Config:502; DetectionEvent.kt:258, 273 (`WAKE_COOLDOWN`) | `sentryWakeAbortStreak`, `lastArmedTrigger`, deadline elapsedRealtime | ✅ SentryWakeCooldownTest (9) | ⚠️ COLISIÓN REAL con el contrato "todo trigger dispara siempre" (§5): en el field 17/18-08 el cooldown + EXIT retenido dejaron el viaje sin nominador vivo; el backstop que preguntó fue DET-SAFETY-NET-001 (por diseño) |
| **DET-FGS-REAPER-001** | Field 2026-07-21, Oppo: ColorOS congela en vez de matar — una FGS de false-enter colgada ~2 h con su notificación pegada | Notificación de detección fantasma glued a proceso muerto/congelado | `shouldReapGhostDetectionFgs = hasStalePending ∧ isPeriodicTick ∧ ¬isDetectionRunning`. Dos candados: sesión VIVA veta, y solo el tick periódico puede segar. Solo DESCARTA la notificación, nunca mata proceso | GhostFgsReapDecision.kt:4-30; ParkingSafetyNetWorker:135, 444 | `hasStalePending` (heartbeat de DET-NEVER-SILENT-001), `isDetectionRunning` (DET-READY-001c), `SOURCE_PERIODIC` | ✅ GhostFgsReapDecisionTest (4) | Consume la MISMA señal stale-pending que DET-NEVER-SILENT-001 y el flag de DET-READY-001c |
| **DET-TOGGLE-001** | — (regla de producto) — NO VERIFICADO incidente | Con detección apagada por el usuario, la app pedía permisos en vez de reflejar su decisión | `!autoDetectEnabled → Disabled(TURNED_OFF)` ANTES del chequeo de permisos; un solo botón hace flag+permisos en un tap; el path BT tampoco arma con toggle off; el registro AR lo respeta | ObserveDetectionReadinessUseCase.kt:31, 79-81; DetectionReadiness.kt:51; AppPreferences.kt:22; BluetoothConnectionReceiver.kt:68; ActivityRecognitionManagerImpl.kt:87; MainActivity.kt:113; UI DetectionUiState.kt:16, 35, 79, HomeViewModel:608, HomeEffect:23, 26, HomeScreen:220, 904, HomeIntent:56, HomeSheetContent:125, HomeDetectionSurface:156 | `observeAutoDetectParking()` | ✅ DetectionUiStateTest (solo presentación); la rama TURNED_OFF del dominio NO VERIFICADO test con tag | Rama Disabled del mismo `resolve()` que 001b; con DET-TOGGLE-002 |
| **DET-TOGGLE-002** | — (regla de producto: fricción de onboarding) — NO VERIFICADO incidente | Usuarios que activaron detección pero nunca aparcaron; apagados sin darse cuenta | (a) Nudge cold-start: `Ready(COORDINATOR) ∧ ¬hasConfirmedFirstPark ∧ nudgeCount<3 ∧ now−last ≥ 3 días`; auto-desactivado al primer park. (b) Confirmación de apagado con undo + snackbar solo en caída genuina working→stopped. (c) Diálogo educativo en "Quizá luego" | EvaluateFirstParkNudgeUseCase.kt:9-60; FirstParkNudgeWorker.kt:19; ConfirmParkingUseCase:55, 343; AppPreferences.kt:25; AppNotificationManager.kt:117, 189 + Impl:195, 372, 460; PaparcarApp.kt:101; MainActivity.kt:198; StartAddParkingEventBus.kt:8; UI Settings/Permissions/Home (SettingsViewModel:127, SettingsEffect:16, SettingsScreen:220, PermissionsViewModel:116, PermissionsIntent:39, PermissionsContent:132, PermissionsState:51, DetectionUiState:95, 103, HomeViewModel:827-856, HomeEffect:29, HomeScreen:208, HomeSheetContent:115, PresentationModule:46); strings ×9 | `hasConfirmedFirstPark`, `firstParkNudgeCount`, `lastFirstParkNudgeAtMillis` | ✅ SettingsViewModelTest (presentación) + EvaluateFirstParkNudgeUseCaseTest (7; mención del tag NO VERIFICADA) | Su nudge produce un pin `MANUAL_REPORT` puro — frontera deliberada con DET-NUDGE-PIN-PROVENANCE-001 |
| **DET-G-01b** | — (affordance de cold-start, sin incidente) — NO VERIFICADO | En true cold start (sin park nunca → sin valla) no existía trigger automático al abrir la app ya conduciendo | "I'm driving" declara ESTE coche y arranca el job de tracking (`ManualParkingDetection.start()`, `DetectionTrigger.MANUAL` — siempre admitido por DET-STRATEGY-GATE-001); al aparcar, el egress crea la primera valla y bootstrapea el loop | ManualParkingDetection.kt:4-14; ManualParkingDetectionImpl.kt:13; AndroidDetectionModule.kt:78; UI HomeIntent:51, HomeViewModel:604, HomeSheetContent:123, HomeDetectionSurface:98 | trigger MANUAL, vehículo declarado | 🔴 sin test | Comparte interfaz con DET-MANUAL-CANCEL-001; MANUAL es la excepción explícita del gate de DET-STRATEGY-GATE-001 |
| **DET-RELIABILITY-001** | Field 08-08 Málaga (la exención de batería es condición previa) + doctrina OEM-kill | Cada superficie re-derivaba sus propias condiciones de fiabilidad | UN evaluador puro: tabla BT×exención×OEM → OPTIMAL/GOOD/REDUCED + issues; el tier (producto) en eje aparte. Todas las superficies leen el MISMO report vía `ObserveDetectionReliabilityUseCase` | EvaluateDetectionReliabilityUseCase.kt:10, 35-67; ObserveDetectionReliabilityUseCase.kt:13; DomainModule.kt:245; ParkingStrategyResolver.kt:118; DetectionReliability.kt:5; UI Settings/Permissions (SettingsViewModel:85, 175, SettingsState:28, SettingsScreen:512, PermissionsViewModel:62, PermissionsState:33, PermissionsContent:313, SettingsIntent:17); MockScenario.kt:33, FakeOtherDataSources.kt:101 | BT pairing, `isBatteryUnrestricted`, OEM agresivo | ✅ EvaluateDetectionReliabilityUseCaseTest (9) | Con DET-TIERS-001 (el tier sale del mismo invoke) y DET-SESSION-RELIABILITY-STAMP-001 (mismos insumos, stamp por sesión) |

### 1.4 Cluster H — capa androidMain, workers, geofence, sensores, BT plataforma, rutas, vehículo/sync (40; BUG-SERVICE-109 fusionado en 1.2)

#### 1.4-A · Servicio FGS + intake + ciclo de vida

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-INTAKE-001** | 2026-07-11 00:38 (field) | Un AR TickOnly llamó `stopSelf()` 10 ms después de entregarse el GEOFENCE_EXIT real; onDestroy canceló su handling a media clasificación y el EXIT se descartó como huérfano | UN intake `Channel(UNLIMITED)` de un solo consumidor: cada intent se procesa a completitud en orden; el teardown se decide en UN sitio por comando y solo si (a) no hay detection job activo y (b) `stopSelfResult(startId)` confirma que el startId sigue siendo el más nuevo | CDS:129-147 (Command+intake), 166-191 (consumidor), 239-241 (enqueue-only), 247-293 (processIntent+epílogo), 921-939 (stopIfIdle), 1005; ForegroundServiceController.kt:64-79 (stop yielding) | `intake: Channel<Command>`, `@Volatile lastStartId`, `startId`, `detectionJob` | 🔴 | **R6** (stop de un trigger decapita a otro); base de la mitigación de R3/R4 |
| **DET-B-01** | — NO VERIFICADO (defensivo, sin incidente) | Acción desconocida al servicio ya promovido → notificación FGS colgada si nadie hace teardown | Acción no manejada = no-op logueado; el teardown lo aplica el MISMO epílogo común (`resolveIdleEpilogue`, :292) que corre tras todo comando | CDS:288-290 | ninguno propio | 🔴 | DOMINADO por DET-INTAKE-001 (§4) |
| **DET-B-02** | Bug del FGS huérfano (fecha NO VERIFICADA) | Restart sticky con intent null tras process kill: el estado vivía solo en memoria; promover el FGS pegaba una notificación sin trabajo detrás | intent == null ⇒ stop SIN promover (`fgs.stopForegroundAndSelf(startId)`); un viaje real re-arma por trigger fresco. FN de coste cero > notificación colgada | CDS:200-210 | ninguno (decisión pura sobre el intent) | ✅ androidUnitTest CoordinatorDetectionServiceTest.kt:38-42 (1 test Robolectric) | No (la rama corre antes del intake) |
| **DET-B-03** (solo-test) | — | — (etiqueta documental del test Robolectric del ciclo FGS) | No es guard de producción: nombra la cobertura | Solo CoordinatorDetectionServiceTest.kt:15 | — | ✅ (es el test) | — |
| **DET-ENDED-VETO-RACE-001** | 2026-07-23, ambos devices (field) | El `DetectionEnded` enviado desde el `finally` del job aún activo se procesaba INLINE (Main.immediate): `stopIfIdle` vio `detectionJob.isActive == true` y vetó su propio teardown → FGS pegado a un proceso vivo ocioso | `DetectionEnded` se registra en `invokeOnCompletion` del job, NUNCA desde su `finally`: el runtime lo invoca con el job COMPLETO (`isActive == false` garantizado); el guard de identidad (`detectionJob === startedJob`) preserva el supersede | CDS:1324-1331 (por qué NO desde el finally), 1337-1350; backlog det-ended-veto-race-001.md; PARKING-DETECTION.md:1435 | `detectionJob`, `invokeOnCompletion`, `intake`, `lastStartId` | 🔴 | **R6** (su segunda mitad); preserva **R4** |
| **DETECT-SERVICE-RACE-001** | 2026-05-30 (heartbeat 15-min con coordinator activo, diagnostics 2026-05-30) | El `finally` de un job supersedido llamaba a stopSelf y destruía el servicio matando por onDestroy al coordinator de reemplazo | Guard de identidad en el `finally`: solo el job actual (`detectionJob === thisJob`) ejecuta teardown/side-effects; un job reemplazado solo loguea | CDS:1311-1315 (+1341); espejo BT [BT-BUG-101]: BluetoothDetectionService.kt:164-175; PARKING-DETECTION.md:678-720 | `detectionJob` (slot único), `thisJob` capturado | 🔴 | **R4** (es exactamente R4); espejo en BT |
| **DET-RESIDENT-FGS-001** | Diseño (Q2, residencia SENTRY); el detector de kill responde a OEM-kills reales; la tormenta que motivó su cooldown es field 2026-08-13 | Muerto el proceso tras cada sesión, todo trigger posterior paga resurrección + restricción FGS-desde-background; y un OEM-kill del residente pasaba inadvertido | Al quedar ocioso con coche aparcado + auto-detect ON + estrategia COORDINATOR, el servicio NO muere: degrada a SENTRY residente (GPS off, notificación MIN); sig-motion despierta al proceso VIVO. Stamp durable de residencia que solo se limpia en salidas deliberadas: si sobrevive al proceso, prueba un kill del OS (2 detectores lo leen) | CDS:111-125, 257, 296-331 (handleSentryWake), 942-1009 (epílogo), 1012-1044 (enterSentry), 1055+ (F3 watch), 1083-1129 (ledger F2), 1186, 1435, 1457-1466; SentryResidenceStore.kt:7; SignificantMotionMonitor.kt:43-81; ParkingSafetyNetWorker:126, 487-514 (kill detector); SentryLifecycleDecision.kt:4, 47; DetectionRuntimeState.kt:50-135; DetectionEvent.kt:250; AppNotificationManagerImpl.kt:315, 420, 442 | `ServicePresence` (Dead/Sentry/Active), `SentryResidenceStore`, `sentryWatchJob`, notificación sentry | 🔴 (la decisión pura sí: SentryLifecycleDecisionTest, 11; la orquestación no) | **R13** (tormenta sig-motion), **R6** (comparte epílogo) |
| **BUG-FGS-001** | 2026-05-25 · 24 crashes, ~9 usuarios (Crashlytics; backlog fgs-crashes-2026-05-25.md) | `ForegroundServiceStartNotAllowedException`: el receiver AR llamaba `startForegroundService()` desde background en Android 12+ | El start privilegiado lo hace GMS: `PendingIntent.getForegroundService` para AR ENTER decisión y geofence EXIT; ningún receiver propio arranca un FGS desde background. El código crashante fue ELIMINADO — el tag sobrevive como restricción de diseño | ActivityRecognitionManagerImpl.kt:52-56; GeofenceManagerImpl.kt:94, 136, 166; CDS:225-231 (degradación si el promote falla), 657, 1494; GeofenceExitWitnessReceiver.kt:15, 29; ActivityTransitionReceiver.kt:90; EvaluateArEnterArmUseCase.kt:38; BluetoothDetectionService.kt:114-116 (promote-primero) | PendingIntents GMS; ventana 5 s de `startForegroundService` | 🔴 | **R7**; parcialmente histórico (§4) — razón documental de DET-G-01, DET-EXIT-WITNESS-001 y la lane de evidencia AR |
| **BUG-FGS-001a** | Crashes 2026-05 (BUG-FGS-002 en el doc: 5 eventos, 1 usuario, beta01) | `SecurityException` en Android 14 al hacer startForeground con tipo LOCATION sin el permiso runtime | `promote(withLocationPermission)`: solo declara `FOREGROUND_SERVICE_TYPE_LOCATION` si el permiso está concedido; si no, promote sin tipo | ForegroundServiceController.kt:24, 32-45; CDS:212-223 | flag `hasRequiredPermissions()` | 🔴 | No |
| **BUG-FGS-100** | Notificación FGS stale pegada al nuevo ciclo (Android 11–14, re-entrada por tap) | 5 paths llamaban `stopSelf()` sin `stopForeground(STOP_FOREGROUND_REMOVE)`: la notificación vieja quedaba pegada | TODO teardown pasa por el controller: `stopForeground(STOP_FOREGROUND_REMOVE)` antes de stop, en cada path; variante yielding con `stopSelfResult(startId)` | ForegroundServiceController.kt:19, 48-79, 91-93; CDS:937, 1005; BT: BluetoothDetectionService.kt:95, 110, 172, 195, 204 | notificación FGS, `startId` | 🔴 | **R6** (el `stopSelfResult` es la mitad del guard de R6) |

#### 1.4-B · Triggers de plataforma: heartbeat, sensores, AR, geofence

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-EXACT-HEARTBEAT-001** | FNs de campo por Doze-batching del periódico 15-min (aprendizaje Driversnote, heartbeat 300 s decompilado) | El periódico de WorkManager se batchea en ventanas de mantenimiento; los triggers de eventos dependen de GMS entregando a un proceso posiblemente muerto | Alarma exacta one-shot ~5 min mientras hay sesión aparcada (`setExactAndAllowWhileIdle`); cada tick re-arma el siguiente; `canScheduleExact` gatea y degrada a inexacta; `sync()` desde el safety-net en CADA tick = un solo punto de armado auto-sanante; `firedDelayMs` = métrica de puntualidad. Despertar NUNCA confirma | ExactHeartbeatScheduler.kt:13-88; ExactHeartbeatReceiver.kt:16; ParkingSafetyNetWorker:142, 722-725 (SOURCE_EXACT_ALARM); Manifest:34, 137; backlog det-exact-heartbeat-001.md | prefs `exact_heartbeat` (`next_at`), AlarmManager, permiso SCHEDULE_EXACT_ALARM | 🔴 | `enqueueCheckNow` unique REPLACE: una fuente puede pisar el check pendiente de otra (doc 03 §4.4; solo telemetría) |
| **DET-SIGMOTION-001** | 2026-07-04 (field): salida perdida porque el OEM mató el sampling background de Play Services | Geofence EXIT y AR viven dentro de GmsCore, que el OEM puede matar de hambre → salida sin señal | Sensor hardware `TYPE_SIGNIFICANT_MOTION` (one-shot, co-procesador, ~0 batería) como despertador que NO decide: encola el safety-net check (o despierta al SENTRY); el re-arme solo lo hace `sync()` si sigue aparcado y ocioso | SignificantMotionMonitor.kt:19-91, 115-152; espejos de sync: ParkingSafetyNetWorker:138, 841, PaparcarApp.kt:94, teardown del servicio; AndroidDetectionModule.kt:54 | `armed` (memoria, @Synchronized), cooldown [DET-SENTRY-COOLDOWN-001] en el monitor | 🔴 | **R13** (tormenta ~18 s/ciclo, 2026-08-13 — cerrada por el cooldown que vive AQUÍ porque 3 espejos llaman a sync) |
| **DET-STEP-SENSOR-REDMI-001** | 2026-07-18 · Redmi, llegada a casa (field) | Egress de ~40 pasos entregó 2: el detector NON-WAKEUP encola en el FIFO del sensor-hub y se pierde con el AP suspendido → el ancla nunca bloqueó (necesita 8) y derivó a la puerta | Pedir explícitamente la variante WAKEUP (`getDefaultSensor(TYPE_STEP_DETECTOR, wakeUp = true)`), fallback a non-wakeup solo si el device no tiene | AndroidStepDetectorSource.kt:28-44 | selección de sensor (sin estado) | 🔴 | Alimenta ANCHOR-LOCK-001 — sin pasos entregados aquel guard no puede actuar |
| **DET-AR-FIRST-001b** | 2026-07-11 (field): ENTERs stale con hasta 32 min de lag flash-arrancaron el FGS de la lane de decisión toda la tarde con el usuario en casa | GMS re-entrega la ÚLTIMA transición IN_VEHICLE conocida en CADA re-registro del mismo PendingIntent — y `registerTransitions()` corre en cada apertura de la app | Throttle in-process: como mucho un registro real por `RE_REGISTER_MIN_INTERVAL_MS` (30 min); la muerte de proceso lo resetea a propósito | ActivityRecognitionManagerImpl.kt:72-102, 185 | `@Volatile lastRegisteredAtMs` (memoria) | 🔴 | **R7** (entregas stale/re-entregadas) |
| **DET-G-01** | Validado en campo: Oppo 2026-07-10, 6/6 starts privilegiados | Un receiver/worker en background no puede legalmente arrancar un FGS en Android 12+ → el geofence EXIT no podía despachar salida Y armar la siguiente detección | La transición de geofence se entrega DIRECTA al servicio vía `PendingIntent.getForegroundService` (GMS concede el start privilegiado); FLAG_MUTABLE obligatorio; AR con doble carril: broadcast siempre-vivo = EVIDENCIA (solo estampa), getForegroundService ENTER-only = DECISIÓN | GeofenceManagerImpl.kt:133-150; ActivityRecognitionManagerImpl.kt:38-70; ActivityTransitionReceiver.kt:21; CDS:87-88, 225, 407, 634, 1492; ActivityRecognitionManager.kt:10 | PendingIntents (request codes 9100 / AR), `GeofenceEventBus` | 🔴 | **R7**; BUG-FGS-001 es su razón de ser |

#### 1.4-C · Workers + persistencia entre despertares + geofence lifecycle

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **ANCHOR-PERSIST-001** | 2026-07-05 · Oppo (field): salida a 69 km/h degradada a prompt porque el ancla in-memory estaba vacía tras un OEM-kill; mismo día: re-nag del prompt en cada app-start por throttle in-memory | Un ancla/throttle en memoria está vacío exactamente cuando el reconcile lo necesita → plaza perdida conduciendo, o nag repetido | El ancla de posición (y el throttle de prompts) se persisten a DISCO (SharedPreferences `parking_safety_net`, claves por geofence), se re-sellan en cada cura y se podan cuando la sesión muere | ParkingSafetyNetWorker:243-248 (escritura en cura), 391-400 (throttle), 607-672 (persistencia+prune), 740-752 (claves); misma filosofía TripTrailImpl.kt:13 | prefs `parking_safety_net`: `anchor_`, `anchor_steps_`, `anchor_seal_pos_`, `anchor_seal_at_`, `prompt_`, `exit_delivered_`, `cure_` | 🔴 | Obs. 3 del doc 03: prefs compartido servicio/worker/AndroidDetectionStepAnchors, prune orden-sensible; **R8** |
| **DET-ARRIVAL-HANDOFF-001** | 2026-07-08 20:41 · Oppo (field): el evaluador detectó el viaje de vuelta EN MARCHA, limpió la sesión, y nadie escuchaba cuando el usuario aparcó 5 min después | Salida despachada sin dueño de la LLEGADA = llegada huérfana | Toda salida despachada acaba en exactamente uno de: backfill encadenado O detección viva; si el FGS-start se deniega, prompt "¿sigues aparcado?" — una notificación gana al silencio, NUNCA "ninguno" | ParkingSafetyNetWorker:351-379 | cadena WorkManager (departure→backfill), `manualParkingDetection.start()`, `anyPromptActive` | 🔴 | **R1** (cerró el "ninguno"; DET-ARRIVAL-DOUBLE-PIN cerró el "ambos") |
| **DET-ARRIVAL-DOUBLE-PIN-001** | 2026-07-20 · Redmi (field): pin fantasma 0.5 en Calle Pantoque a 96 m del real — el backfill ejecutó cuando una sesión viva ya seguía el MISMO aparcamiento | Dos pipelines colocan la misma llegada | Exactamente un pipeline COLOCA la llegada: si `detectionRuntime.isRunning` al ejecutar el backfill, se difiere al vivo (o a su nudge) y success; la salida vieja ya fue despachada, el spot se libera igual | ParkingBackfillWorker.kt:51-67; complementado por DET-BACKFILL-TAINT-001 (:74-103); backlog det-arrival-double-pin-001.md | `DetectionRuntimeState.isRunning` (check-then-act) | 🔴 | **R1** — el doc 03 marca NO VERIFICADO que la sub-ventana lectura→confirm esté cerrada; defensa final = `replaceActiveSession` @Transaction (R10) |
| **BUG-WORKER-001** | 2026-05 (logs diagnostics/2026-05-14, ambos devices): `NOT_FOUND` — address/placeInfo no llegaban NUNCA a Firestore | El worker de update disparaba en paralelo antes del `set()` del save | Encadenado en la MISMA unique chain `parking_chain_$sessionId` con APPEND_OR_REPLACE: el update solo corre tras el save; si el save agotó reintentos, corre igual (dato parcial > nada) | WorkManagerParkingSyncScheduler.kt:48-59; warning residual UpdateParkingSessionAddressAndPlaceWorker:65-67 | cadena WorkManager unique por sessionId | 🔴 | **R11** |
| **BUG-WORKER-002** | Doc 2026-05-25: `JobCancellationException` a mitad de escritura dejaba Room y Firestore inconsistentes | El OEM mata el Job de WorkManager en vuelo | Las escrituras Firestore del save van en `withContext(NonCancellable)`: o completan o fallan con retry, nunca a medias | SaveNewParkingSessionWorker.kt:65-76 | NonCancellable + retry ×5 exp 30 s | 🔴 | **R11/R10** |
| **MAPPER-003** | Field: el doc de Firestore acabó con `detectionReliability=null` aunque el coordinator pasó 0.90 | `workDataOf` no preserva nulls para primitivos: el null legítimo y el valor real colisionaban | Centinela NaN para "ausente" en la Data del worker; el mapper del otro lado decodifica a null | SaveNewParkingSessionWorker.kt:137-139; ParkingSyncWorkerTest.kt:91 | Data de WorkManager (centinela NaN) | ✅ ParkingSyncWorkerTest (12) | No |
| **SESSION-RESTORE-001** | — NO VERIFICADO incidente (deriva de sensibilidad) | Una geocerca restaurada con radio plano cambia la sensibilidad de EXIT respecto a la original | La restauración re-registra con el MISMO radio talla/precisión: `config.geofenceRadiusFor(size, accuracy)` leyendo `session.sizeCategory` | GeofenceJanitorWorker.kt:89-98; misma función en EvalSNC:186 | `session.sizeCategory`/`accuracy` (Room), `geofenceRadiusFor` | 🔴 | **R8**; patrón correcto: regla única en config, no duplicada |
| **GEOF-001** | Reboots/reinstalls de campo; diseño post-TTL | GMS borra TODAS las geocercas en reboot y reinstall; un TTL habría dropeado la detección de salida en silencio | NEVER_EXPIRE + restauración: janitor (12 h periódico + pase one-shot tras boot/login-sync) relee sesiones activas de Room y re-registra (idempotente, initial trigger 0); la prevención de huérfanas es borrado EXPLÍCITO en cada session-end | GeofenceManagerImpl.kt:190-194; GeofenceJanitorWorker.kt:25-36, 138-149; UserParkingRepositoryImpl.kt:117 (restore post-sync); PaparcarApp.kt:82; ParkingSyncScheduler.kt:55 | Registro GMS vs Room (fuente de verdad) | 🔴 | **R8** (janitor vs cura sobre la misma valla; ventana ciega del re-registro) |
| **GEOF-RESTORE-001** | Post-update: la app actualizada quedaba ciega hasta la siguiente apertura manual | `MY_PACKAGE_REPLACED`/reboot borran vallas; un aparcado activo pierde su salida durante esa ventana | `BootCompletedReceiver` (BOOT_COMPLETED incl. sintético Android 15+ post-force-stop, + MY_PACKAGE_REPLACED) re-arma AR + re-encola periódicos + restauración INMEDIATA de geofences | BootCompletedReceiver.kt:23 (+42-47); PaparcarApp.kt:89; Manifest:88 | colas WorkManager (KEEP → inocuo vs PaparcarApp) | 🔴 | **R8**; doble-enqueue con PaparcarApp mitigado por KEEP |
| **DET-EXIT-WITNESS-001** | 2026-07-09 · Oppo 12:55 (field): el EXIT desapareció SIN traza — imposible distinguir "GMS nunca emitió" de "emitió y el start fue rechazado" (los 24 crashes de BUG-FGS-001 eran esta lane) | La entrega principal del EXIT va por `getForegroundService`: si el OS se la traga, no queda evidencia | Valla EXIT testigo idéntica entregada a un receiver de SOLO log, decision-free por contrato. Discriminador: línea witness sin onStartCommand en segundos = entrega tragada; sin línea witness = GMS nunca disparó | GeofenceManagerImpl.kt:90-113, 165-176, 186-187; GeofenceExitWitnessReceiver.kt:12-29; Manifest:130 | valla `witness_`+geofenceId, request code 9102; solo log | 🔴 | **R7** (instrumento de diagnóstico de R7, no guard de decisión) |

#### 1.4-D · Bluetooth plataforma

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-BT-TIMEOUT-SAVE-001** | 2026-08-06 01:46 · Kamiq (field): aparcó en casa y entró directo — nunca cubrió 30 m andando; perder la sesión PROPIA era regresión vs el coordinator | El walk-away de ≥30 m guarda la confianza COMUNITARIA, no el "dónde está mi coche"; exigirlo para la sesión propia perdía el home-park | Si la ventana de walk-away expira con el candidato ESTACIONARIO pin-grade en pie → guardar la sesión propia con fiabilidad reducida (`reliabilityBluetoothTimeoutSave`), path `bt_timeout`; nada comunitario se publica al confirmar. Un drop de BT en marcha no llega aquí: desplazamiento a ritmo de vehículo aborta | BluetoothParkingDetector.kt:123-153, 210-213; Config:700; backlog det-bt-timeout-save-001.md | `config.btWalkAwayTimeoutMs`, veredictos `EvaluateBtParkUseCase`, `PATH_BLUETOOTH_TIMEOUT` | 🔴 la rama del detector; el veredicto de dominio ✅ EvaluateBtParkUseCaseTest (11) | **R2** (su confirm compite con el coordinator por `ConfirmParkingUseCase`) |
| **DET-RETURN-ANCHOR-001** | 2026-07-07, ambos devices (field): la plaza de la playa nunca se liberó — vuelta HORAS después, ancla expirada y estado EXIT de GMS envenenado OUTSIDE → el drive-away no produjo NINGUNA señal | El regreso al coche es el único momento que arregla ancla y valla a la vez, y nadie lo escuchaba | TRES señales de "he vuelto", todas aceleradores del MISMO evaluador (nunca deciden): (1) valla ENTER gemela → broadcast → check gated (jamás al servicio); (2) BT CONNECT al MAC emparejado (arranca con el motor, inmune a Doze); (3) post-confirm BT sella el ancla. El check re-sella ancla+pasos y cura la valla INSIDE segundos antes del drive-away | GeofenceManagerImpl.kt:63-88, 152-163, 184-185; GeofenceEnterReceiver.kt:14; BluetoothDetectionService.kt:150-157, 185-194; ParkingSafetyNetWorker:709-721 (SOURCE_GEOFENCE_ENTER/BT_CONNECT/BT_PARK); Manifest:123 | valla `enter_` (sin initial trigger — evita cure-loop), `BtConnectionStore`, claves anchor del prefs | 🔴 | **R8** (la cura abre la ventana ciega — por eso el throttle); R2 (lane BT) |

#### 1.4-E · Rutas

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-ROUTE-TRACK-001** | 2026-08-09 (field): limpiar la ruta en el `finally` borraba una ruta EN CURSO cuando un fin de job espurio era seguido de re-arm → línea recta desde el pin | La ruta conducida debe sobrevivir background/process-death y morir en el momento correcto | Store a disco alimentado por (a) tap del stream de tracking y (b) tap pasivo; se limpia EN CONFIRM (ruta consumida al pin), NUNCA en el finally; freshness-gate al adjuntar (`ROUTE_FRESHNESS_MS` 30 min); red de seguridad `NEW_TRIP_GAP_MS` (20 min) resetea en frío | DrivingRouteStoreImpl.kt:12 (prefs `driving_route`, synchronized); CDS:100, 1269-1270, 1318-1323; ConfirmParkingUseCase:69, 213-217, 267-269, 356-374, 418-421; DrivingRoute.kt:8, 28-32; DrivingRouteStore.kt:8; Migrations.kt:187 (v15→16), UserParkingEntity.kt:54, DomainModule.kt:131; HomeTripController.kt:97, 300-311, 393 | prefs `driving_route`, columna `routePolyline` (Room+Firestore), `ROUTE_FRESHNESS_MS` | ✅ ConfirmParkingUseCaseTest:588+, ParkingSessionMapperTest:178, HomeTripControllerTest:256 | La carrera clear-vs-re-arm es R4-adjacent; resuelta moviendo el clear a confirm |
| **ROUTE-PASSIVE-FILL-001** | 2026-08-14 (field, `9625c0ed`): siesta GPS de 7 min de MIUI → agujero de 4.6 km que el matcher tuvo que reconstruir | El OEM throttlea NUESTRA petición de fixes; una app de navegación viva tiene fixes que nosotros no vemos | Tap `PRIORITY_PASSIVE` que hereda a coste cero los fixes de OTRAS apps y los vierte SOLO en la ruta persistida — JAMÁS en el stream del coordinator; el gate de precisión+decimación del store absorbe la calidad; el tap muere con la sesión | AndroidLocationDataSourceImpl.kt:93-113, 135-137; CDS:102, 1224-1235, 1309; LocationDataSource.kt:26; IosLocationDataSourceImpl.kt:38 (no-op); FakeLocationDataSource.kt:57 | job `passiveRouteTap`, `DrivingRouteStore` | 🔴 | No (por diseño no puede provocar geofences ni scoring) |
| **ROUTE-FIX-ACCURACY-001** | 2026-08-14 (field, `214850cf`): fixes de acc 98–157 m doblaban la línea urbana en bucles fuera de calle | Un fix sin precisión no mide nada dibujable, pero descartarlo del matcher pierde el único dato de un tramo | Doble capa: (1) gate de ingesta `HOPELESS_ACCURACY_METERS=100` en el store; (2) los que entran se pesan por su PROPIA precisión en el matcher (σ por medición, floor `EMISSION_SIGMA_METERS`) | DrivingRoute.kt:20-26, 47; TrailMapMatcher.kt:21, 76-83, 178-187 | constantes puras | ✅ DrivingRouteTest:43+, TrailMapMatcherTest:368+ | No |
| **ROUTE-GAP-HONEST-001** | 2026-08-14 (field, `a244d975`) + 2026-08-14 22:51 (stub de 40 m/5 puntos en un pin backfill con marcador de origen pegado al destino) | Un agujero ruteado como dato inventa un tour; un "route" de puñado de fixes dibuja un origen falso | Un agujero medido NUNCA se rutea como dato: se corta la línea (cuts), lo reconstruido se marca INFERRED, la UI lo atenúa y PREGUNTA (veredicto persistido y sincado); ruta < `MIN_ROUTE_EXTENT_METERS`(150) → no se adjunta (el worker post-park reconstruye pin-to-pin, marcado inferred) | ConfirmParkingUseCase:396-410, 430-433; TrailMapMatcher.kt:43, 110-296; InferredRoute.kt:9-20; Room v17→18 Migrations.kt:215, UserParkingEntity.kt:60-63, UserParkingDao.kt:169, 201; ParkingHistoryDto.kt:41-44, RemoteUserProfileDataSourceImpl.kt:183, 244; UserParkingRepositoryImpl.kt:225, UserParkingRepository.kt:88-95; EnrichParkingSessionWorker.kt:84, 135, 200; UI ParkingLocationScreen.kt:151-292, PaparcarMapView.kt:464, 613, 1266, ParkingLocationViewModel.kt:72; UserParking.kt:78-86, RouteInferenceResolution.kt:5 | `inferredStretches` + `routeInferenceResolution` (Room/Firestore), cuts del matcher | ✅ TrailMapMatcherTest:291+, ConfirmParkingUseCaseTest:594-652 | No |
| **ROUTE-QUALITY-001** | 2026-08-10 (field): la línea empezaba en la A-491, ~500 m pasado el spot real | El primer punto del store es el primer fix tras armar, ya dentro del drive; y las vías de servicio robaban tramos | (1) Prepend del aparcamiento previo como ORIGEN verdadero, ventana de plausibilidad 15–5000 m; (2) handicap del matcher a vías menores/servicio; (3) UI: sin ruta dibujable → sin marcador | ConfirmParkingUseCase:218-223, 380-395, 423-428; TrailMapMatcher.kt:129, 401, 452, 581; RoadNetworkDataSource.kt:10; OverpassRoadNetworkDataSourceImpl.kt:40-52 (isMinor); UI ParkingLocationScreen.kt:171, 414, 695, PapSectionHeader.kt:52 | `MIN/MAX_ORIGIN_PREPEND_METERS`, `RoadWay.isMinor` | ✅ ConfirmParkingUseCaseTest:649+, TrailMapMatcherTest:246-445 | No |
| **ROUTE-SNAP-001** | Diseño (línea on-road en vivo); cualquier fallo → trail crudo | La línea GPS cruda corta manzanas por deriva | Snap a red OSM (Overpass) con caché por bbox; TODO fallo de red/parse devuelve `Result.failure` y el caller conserva el trail crudo; el matched se limpia con el trail | OverpassRoadNetworkDataSourceImpl.kt:18; HomeTripController.kt:44-229, 386; RoadNetworkDataSource.kt:5, 17; PaparcarMapView.kt:607, 1249; PresentationModule.kt:28; MockModule.kt:73, DevCatalogScreen.kt:132 | caché de roads por bbox, `matchedTrail` | ✅ HomeTripControllerTest:45, 204 | No |
| **DET-ROUTE-SNAP-STORE-001** | Diseño (historial sin re-computar); deriva del trabajo de rutas 08-2026 | Re-snapear en cada render del historial es caro y no determinista | El snap se hace UNA vez post-park en el worker de enrichment y se persiste (`routeSnapped=true`); hasta entonces "recalculating"; offline → retry; agotados los retries se acepta la ruta cruda como final | EnrichParkingSessionWorker.kt:29-32, 65-78, 193; Room v16→17 Migrations.kt:201, UserParkingEntity.kt:57, UserParkingDao.kt:179; ParkingHistoryDto.kt:38, RemoteUserProfileDataSourceImpl.kt:180; UserParkingRepositoryImpl.kt:204, UserParkingRepository.kt:24, 77; UI ParkingLocationScreen.kt:144, 210; UserParking.kt:74 | flag `routeSnapped` (Room+Firestore) | ✅ ParkingSessionMapperTest:178 | Indirecta con la cadena de sync (R11) |
| **ROUTE-START-AT-CAR-001** | 2026-08-17 23:57 · Redmi (field, `12b1969a`): en un viaje SANO la salida verificada liberó el spot minutos después de arrancar → el lookup solo-ACTIVA devolvía null y la ruta nacía 130 m pasado el pin previo | El seed de origen solo funcionaba cuando la detección de salida había FALLADO | "Aparcamiento previo" NO exige sesión aún ACTIVA: el pin más reciente del coche, liberado o no, es donde empezó este viaje (`getActiveSessionByVehicle ?: getPreviousSession`); la ventana de plausibilidad sigue rechazando un origen stale cross-town | ConfirmParkingUseCase:224-238 (ruta persistida); HomeTripController.kt:250-262, 319-334 (trail en vivo, `previousParkedOriginFor` + techo 5 km) | `getPreviousSession` (Room), ventana de plausibilidad | ✅ ConfirmParkingUseCaseTest:678 (fixture del field) | SOLAPE doble implementación deliberada (§3-otros): un cambio en "pin previo" exige barrer ambos |
| **ROUTE-END-AT-CAR-001** | Cola peatonal: el store sigue muestreando mientras el usuario se aleja con GPS vivo (egress proof/hold) | La línea terminaba en el peatón, o cortaba en seco antes del pin | La ruta guardada es la de CONDUCCIÓN y termina en el ancla — `endAtAnchor`: se descartan los fixes posteriores al timestamp del ancla y el ancla se añade como vértice final si la línea queda corta (ventana 15–5000 m, espejo del origin-prepend); ancla sin timestamp real no recorta | DrivingRoute.kt:34-40, 59-79; ConfirmParkingUseCase:231-233, 362-364, 375-378; UI PaparcarMapView.kt:265, 622, 894, 970, ParkingLocationScreen.kt:176, PaparcarMapMarkers.kt:287 | `MIN/MAX_ANCHOR_APPEND_METERS`, timestamp del ancla | ✅ DrivingRouteTest:66+, ConfirmParkingUseCaseTest:711-759 | No |

#### 1.4-F · Vehículo / sync

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **VEHICLE-SYNC-001** | — NO VERIFICADO incidente de campo | Un confirm sin vehicleId explícito no sabía a qué coche atribuir la sesión | Resolución en dos niveles: vehicleId explícito (BT lo resuelve del MAC — fallar el lookup es violación de precondición, no fallback) / null → vehículo activo (legacy mono-vehículo) | ConfirmParkingUseCase:125-145 (única ocurrencia) | `vehicleId` param, `getActiveVehicle` | 🔴 (ConfirmParkingUseCaseTest cubre el use case sin citar el tag) | **R9** — mitad de atribución dominada por `nominatingVehicleId` de VEH-ACTIVE-FENCE-001 (§4) |
| **VEHICLE-CATEGORIZATION-001** | BUG-SIZE-PARITY (field/sync): el worker dropeaba size/carbody en el sync, la Spot decía "size unspecified" y `syncFromRemote` PISABA el size de Room con null | Un campo que falta en el DTO/worker degrada silenciosamente en el round-trip | Paridad end-to-end del par size/carbody: claves propias en la Data del worker, fila del DTO, defensa en el mapper para docs legacy (blank → null), inferencia `VehicleCatalog.inferBodyType` con fallback regex | SaveNewParkingSessionWorker.kt:112-116, 140-142; VehicleMapper.kt:17; strings ×9; docs/architecture/VEHICLE-CATEGORIZATION.md | `sizeCategory`/`carbodyType` en Data, DTO, Room, Firestore | ✅ VehicleCatalogTest:31; FirestoreDeserializerParityTest:14 | **R10** (el sync remoto pisando Room fue el síntoma) |
| **VEH-ACTIVE-FENCE-001** | Field 2 coches: liberar "por ranking" podía soltar la plaza del coche EQUIVOCADO; y la valla de un coche inactivo generaba FGS espurios en cada janitor | Con varios coches, ¿quién posee valla del OS y a quién se atribuye el pin? | Solo el vehículo ACTIVO (o con BT emparejado) posee geocerca del OS (`VehicleFenceOwnershipPolicy.shouldOwnFence`); el confirm de un inactivo no-BT no registra valla; el janitor NO resucita vallas de inactivos; declarar activo = camino único `DeclareActiveVehicleUseCase` + swap de vallas; el `nominatingVehicleId` de la valla que disparó GANA al activo (swap-race); liberar el spot de un inactivo ES declararlo activo; toda release es observable | VehicleFenceOwnershipPolicy.kt:6; ConfirmParkingUseCase:294-303; GeofenceJanitorWorker.kt:77-88; CPD:487-490, 1008, 1333; CDS:1273; SwapActiveVehicleFencesUseCase.kt:15; DeclareActiveVehicleUseCase.kt:7; ReleaseActiveParkingSessionUseCase.kt:76; DetectionEvent.kt:191; UI Home/Vehicles (múltiples); DomainModule.kt:166; PaparcarError.kt:57 | `vehicle.isActive`, `bluetoothDeviceId`, `nominatingVehicleId` del TripContext | ✅ VehicleFenceOwnershipPolicyTest (16), SwapActiveVehicleFencesUseCaseTest, HomeViewModelTest:900-1049, ConfirmParkingUseCaseTest:47-102, CPDTest:157, ReleaseActiveParkingSessionUseCaseTest:59 | **R9** (es EL guard de R9); toca R8 (qué vallas re-registra el janitor) |
| **GEOCODE-DEADLINE-001** | Colgones de búsqueda (search nunca emitía) | `HttpURLConnection` tiene timeouts INFINITOS por defecto; y el ciclo de fixes reiniciaba el ask del geocoding sin dejarlo contestar | Deadline explícito en la instancia nativa (`connectTimeout`/`readTimeout`); en común: deadline + pureza de caché en el repo y "la respuesta del primer ask ES la respuesta" en Home | AndroidGeocoderDataSourceImpl.kt:108-111; AddressAndPlaceRepositoryImpl.kt:19; HomeGeocodingController.kt:80, 100 | timeouts, caché del repo | ✅ variante común (AddressAndPlaceRepositoryImplTest:73, HomeGeocodingControllerTest:17); 🔴 la instancia androidMain | No |

### 1.5 Correcciones a `05-cobertura-tests.md`

Erratas verificadas por los parciales (el barrido del subagente E contó solo el literal exacto del tag):

1. **LOC-001 y LOC-002 NO están huérfanos de test**: `CoordinatorParkingDetectorTest.kt:344, 362, 368, 399, 427, 442, 469` los citan por nombre (sin corchetes — causa probable del falso 🔴).
2. **DET-LOG-03 figura 🔴 y tiene tests**: `CoordinatorParkingDetectorTest:216` ("coordinator emits a diagnostics session trace") y `FakeDetectionEventLogger:8` lo nombran.
3. **REFACTOR-300 figura 🔴 y tiene menciones de test**: `CoordinatorParkingDetectorTest:570` ("[REFACTOR-300 follow-up]"), `:2661` (REFACTOR-300-FIX), `FakeAppNotificationManager:18`.

Cifras corregidas: de los **129 tags de producción**, **90 con test (69.8 %)** y **39 sin test** (05 decía 86/43). Los 3 tags solo-test (DET-B-03, DET-D-04, DET-UNVERIFIED-ARM-DRIVE-PROOF-001) no entran en ese cociente.

---

## 2 · Agrupación por CLASE DE FALLO

Cada tag aparece en exactamente UN grupo primario (el fallo de producto que su incidente de campo
produjo o habría producido); los cruces se anotan. 15 grupos, 132 tags.

### G1 · «El ancla sigue al peatón» — el pin acaba donde está el CUERPO, no el coche (9)
ANCHOR-LOCK-001 · DET-ANCHOR-FREEZE-001 · DET-SHORT-TRIP-FREEZE-001 · LOC-001 · LOC-002 ·
PARKING-001 · DET-CREDIBLE-DRIVE-001 · DET-STEP-SENSOR-REDMI-001 · DET-CONFIRM-ANCHOR-001.
El eje: qué puede mover/borrar/re-capturar `bestStopLocation` una vez el coche descansa, y qué
taint arrastra la caminata de entrada. STEP-SENSOR-REDMI es la precondición de hardware (sin pasos
entregados el lock no puede actuar); CONFIRM-ANCHOR es el mismo fallo en la rama user-confirm (el
"Sí" tardío anclaba en el destino peatonal). *Cruces:* CREDIBLE-DRIVE también evita FN (Galeote);
la familia geométrica ×4 cruza con G2.

### G2 · «El pin cae en un punto intermedio del trayecto» — errand stop, semáforo, recogida (9)
DET-C-01 · DET-C-02 · DET-ANCHOR-EGRESS-001 · DET-GAP-ANCHOR-001 · DET-CONFIRM-FRESHNESS-001 ·
DET-EGRESS-PEDESTRIAN-CEILING-001 · DET-STEP-SPEED-GATE-001 · BUG-DETECT-310503 · BUG-GARAGE-COLA-001.
El eje: una parada que NO es el parking final no debe confirmar — egress obligatorio (C-01), confirm
tentativo con hold (C-02), el egress debe NACER en el ancla (ANCHOR-EGRESS), un ancla gap-born es
sospechosa de drive-past (GAP-ANCHOR), el hold re-valida frescura (CONFIRM-FRESHNESS), y los pasos
fantasma en marcha no cuentan (STEP-SPEED-GATE, CEILING). *Cruces:* GAP-ANCHOR tiene su mitad FN en
G5 (GAP-ANCHOR-ZONE); CONFIRM-FRESHNESS comparte fórmula con la familia geométrica (§3-b).

### G3 · «Movimiento no vehicular leído como coche» — peatón/bici/espejismo Doppler (5)
DET-DRIVE-PROOF-001 · DET-SENTRY-ARM-PEDESTRIAN-CLOCK-001 · DET-UNVERIFIED-CONFIRM-001 ·
DET-BIKE-NOT-A-CAR-001 · BUG-SCOOTER-001.
El eje: «esta sesión midió CONDUCCIÓN» solo lo enciende un track que un peatón/una bici/un mirage
no puede producir. *Cruces:* DRIVE-PROOF y SHORT-HOP (G5) son dos productores del mismo latch;
BIKE-NOT-A-CAR y SCOOTER alimentan un solo predicado `humanPowered`.

### G4 · «Evento stale/espurio con autoridad» — el evento nomina, nunca confirma (10)
DET-G-04 · DET-G-05 · BUG-WALK-DEPART-001 · DET-RIDE-PROOF-001 · DET-DEPART-PROOF-001 ·
DET-SESSION-BIRTH-001 · DET-EXIT-TRUST-001 · DET-ZOMBIE-PROBE-001 · DET-AR-FIRST-001b ·
DET-CONJUNCTION-001.
El eje: EXIT/ENTER re-entregados (Doze/OEM), ecos del mismo fix, o eventos disparados por un peatón
no pueden colocar pins, liberar plazas ni armar con seed. CONJUNCTION es la cara constructiva
(dos eventos independientes de acuerdo + física peatonal sí prueban, con todos los sentidos rotos).
*Cruces:* la tríada de evidencia triplicada (§3-a) vive aquí; G-04/G-05 tocan G1 (arm sobre peatón).

### G5 · «Pérdida total del aparcamiento (FN)» — degradar precisión antes que perder el parking (13)
DET-HONEST-CLOSE-001 · DET-GAP-ANCHOR-ZONE-001 · DET-WALK-ENTERED-ANCHOR-ZONE-001 ·
DET-NODRIVE-ZONE-001 · DET-KINEMATIC-EGRESS-001 · DET-SHORT-HOP-PROOF-001 ·
DET-UNVERIFIED-ARM-DRIVE-PROOF-001 · DET-JAM-WINDOW-001 · DET-AR-FIRST-001 · DET-D-03 ·
DET-BT-TIMEOUT-SAVE-001 · DET-RECONCILE-001 · BUG-DETECT-310502.
El eje: un viaje real con datos no puede acabar en silencio — zonas con duda acotada, cierre
honesto, pruebas de conducción alternativas para hardware degradado, presupuestos que no matan la
sesión en un atasco, arm AR-first cuando el EXIT llega tarde, save-don't-discard en el timeout.
*Cruces:* casi todos consultan los taints de G1/G2 antes de degradar; SHORT-HOP produce el latch de G3.

### G6 · «Liberación falsa del pin correcto» — el cierre/reconcile inventa una salida (5)
DET-FROZEN-COUNTER-001 · DET-STEP-BUDGET-ORIGIN-001 · DET-TRIP-WITNESS-001 · DET-WALK-FLOOR-001 ·
DET-BT-IDENTITY-GATE-001.
El eje: el step-budget walked-vs-rode solo es interpretable con contador vivo, sello fresco, origen
correcto y distancia fuera del ruido; y un viaje en el coche de OTRO no libera el propio (IDENTITY-GATE).
*Cruces:* espejo declarado EvalHC ↔ EvalSNC (§3-c); BUG-WALK-DEPART (G4) es su techo doctrinal.

### G7 · «Doble guardado / dos deciders sobre una llegada / sesión zombie» (7)
BUG-COORD-115 · DET-ARRIVAL-HANDOFF-001 · DET-ARRIVAL-DOUBLE-PIN-001 · DET-BACKFILL-TAINT-001 ·
DET-SUPERSEDE-001 · DET-AR-REARM-001 · DET-MANUAL-CANCEL-001.
El eje: exactamente UN colocador por llegada (ni «ninguno» ni «ambos»), un tap gana a todo
auto-confirm, una sesión zombie no bloquea el park verdadero, y un pin manual mata la detección en curso.

### G8 · «Atribución al coche equivocado» — multi-vehículo y BT como árbitro (8)
DET-TIERS-001 · DET-STRATEGY-GATE-001 · ARCH-MONITORING-002 · DET-BT-CONNECTED-NOT-PAIRED-001 ·
DET-BT-WRONG-CAR-ABORT-001 · DET-BT-OWNERSHIP-001 · VEH-ACTIVE-FENCE-001 · VEHICLE-SYNC-001.
El eje: la identidad de un coche BT solo la establece su MAC; una valla/el movimiento solo prueban
que el TELÉFONO se movió; BT nunca puntúa en el Coordinator — lo supersede o veta entero.
*Cruces:* DET-BT-IDENTITY-GATE (G6) es la misma asimetría aplicada al auto-release.

### G9 · «Pérdida por proceso/OS» — Doze, OEM-kill, GMS amnésico (16)
DET-SAFETY-NET-001 · DET-CURE-FRESH-001 · DET-NEVER-SILENT-001 · DET-RESIDENT-FGS-001 ·
DET-SIGMOTION-001 · DET-EXACT-HEARTBEAT-001 · ANCHOR-PERSIST-001 · DET-RETURN-ANCHOR-001 ·
GEOF-001 · GEOF-RESTORE-001 · SESSION-RESTORE-001 · DET-WATCH-REACTIVATE-001 ·
DET-WATCH-RESUME-RACE-001 · DET-SENTRY-COOLDOWN-001 · DET-BREADCRUMBS-001 · BUG-COORD-112.
El eje: nada crítico vive solo en memoria ni depende de una única entrega de GMS — reconcile
level-triggered, despertadores propios (alarma exacta, sig-motion, residencia sentry), persistencia
a disco de ancla/pending/ruta, restauración de vallas, y damping de tormentas (SENTRY-COOLDOWN, con
su colisión abierta en §5). *Cruces:* el safety net es a la vez el backstop de G5.

### G10 · «Carreras y teardown del servicio FGS» — notificaciones colgadas, jobs decapitados (12)
DET-INTAKE-001 · DET-B-01 · DET-B-02 · DET-B-03 · DET-ENDED-VETO-RACE-001 · DETECT-SERVICE-RACE-001 ·
BUG-FGS-001 · BUG-FGS-001a · BUG-FGS-100 · DET-G-01 · DET-FGS-REAPER-001 · BUG-SERVICE-109.
El eje: intake serializado, teardown decidido en un solo sitio, guards de identidad de job, starts
FGS solo por vías legales (GMS), y notificaciones que nunca sobreviven a su trabajo.

### G11 · «Sync/persistencia incoherente» — Room ↔ Firestore ↔ Data de workers (4)
BUG-WORKER-001 · BUG-WORKER-002 · MAPPER-003 · VEHICLE-CATEGORIZATION-001.
El eje: cadenas unique por sesión, escrituras NonCancellable, centinelas para nulls, paridad de
campos end-to-end.

### G12 · «Ruta mentirosa» — la línea guardada debe medir, no inventar (9)
DET-ROUTE-TRACK-001 · ROUTE-PASSIVE-FILL-001 · ROUTE-FIX-ACCURACY-001 · ROUTE-GAP-HONEST-001 ·
ROUTE-QUALITY-001 · ROUTE-SNAP-001 · DET-ROUTE-SNAP-STORE-001 · ROUTE-START-AT-CAR-001 ·
ROUTE-END-AT-CAR-001.
El eje: la ruta empieza en el pin que el coche dejó, termina en el ancla, no rutea agujeros como
dato, pesa cada fix por su precisión y sobrevive al proceso — pero JAMÁS alimenta la decisión de
detección (frontera con G3/G5).

### G13 · «UX deshonesta / estado visible incoherente» (13)
DET-PHASE-001 · DET-READY-001b · DET-READY-001c · DET-READY-001i · DET-READY-TRIP-OVER-PARKED-001 ·
DET-TOGGLE-001 · DET-TOGGLE-002 · DET-G-01b · DET-WATCH-HONEST-001 · DET-NUDGE-PERSIST-001 ·
REFACTOR-300 · DET-RELIABILITY-001 · GEOCODE-DEADLINE-001.
El eje: lo que la UI afirma (vigilando / aparcado / en viaje / pregunta pendiente / fiabilidad)
debe derivar del estado real y persistir lo que el usuario aún no respondió; nunca "green lie".
*Cruces:* NUDGE-PERSIST es la mitad visible de DET-NEVER-SILENT (G9).

### G14 · «Observabilidad/forense» — sin traza no hay fixture (6)
DET-LOG-03 · DET-LOG-04 · DET-PIN-PROVENANCE-001 · DET-NUDGE-PIN-PROVENANCE-001 ·
DET-SESSION-RELIABILITY-STAMP-001 · DET-EXIT-WITNESS-001.
El eje: cada sesión deja trace replayable, cada pin lleva provenance, cada EXIT tiene testigo, cada
cabecera estampa las exenciones. Es la condición de posibilidad del harness de replay (DET-SOLID-001 C4).

### G15 · «Doctrina y arquitectura» — tags sin condición ejecutable propia (6)
DET-D-01 · DET-D-02 · DET-D-04 · DET-SOLID-001 · DET-VERDICT-NOT-PREDICATE-001 · REFACTOR-200.
DET-SOLID-001 es el paraguas con sub-guards vivos repartidos por G2/G3/G4 (sus C1/C2/B4 ya están
contados ahí vía los tags que los heredaron); aquí se registra como doctrina («cada bug de campo →
fixture», «weak evidence → prompt»). REFACTOR-200 nombra la máquina de fases (viva) sin guardar
condición.

---

## 3 · Políticas únicas candidatas (con demostración caso por caso)

> ⛔ Recordatorio de doctrina [DET-VERDICT-NOT-PREDICATE-001]: ninguna de estas políticas empieza a
> codificarse hasta validar los 4 fixes de campo pendientes. Aquí solo se demuestra (o refuta) que
> N parches son el mismo problema.

### 3-a · «DriveProof» — la tríada de evidencia de salida triplicada+1 → un solo verificador. **SE SOSTIENE** (como verificador único parametrizado por momento; NO como fusión de veredictos)

Hoy la misma orquestación de tres patas (velocidad creíble `isCredibleDrivingSpeed` · ENTER
admisible (ventana + session-birth) · corroboración `isBeyondPedestrianReach`) está implementada en
`VerifyDepartureEvidenceUseCase` (pre-arm), `DetectParkingDepartureUseCase` (worker en vivo) y
`EvaluateSafetyNetCheckUseCase` (reconcile), con `EvaluateShortHopDriveProofUseCase` como cuarto
consumidor de la misma física (verificado línea a línea en el parcial G; los tres KDoc se citan
entre sí). La física ya está centralizada en Config (:1078, :1092); lo triplicado es la
ORQUESTACIÓN (qué evidencia, en qué orden, con qué fallback).

**Política única:** *una salida/conducción solo se afirma con movimiento MEDIDO que un peatón no
puede producir; toda evidencia pasa por el MISMO verificador con cuatro cláusulas — (1) admisibilidad
temporal (evidencia ≥ sessionStart, 3-d), (2) ENTER en ventana + corroboración física peatonal,
(3) velocidad creíble con independencia del fix disparador, (4) salud del contador — parametrizado
por PERFIL de momento: pre-arm / worker-live / reconcile / short-hop-track.*

Demostración tag a tag (cada escenario original queda cubierto):
- **DET-G-05 / BUG-REPARK-WALK-001** (2026-07-03 El Puerto, EXIT armó sobre peatón): el perfil
  pre-arm exige exactamente las cláusulas (2)+(3) que hoy exige `VerifyDepartureEvidence` — un EXIT
  a pie sigue saliendo `Unverified` y el upgrade tardío sigue siendo `verified_late` débil.
- **DET-DEPART-PROOF-001** (2026-07-27, el MISMO fix rompió la valla y "confirmó"): la cláusula (3)
  lleva el parámetro `departureProofMinGapMs`, activo SOLO en el perfil worker-live (el reconcile
  muestrea su propio fix en otro instante — el parcial G ya demostró que ahí NO está duplicado).
  Cubierto si y solo si la política conserva el parámetro por-perfil.
- **DET-SESSION-BIRTH-001** (2026-07-08, ENTER re-entregado del viaje de IDA borró un parking
  correcto): la cláusula (1) es la primera puerta del verificador — las 4 copias actuales colapsan
  en una (3-d).
- **DET-RIDE-PROOF-001** (2026-07-09, ENTER fantasma andando liberó la plaza real): cláusula (2) —
  `isBeyondPedestrianReach` corrobora; «el evento nomina, el movimiento confirma» es el enunciado
  de la política entera.
- **DET-EXIT-TRUST-001** (EXITs entregados a >900 m): la partición boundary/stale NO es física sino
  clasificación de la entrega; queda como INPUT del perfil (cuánto vale esta entrega), igual que hoy
  alimenta `staleExitDelivery`. Nada que fusionar, nada que perder.
- **DET-CONJUNCTION-001** (2026-07-08 cine, todos los sentidos degradados): modo de evidencia
  degradada del perfil reconcile — dos eventos OS independientes de acuerdo + (2) + contador mudo.
  Es una cláusula MÁS del mismo verificador (hoy ya comparte la conjunción con `ArmMidTrip` de
  EvaluateArEnterArm), no un verificador aparte.
- **DET-SHORT-HOP-PROOF-001** (2026-08-14, hop 900 m con `drive 3/303`) y
  **DET-UNVERIFIED-ARM-DRIVE-PROOF-001** (2026-08-15, arm unverified + EXIT comido por MIUI): el
  perfil short-hop-track (desplazamiento anclado al PIN que el coche dejó, fix a fix) es el cuarto
  productor del latch `driveProven`; la fusión documental de estas DOS etiquetas sobre UNA física ya
  está demostrada (CPDTest:1620 las cita juntas).
- **DET-SENTRY-ARM-PEDESTRIAN-CLOCK-001** (2026-08-16, playa a pie): su cláusula «cada fix del run
  debe medir conducción» YA vive dentro del perfil short-hop (SHP:79-94) y en la señal cruda de
  EvalUS — cubierto sin regla nueva.
- **DET-DRIVE-PROOF-001** (2026-07-27, mirage en el salón): `corroboratesDrive`
  (ventana+progresión) es el tercer productor del latch; entra como perfil track-window.
- **DET-UNVERIFIED-CONFIRM-001 + DET-SOLID-001 (weakLabels)**: un solo predicado `weakEvidenceOnly`
  con dos etiquetas — la política los funde en la tabla de pesos de etiquetas del verificador.

**Límite demostrado:** los VEREDICTOS no se fusionan. `ArmEvidence` (pre-arm), `DepartureDecision`
(worker), `SafetyNetAction` (reconcile) y `driveProven` (coordinator) son 4 veredictos citables en
diagnóstico con salidas distintas — la política es UN verificador de evidencia con N veredictos
consumidores, exactamente el patrón predicado-compartido de la doctrina. `isSustainedDepartureFromAnchor`
(DET-CREDIBLE-DRIVE) comparte física pero su DESTINO es limpiar el ancla: el predicado se expone
desde DriveProof, el destino pertenece a AnchorTrust (3-e).

### 3-b · «Pedestrian reach envelope» — la familia geométrica ×4 → una función. **SE SOSTIENE ÍNTEGRAMENTE**

La MISMA fórmula `d(base, fix) > steps × stride + acc_base + acc_fix + FLOOR` está copiada 4 veces
en CPD con (base, floor) distintos (ya catalogada en 02 §6):

| Copia | Líneas | base | steps | floor | Escenario de campo que cubre |
|---|---|---|---|---|---|
| `movementOutrunsSteps` | CPD:1825-1834 | ancla | stepCount | `minEgressDisplacementMeters` | sostener el ancla frente a deriva (Gavia: 2 pasos espurios no rompen; mute-hop envenenado del 12-07) |
| `egressExceedsWalkReach` | CPD:1880-1889 | ancla | stepCount | `egressBirthFloorMeters` | Calle Abeto 18-07: 26 pasos fantasma + coche a 500 m ⇒ Rejected/Ask(VEHICULAR_EGRESS) |
| `heldConfirmOutrunByVehicle` | CPD:1843-1855 | pin holdeado | stepCount | `egressBirthFloorMeters` | Bodegas Osborne/Abeto 23/24-07: hold liquidaba con el coche a 570 m ⇒ HOLD_STALE_DISCARDED |
| `escapesAnchorEnvelope` | CPD:1861-1868 | ancla | 0 | `minEgress` | salida stepless 2252-2283: contador VIVO en silencio + creep del coche ⇒ CAR |

Una función pura `outrunsPedestrianReach(base, baseAcc, fix, steps, floor)` con los 4 juegos de
parámetros NOMBRADOS reproduce cada copia exactamente (la única diferencia entre filas ES el juego
de parámetros); cada tag conserva su escenario porque su condición es la misma función con su
parametrización. Nota de frontera: es PARIENTE pero NO idéntica a `isBeyondPedestrianReach` de
Config:1092 (envelope por TIEMPO: `maxPedestrianSpeed × elapsed`); unificarlas entre sí exigiría su
propia demostración — no se afirma aquí.

### 3-c · Step-budget walked-vs-rode (EvalHC ↔ EvalSNC). **SE SOSTIENE como predicado compartido; NO como fusión de los dos use cases**

Ambos KDoc se declaran «mirror of» (EvalHC:20-24, 93; 01:136-138): el mismo presupuesto
`pasosRequeridos = distance(sealOrigin→fix)/stride × walkedStepFraction` con los mismos guards de
salud del contador. La política única es una función `walkedVsRode(seal, fix, stepsDelta,
counterHealth)` en `domain/detection/` cuyas cláusulas son exactamente los guards con incidente:
- **DET-STEP-BUDGET-ORIGIN-001** (22-07 Glorieta): el origen del budget es `sealPoint`, jamás el pin — parámetro del predicado.
- **DET-TRIP-WITNESS-001** (30-07, sello de 16 h): `sealAge > max` ⇒ ininterpretable — guard previo del predicado (y su precedencia sobre mute/frozen queda documentada en el predicado, no repartida).
- **DET-FROZEN-COUNTER-001** (25-07 Jerez): cumulativo que reporta menos que lo presenciado = congelado — guard del predicado.
- **DET-WALK-FLOOR-001** (26-07): distancia ≤ ruido de accuracy ⇒ el conteo es cuantización — floor del predicado.
Los cuatro escenarios quedan cubiertos porque son CLÁUSULAS del mismo cálculo, no reglas paralelas;
el espejo de EvalSNC consume el mismo predicado con sus insumos persistidos (ANCHOR-PERSIST-001).
**Lo que NO se sostiene:** fusionar `EvaluateHonestCloseUseCase` y `EvaluateSafetyNetCheckUseCase`
en un use case — son dos VEREDICTOS distintos (cierre de un abort inmediato con 9 razones tipadas
vs acción del reconcile CureGeofence/Dispatch/Prompt/None), con entradas y momentos distintos.
Predicado único, dos dueños de veredicto.

### 3-d · `evidencia ≥ sessionStart` ×4 → una función. **SE SOSTIENE (trivial y ya demostrado)**

El mismo predicado de una línea en `DetectParkingDepartureUseCase.kt:129-135`,
`VerifyDepartureEvidenceUseCase.kt:72-73`, `EvaluateSafetyNetCheckUseCase.kt:158-160` y
`EvaluateArEnterArmUseCase.kt:81-83` (verificado por el parcial G). El escenario de campo
(2026-07-08 Redmi: el ENTER del viaje de ida "verificó" un EXIT andando) queda cubierto por
construcción: los 4 sitios implementan la comparación idéntica, así que extraerla a
`domain/detection/` (patrón `SentryWakeCooldown`) no cambia ningún comportamiento y elimina el
riesgo de divergencia. Es además la cláusula (1) del verificador 3-a.

### 3-e · «AnchorTrust» — 6 flags + 6 snapshots del ancla → un dueño de estado. **SE SOSTIENE (como dueño de estado + predicados; los veredictos consumidores siguen aparte)**

Hoy la pregunta «¿cuánto me creo que el coche está aquí?» está repartida en ~12 campos del state
(02 §1: `bestStopLocation`, `anchorCapturedAtStop`, `anchorFrozen`, `walkFixesSinceDriving`,
`kinematicEgressFixes`, `egressOriginFix/StepCount`, los 5 `anchor*AtCapture` + `anchorWalkInSpanMeters`,
`stopEnteredAfterGapMs`) con la condición de rebind repetida LITERALMENTE 5 veces (CPD:2165-2194) y
el nacimiento del egress duplicado en 2 sabores con gates ligeramente distintos (02 §5 máquina 6).

**Política única:** un objeto `AnchorTrust` sellado por el rebind (un solo snapshot atómico) con
predicados `pinned` (lock ∨ frozen), taints (`walkEntered`, `gapEntered` con magnitud), `egressBornAtAnchor`,
`refinedParkLocation` y `doubt` (radio de zona). Demostración por tag:
- **ANCHOR-LOCK-001** (supermercado/Gavia): `locked = steps ≥ 8` es campo del dueño; `pinnedToOtherStop` es su regla de re-captura.
- **DET-ANCHOR-FREEZE-001** (11-07 contador mudo; Av. Sanlúcar): `frozen` + maduración son el segundo camino a `pinned`; el mismo dueño exporta el veto de reposición.
- **DET-SHORT-TRIP-FREEZE-001** (Durango→Glorieta): deja de ser tag-guard y pasa a PARÁMETRO (`restProvenByFixes`) de la maduración — exactamente lo que ya es.
- **LOC-001 / PARKING-001 / LOC-002**: reglas de captura/clear del mismo dueño (ventana inicial, burst de reposición, gate de accuracy del clear) — sus 3 escenarios (fixes tardíos, maniobra a la plaza, alucinación GPS) son casos del MISMO ciclo de vida.
- **DET-GAP-ANCHOR-001/-ZONE-001** (29-07; 17-08): `gapMs` con magnitud es campo del dueño; `doubt = gap × pedestrian` es su método de zona.
- **DET-WALK-ENTERED-ANCHOR-ZONE-001** (16-08): `walkInSpan` (habla con contador mudo) es campo del dueño; mismo método de zona.
- **DET-CREDIBLE-DRIVE-001 (taint)** (Camelias/Galeote): `walkEntered` con exención de maniobra es predicado del dueño (hoy CPD:1801-1807); sus otras 2 piezas (sustained departure, corroborated hop) son física DriveProof con destino en este dueño.
- **DET-ANCHOR-EGRESS-001** (Enamorados): `egressBirth` + `egressBornAtAnchor` + Rule A son del dueño; unificar aquí los DOS sabores del birth elimina la divergencia silenciosa señalada — con la condición de re-validar contra las trazas Enamorados/CameliasOppo.
- **DET-CONFIRM-ANCHOR-001** (11-08): consumidor — el fallback del "Sí" lejano pregunta al dueño por el witnessed stop.
- La familia geométrica 3-b son los predicados de escape del mismo dueño.
Beneficio demostrado por 02: la condición de rebind ×5 y los resets en cascada de 9 campos
(`shouldClearBestStop`) colapsan en el ciclo de vida del objeto. Los veredictos que lo consumen
(EvalPD, EvalUS, rama user-confirm) siguen siendo casos de uso propios — el dueño solo responde
la pregunta de confianza.

### 3-f · Otros solapes registrados (NO elevados a política — candidatos, pendientes de demostración o deliberados)
1. **Dos escaleras de degradación pin→zona→ask** (EvalHC:316-321 ↔ `zoneOrAsk` EvalUS:304-314): misma forma, helpers privados distintos. Candidato razonable, sin demostración caso-por-caso escrita → pendiente.
2. **BT-suprime-Coordinator dicho en 3 tags** (CONNECTED-NOT-PAIRED decide, STRATEGY-GATE ejecuta, ARCH-MONITORING-002 explica): resuelto como dominancia (§4), no exige política nueva.
3. **Señal stale-pending con 3 consumidores** (NEVER-SILENT → nudge, FGS-REAPER, NUDGE-PERSIST): correcto por diseño (una señal, tres consecuencias); barrer juntos ante cualquier cambio del heartbeat.
4. **Frontera espacial `radio + accuracy` ×3** (ArmAtCar · guard anti-loop AR-REARM · SessionSupersede, citado en SessionSupersede.kt:16): una geometría, tres tags — cambiar el cálculo del radio barre a los tres.
5. **Asimetría «identidad BT = solo MAC» en 3 capas** (BT-OWNERSHIP atribución · BT-IDENTITY-GATE release · PARK-DELETE-NO-DECLARE): tres veredictos correctos per doctrina; falta un fichero doctrinal único que enuncie el axioma.
6. **Rutas — dobles implementaciones deliberadas**: lookup del pin previo ×2 (ConfirmParking ↔ HomeTripController), freshness de ruta ×2 (constantes independientes, NO VERIFICADO que coincidan), ventana de plausibilidad 15–5000 m ×3 (espejos mantenidos a mano), gate "esto no es un drive" ×2 (`MIN_ROUTE_EXTENT_METERS` vs "trivial route" del EnrichWorker, NO VERIFICADO mismo umbral). Barridos obligatorios, política única no demostrada.
7. **Guard `thisJob === detectionJob` ×2** (CDS:1315 ↔ BluetoothDetectionService:170, "ported/mirrors"): física idéntica en dos servicios.
8. **Espejo triple de `sync()`** (sig-motion/heartbeat, ParkingSafetyNetWorker:138/142 + PaparcarApp + teardown): duplicación deliberada y documentada; el cooldown vive en el monitor precisamente por eso.
9. **`creepWindow` (local) vs `recentFixes` (estado)**: dos rings de fixes recientes con podas distintas (02 §7.5) — candidato menor.
10. **Taint walk-entered con dos dueños de etiqueta** (lo crea CREDIBLE-DRIVE, lo resuelve WALK-ENTERED-ZONE): coherente; cambio en uno debe barrer el otro (queda absorbido si 3-e se ejecuta).

---

## 4 · Tags inalcanzables o dominados (justificación de flujo íntegra)

Distinción: **[borrable tras ticket]** = el guard/etiqueta separado puede retirarse en un ticket
futuro sin perder protección (su rationale se muda al dueño) · **[rationale-como-comentario]** = la
condición ya no muerde o está dominada, pero el comentario carga el porqué y debe quedarse.

### Inalcanzables (demostrados)
1. **DET-FROZEN-COUNTER-001, rama `REASON_SESSION_MEASURED_DRIVING`** (EvalHC:206-222) —
   [rationale-como-comentario]. Su propio KDoc (:146-150) la declara *«defensively unreachable»*
   para los DOS únicos outcomes que hoy disparan la escalera: `aborted_false_enter` exige
   `¬hasEverReachedDrivingSpeed` (rama 2 del collect, CPD:928) y `aborted_no_movement` también
   (rama 3, CPD:950); ambos abortan PRECISAMENTE porque nunca se midió conducción, luego
   `lastFinishedMaxSpeedMps ≥ minimumTripSpeedMps` no puede darse desde el gate del servicio
   (CDS:818). Es defensa deliberada + telemetría para cualquier caller futuro con otro outcome —
   NO código muerto a borrar.
2. **BUG-DETECT-310503 (condición original)** — [rationale-como-comentario]. El fix documentado
   (`if (signals.activityStill && …) score += fastPathAccuracyBonus`, PARKING-DETECTION.md:896-903)
   NO existe en CPC: Config:25 declara «`fastPathAccuracyBonus` removed: it was gated on the STILL
   signal» y la señal `activityStill` fue purgada de `ParkingSignals` (DET-SOLID-001 C1). La
   condición original es inalcanzable; lo que el tag nombra hoy es el techo aritmético
   `0.50+0.15=0.65 < 0.75` (Medium nunca abre CANDIDATE), que SÍ está fijado por test por nombre.
   Como guard puntual es sedimento; como nombre del invariante es válido.
3. **BUG-FGS-001 (clase de crash original)** — [rationale-como-comentario]. El código crashante
   (nuestro `startForegroundService()` desde `ActivityTransitionReceiver`) fue ELIMINADO el
   2026-05-25: hoy ningún receiver propio arranca FGS, así que la clase original es inalcanzable.
   El tag sobrevive como restricción de diseño citada en 8+ sitios y es la razón documental de
   DET-G-01, DET-EXIT-WITNESS-001 y la lane de evidencia AR. El único guard ejecutable restante es
   la degradación del promote (CDS:224-234). No borrar.
4. **Rama null-intent de `BluetoothDetectionService` (:92-96, BT-BUG-100)** — fuera del censo de
   132 (rama, no tag): el servicio es START_NOT_STICKY (el framework no re-entrega intents null) y
   `exported="false"` impide a terceros enviar intents sin action. Guard defensivo puro.

### Dominados (con orden/flujo demostrado)
1. **DET-B-01 dominado por DET-INTAKE-001** — [borrable tras ticket]. La rama `else` (CDS:290) no
   contiene mecanismo propio — solo loguea; el teardown que el tag reclama lo ejecuta
   `resolveIdleEpilogue(...)` (:292), que corre tras TODO comando por diseño del intake. Si
   DET-INTAKE-001 se preserva, DET-B-01 se preserva gratis; como invariante separado solo aporta el
   log. Cualquier reordenación que sacara el epílogo del camino común rompería a la vez los otros
   10 handlers — no hay ventana en la que DET-B-01 proteja algo que el intake no proteja.
2. **ARCH-MONITORING-002 dominado en runtime** — [rationale-como-comentario]. No existe ninguna
   rama de código propia: `strategyFor` devuelve BLUETOOTH por la condición de
   DET-BT-CONNECTED-NOT-PAIRED-001 (ParkingStrategyResolver:83-95) y la supresión ocurre en
   `coordinatorMayArm` (DET-STRATEGY-GATE-001, :63-64); el comentario :31-36 no guarda condición
   adicional. NO redundante como documentación: explica la decisión de producto (suprimir aunque el
   primario no tenga BT) y lo cita `VehicleRegistrationViewModel.kt:274`.
3. **Rama DISCONNECT-different-car de DET-BT-WRONG-CAR-ABORT-001** — [rationale-como-comentario;
   borrable solo con ticket]. En `EvaluateBtArbitrationUseCase.kt:85-97`, la rama
   `isDifferentOwnCar ∧ DISCONNECT → SupersedeWithBluetooth` (:87) produce EXACTAMENTE el mismo
   veredicto que el fallthrough genérico `DISCONNECT → SupersedeWithBluetooth` (:97): para todo
   input con `event==DISCONNECT ∧ coordinatorRunning` ambas rutas devuelven el mismo objeto (la
   distinción solo existe para CONNECT). Dominada en comportamiento; documenta un rationale
   distinto (misatribución vs salida propia) y protege contra divergencia futura.
4. **BUG-COORD-115 dominado parcial por la rama hold** — [rationale-como-comentario + KDoc a
   corregir]. Cuando existe `pendingConfirm` (todo confirm automático con `confirmHoldMs`=2 min
   pasa por ahí), el "Sí" se resuelve en la rama 1b (CPD:887-898) con `return@collect` — la rama 5
   que porta el tag (CPD:1050) NUNCA se alcanza en ese flujo; solo decide sin hold en curso (Sí
   antes del primer confirm tentativo, o `confirmHoldMs=0`). El invariante semántico («un tap
   gana») sobrevive duplicado en dos ramas; el KDoc de precedencia (CPD:81-93) documenta un orden
   que ya no es real (confirmado en 02 §4). Candidato a UNIFICAR, no a borrar.
5. **LOC-001 dominado parcial por DET-ANCHOR-FREEZE-001** — [rationale-como-comentario]. Su regla
   original («solo la ventana inicial de 30 s captura») fue relajada: `sameStopPreEgress`
   (CPD:2109, `anchorCapturedAtStop == startedAt ∧ stepCount == 0`) permite refinar TODO el stop
   mientras no haya pasos (field 11-07 Av. Sanlúcar), y `pinnedToOtherStop` (CPD:2101) veta stops
   posteriores con más autoridad. El corte a 30 s solo decide ya en la franja «mismo stop, tras el
   primer paso contado» — franja real (el paseante que vuelve al mismo stop), luego NO redundante,
   pero dominado en el resto de su dominio original.
6. **DET-SHORT-TRIP-FREEZE-001 = predicado sin veredicto** — [borrable tras ticket, absorbiéndolo].
   Es una cláusula OR (`restProvenByFixes`) dentro de `matured` de DET-ANCHOR-FREEZE-001
   (CPD:2125-2134). Nunca dispara «antes» ni «después» de FREEZE: ES FREEZE. Candidato natural a
   parámetro en la ficha del futuro dueño AnchorTrust (3-e), no guard aparte.
7. **VEHICLE-SYNC-001 (mitad de atribución) dominado por VEH-ACTIVE-FENCE-001** —
   [rationale-como-comentario]. El fallback «null → vehículo activo» (ConfirmParkingUseCase:131-141)
   solo es la última palabra cuando NO hay `nominatingVehicleId`; cuando la valla nominó,
   CPD:487-490 fuerza el vehicleId del nominador antes del confirm. La rama explícita (BT) sigue
   siendo suya. Es el suelo del fallback — un refactor de atribución debe tratarlos como UNA política.
8. **BUG-GARAGE-COLA-001 recortado** — [rationale-como-comentario]. Su mecanismo («8 pasos
   confirman») fue endurecido dos veces (DET-A: ∧ egress; DET-C-01: egress para todo); la mitad
   «pasos como señal canónica» vive repartida entre DET-A/DET-C-01/DET-STEP-SPEED-GATE. Le queda en
   propiedad el DISCARD del candidate expirado (CPD:1729) y el linaje del `StepDetectorSource`.
9. **BUG-SERVICE-109 enmendado por DET-AUDIT-002 T8** — [KDoc a corregir]. «Reset SIEMPRE en el
   finally» ya no es literal: T8 (CPD:1305) lo condiciona a la propiedad de `currentSessionId`
   (una sesión superseded no toca el estado del sucesor). No es regresión (el sucesor resetea en SU
   entrada), pero el KDoc :95-97 describe el contrato pre-T8. El invariante lo garantizan 109+T8 juntos.
10. **DET-TRIP-WITNESS-001 domina el ORDEN de la escalera** (no redundancia): con sello viejo,
    mute/frozen/no_seal_origin jamás se evalúan (EvalHC:242 < 251 < 262 < 275). Con sello fresco los
    tres corren y cada uno tiene su incidente (25-07, 22-07, 26-07). Precedencia documentable para
    el predicado 3-c.
11. **DET-UNVERIFIED-ARM-DRIVE-PROOF-001 (etiqueta)** — [borrable tras ticket, fusionando doc]: dos
    tags sobre una física (con DET-SHORT-HOP-PROOF-001); el tag solo existe en tests.
12. **REFACTOR-200 (etiqueta)** — [borrable tras ticket, renombrable]: no guarda condición; nombra
    la arquitectura de fases (viva). Riesgo real de grep por colisión de nombre con BT-REFACTOR-200.

Explícitamente NO dominados pese a sospecha: **PARKING-001** (sigue alcanzable: ancla capturada sin
lock ni freeze + 3 fixes ≥1.7 m/s acc ≤15 → clear; exactamente su escenario original de maniobra),
**BUG-DETECT-310502** (única vía de prompt en paradas que nunca alcanzan High) y **DET-B-02** (rama
pre-intake). Ningún guard del cluster G resultó inalcanzable (se examinaron `Candidate→NoOp` del
árbitro BT, `CureGeofence` fresco y `PromptStillParked` por `userPresent` — todos alcanzables).

---

## 5 · Colisiones de doctrina ABIERTAS (decisiones de producto — no se resuelven aquí)

1. **DET-SENTRY-COOLDOWN-001 vs «todo trigger dispara SIEMPRE»** — la colisión real y documentada.
   Field 17/18-08 Chema→Balsa: el EXIT del Oppo llegó 11 h tarde Y el cooldown del sentry cubría la
   ventana del viaje → FN; el único backstop vivo fue DET-SAFETY-NET-001, que PREGUNTÓ (por diseño;
   no se abrió DET-STALE-PIN-FAR-WAKE-NUDGE-001). La división actual es: el cooldown solo duerme el
   nominador significant-motion; EXIT, AR y net periódico quedan inmunes. Decisión de producto
   pendiente: ¿es aceptable que durante cooldown+EXIT-retenido el resultado sea una pregunta en vez
   de un pin? ⏳ validación de campo; cualquier refactor debe conservar EXACTAMENTE esta división.
2. **Watchdog del hold (DET-AUDIT-002 T7) vs DET-CONFIRM-FRESHNESS-001** — el watchdog finaliza un
   hold hambriento de fixes por RELOJ **sin** la re-validación de frescura que la rama 1a sí hace
   (asimetría documentada en CPD:691-693: sin fixes no hay con qué re-validar). Consecuencia
   posible: pin stale confirmado tras 2 min de silencio GPS. Hoy deliberado; decisión de producto si
   ese camino debería degradar a prompt en vez de confirmar.
3. **(Declarada, vigilar) DET-RECONCILE-001 rama 7 (SaveExact reliability 0.5) vs «mejor FN que
   FP»** — la asimetría de costes se invierte a propósito en el response-timeout (perder ≪ corregir
   con un tap), hoy acotada por el veredicto de 7 vías de EvalUS (human-powered → vehicular-egress →
   unpinned → zonas → ask). No abierta, pero toda política 3-a/3-e debe conservar esa precedencia.
4. **(Declarada, resuelta con matiz) DET-BT-TIMEOUT-SAVE-001 vs walk-away comunitario** — guardar
   la sesión PROPIA sin walk-away con fiabilidad reducida y path forense propio; nada comunitario se
   publica. Mantener la distinción sesión-propia/spot-comunitario en cualquier refactor BT.

---

## 6 · Cifras finales

| Métrica | Valor |
|---|---|
| Filas de origen en los 4 parciales | 133 (27 F1 + 27 F2 + 38 G + 41 H) |
| Tags únicos consolidados | **132** (BUG-SERVICE-109 fusionado F2+H) |
| En producción / solo-test | 129 / 3 (DET-B-03, DET-D-04, DET-UNVERIFIED-ARM-DRIVE-PROOF-001) |
| Guard-código vivo | **124** |
| Solo-comentario / doctrinal / solo-test | 7 (DET-D-01, DET-D-02, DET-D-04, DET-B-03, REFACTOR-200, DET-VERDICT-NOT-PREDICATE-001, DET-UNVERIFIED-ARM-DRIVE-PROOF-001) |
| Frontera guard/histórico | 1 (BUG-DETECT-310503) |
| Con test (tras correcciones a 05) | **90 de 129 (69.8 %)**; sin test 39 — 24 de ellos en androidMain (2 ficheros / 13 tests Robolectric para toda la capa) |
| Inalcanzables demostrados | 4 (2 ramas defensivas: FROZEN-COUNTER measured_driving, BT null-intent; 2 condiciones originales purgadas: BUG-DETECT-310503, clase de crash de BUG-FGS-001) — ninguno borrable sin conservar rationale |
| Dominados | 3 totales/runtime (DET-B-01, ARCH-MONITORING-002, rama DISCONNECT-different-car) + 4 parciales (LOC-001, BUG-COORD-115, VEHICLE-SYNC-001, BUG-GARAGE-COLA-001) + 1 predicado sin veredicto (DET-SHORT-TRIP-FREEZE-001) + 2 etiquetas fusionables (DET-UNVERIFIED-ARM-DRIVE-PROOF-001, REFACTOR-200) |
| Grupos de clase de fallo | **15** |
| Políticas únicas candidatas evaluadas | 5 — **las 5 se sostienen**, 2 con alcance acotado: 3-a (verificador único, sin fusionar veredictos) y 3-c (predicado compartido, sin fusionar los 2 use cases); +10 solapes registrados NO elevados a política (3-f) |
| Colisiones de doctrina abiertas | 2 (sentry-cooldown vs todo-trigger; watchdog T7 sin re-validación) + 2 declaradas a vigilar |

---

## Addendum 2026-08-19 — tags posteriores a la línea-base

> Dos fixes de campo entraron en master DESPUÉS de la línea-base de esta síntesis (`fbc83847`):
> `08b53548` (DET-MOTOR-PROOF-001) y `bf92070c` (DET-UNWITNESSED-DISPLACEMENT-001). Este addendum
> los incorpora SIN reescribir la tabla vieja; las líneas citadas aquí son las del árbol actual
> (CPD ahora **2677 líneas** — los line-refs del cuerpo van desplazados en el tramo del CPD
> posterior a ~:350, ver addendum del doc 08).
>
> **Censo actualizado** (corrige §6 sin reescribirlo): tags vigentes **133 → 135** · en producción
> **129 → 131** (solo-test sigue 3) · guard-código vivo **124 → 126** · grupos de clase de fallo
> siguen 15 (G3 pasa de 5 a 6 miembros; G6 de 5 a 6).

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-MOTOR-PROOF-001** | 2026-08-18 20:32 Oppo sesión `1787077943062` — FP bici: paseo de 6,1 min plantó el pin `aa0d7b1d` EN SILENCIO (`steps+egress`, rel 0.9) con el Kamiq a ~540 m; AR mudo (0 eventos de 316 — AR no clasifica trayectos cortos) y el pico 18,1 km/h en 2/124 fixes (~6 s en banda) pasó el bar por 0,1 km/h (`08b53548`) · backlog | «Conducción medida» se probaba con un PICO — una bici lo produce sin esfuerzo (la del 16-08 picó 38 km/h); y sin AR no había 2ª fuente de `humanPowered` | (a) `sessionSawDriving = sustainedDrivingMs ≥ sustainedDriveProofMs` (30 s): tiempo SOSTENIDO en banda de conducción creíble — huecos entre fixes in-band SUCESIVOS, acreditados solo si el hueco ≤ `driveProofWindowMaxMs` (60 s, la misma ventana que el drive-proof ya confía: Gavia es UN hop de 36 s; Enamorados parte la racha en fragmentos); publicada con la promoción de `maxSpeedMps`: **cero hasta `driveProven`** (`provenDrivingBandMs`). (b) 2ª fuente CINEMÁTICA de `isHumanPoweredRide`: `fastMotionStepEvents ≥ 12 ∧ fastMotionStepFixes ≥ 2` — steps concurrentes (frescura ≤ 10 s) con fix creíble por ENCIMA del techo peatonal (`egressStepMaxSpeedMps`) en fixes distintos = firma de pedaleo; juzgada ANTES que los sellos AR (medir gana a recordar; sin supersede por embarque — coste aceptado: bici→coche degrada a Prompt). Ambos degradan a Prompt/nudge, nunca descartan | CPD:354-396 (campos + `provenDrivingBandMs`), 679-716 (juez de cadencia en el stepJob), 870-910 (reloj de banda; promoción `maxSpeedMps` ahora :895), 1354/1800 (input al evaluador), 1452-1456 (args a humanPowered); HumanPoweredRide.kt:33-45, 69-76; EvalPD:48-55, 150-154; Config:502-527, 969-984 | `drivingBandMs`, `lastBandFixTimestampMs`, `lastFixSeenAtMs`, `lastFixCredible`, `fastMotionStepEvents`, `fastMotionStepFixes`, `fastMotionCreditedFixAtMs` | ✅ HumanPoweredRideTest:100-146 (cadencia con AR mudo · ráfagas 1-3 no vetan · un solo fix no veta · cadencia sobrevive al boarding AR); EvaluateParkingDecisionUseCaseTest:234-300 (burst→Prompt · hop sostenido→Confirmed · kinemático vetado por burst · humanPowered con banda sostenida→Prompt); CPDTest:2987 (helper `emitCorroboratedDrive` recalibrado a 8 fixes×5 s = 35 s en banda); replays Gavia/Enamorados intactos | Redefine el `sessionSawDriving` que gatean DET-SOLID-001/DET-UNVERIFIED-CONFIRM-001 (EvalPD:213) y que exige DET-KINEMATIC-EGRESS-001 (EvalPD:160); `maxSpeedMps` (DET-DRIVE-PROOF-001) queda como TECHO del mismatch (BUG-SCOOTER-001), nunca como prueba de motor; comparte `egressStepMaxSpeedMps` con DET-STEP-SPEED-GATE-001 (el mismo techo, gate de conteo vs firma de cadencia); es la 2ª fuente del predicado de DET-BIKE-NOT-A-CAR-001 (dos fuentes, un veto). `measuredDriving` de EvalUS deliberadamente NO pasa a sostenido (regresión DET-GAP-ANCHOR-ZONE: cubierto porque el veto de cadencia corre PRIMERO en ese veredicto) |
| **DET-UNWITNESSED-DISPLACEMENT-001** | 2026-08-19 03:26/03:59 Oppo DORMIDO en casa — 2 FP encadenados del honest-close: multipath indoor teleportó el fix ~950 m con acc 7 m; `trip_proven` plantó el pin `e34e70ad` en el espejismo (Cantarranas, sesión `1787109972361`), su valla vio el móvil 949 m fuera desde el segundo cero y la cascada plantó `70a0f146` en la CASA del usuario (seal envenenado: FP1 selló en el punto basura) (`bf92070c`) · backlog | La escalera confiaba en el abort fix como «dónde está el cuerpo»: el fix de UN solo wake, sin testigo independiente, probaba un viaje que nadie presenció (velocidad implícita ~107 km/h entre dos observaciones ESTACIONARIAS separadas 32 s) | Gate de coherencia espacio-temporal, tras la conducción medida (que ES tránsito presenciado, EvalHC:240-251) y ANTES de toda inferencia: `d(lastWitnessedFix, abortFix) > acc_testigo + acc_abort + edad_s × honestCloseMaxImpliedTravelSpeedMps` (15 m/s puerta a puerta) ⇒ `KeepSilent(unwitnessed_displacement)` — cuando dos testigos se contradicen físicamente NINGÚN fix es pin-grade. Testigo = UN slot persistido en disco (end-fix del wake anterior / check del safety-net, latest wins), estampado en el epílogo DESPUÉS de que el honest-close consumiera el anterior (una sesión nunca atestigua su propio abort); skew negativo ⇒ sin testigo, jamás refutación falsa. La fórmula se AUTO-limita con la edad (Camelias/D2 con horas pasan intactos — sin cap de frescura). FP2 muere como cascada: sin pin en el espejismo no hay valla | EvalHC:62-74 (verdict + `REASON_UNWITNESSED_DISPLACEMENT`), 151-166 (doctrina), 267-286 (gate), 374-380 (trip_proven estampa distancia/edad para auditar el umbral); RunHC:62-90 (pass-through); CDS:838-849 (lectura + edad a now), 887-888 (evento), 901-931 (`readLastWitnessedFix`/`stampLastWitnessedFix`), 1339-1343 (orden del epílogo); PSNW:196-203 (refresco en cada check), 773-782 (keys); Config:566-574, 995-997; DetectionEvent.kt:149-153; DetectionEventDto.kt:94-95, 166-169 (`witnessAgeMs` viaja en la columna `sessionAgeMs`, reuso patrón Sentry) | Ninguno del state — slot durable `last_witnessed_pos/acc/at` (prefs del safety-net) + args puros `lastWitnessedFix`/`witnessAgeMs` | ✅ EvaluateHonestCloseUseCaseTest:416 (teleport con testigo de 32 s → silencio), :445 (testigo de horas → transparente, protege Camelias/D2), :468/:486 (frontera del techo: justo por encima refuta, por debajo pasa); RunHonestCloseUseCaseTest:218 (replay FP1 end-to-end — el assert «nada guardado» ES el assert de la cascada FP2); regresión Camelias/D2/Glorieta/frozen/walk-floor verdes | Razón nº 10 de la escalera DET-HONEST-CLOSE-001; PRECEDE a `stale_seal`/`mute`/`frozen`/`no_seal_origin` (la tríada del step-budget) — es el flanco «exigir tránsito presenciado» que DET-TRIP-WITNESS-001 dejó diferido, resuelto SIN matar Camelias (el discriminador es el testigo independiente, no «la sesión midió»). El resello del CureGeofence (DET-CURE-FRESH-001, PSNW:260) es lo que mantenía FRESCO el seal envenenado — el gate añade el testigo que ese resello no puede lavar. NO resuelve el bug #2 (techo del radio): refuta por velocidad implícita, no por accuracy — las accuracies SUMAN al allowance. EvalSNC exento con razón (exige evidencia de VEHÍCULO además del fix; el testigo ya se refresca por si el campo dice lo contrario) |

**Adjudicación a los censos del doc (sin reescribirlos):**
- **§2 clases de fallo:** DET-MOTOR-PROOF-001 → **G3** «Movimiento no vehicular leído como coche»
  (6º miembro; el eje del grupo es literalmente su enunciado: solo enciende «midió conducción» un
  track que un peatón/bici/mirage no puede producir). DET-UNWITNESSED-DISPLACEMENT-001 → **G6**
  «Liberación falsa del pin correcto — el cierre/reconcile inventa una salida» (6º miembro: FP1
  liberó el pin real de La Bermeja por reemplazo; es un par de la familia FROZEN-COUNTER /
  STEP-BUDGET-ORIGIN / TRIP-WITNESS / WALK-FLOOR — otro guard de interpretabilidad de la MISMA
  escalera). *Cruce:* con G3 (el espejismo GPS es el productor del fix mentiroso).
- **§3 políticas:** el cambio (a) de MOTOR-PROOF es una cláusula MÁS del verificador **DriveProof
  (3-a)** — «movimiento medido que un peatón no puede producir» gana la dimensión TIEMPO sostenido;
  su cambio (b) vive en el predicado `humanPowered` (patrón `HumanPoweredRide.kt`, ya top-level).
  UNWITNESSED-DISPLACEMENT pertenece a la familia **3-c** como guard previo del walked-vs-rode
  (mismo patrón que TRIP-WITNESS: precondición de interpretabilidad), pero HOY solo EvalHC lo
  consume — EvalSNC queda exento con razón escrita en el backlog; si 3-c se codifica, el testigo
  entra como parámetro opcional del predicado, no como fusión.
- **§5 colisiones:** ninguna nueva; ambos fixes degradan a Prompt/nudge (asimetría respetada).

---

## Addendum 2026-08-20 — DET-STOP-BUTTON-001

> Un commit más entró en master tras el addendum anterior: `1d8f7264` (DET-STOP-BUTTON-001,
> feature — no fix de campo). Mismo patrón: fila nueva SIN reescribir la tabla vieja; line-refs
> del árbol actual (CPD ahora **2699 líneas**, CDS **1656** — deriva detallada en el addendum del
> doc 08).
>
> **Censo actualizado** (corrige la cifra del addendum 19-08 sin reescribirlo): tags vigentes
> **135 → 136** · en producción **131 → 132** (solo-test sigue 3) · guard-código vivo
> **126 → 127**.

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-STOP-BUTTON-001** | No un FP/FN de campo sino un agujero de AUTORIDAD visto en uso real: con una sesión viva (embarcar como pasajero, subirse a un bus con AR armado) el usuario no tenía salida — las únicas palancas eran el toggle global de Ajustes (mata la feature entera, no este viaje) o esperar 4-15 min a que la sesión resolviera, prompt incluido (`1d8f7264`) · backlog `det-stop-button-001.md` | Dos mentiras posibles del botón: (a) sin drop del confirm retenido, el cinturón del finally [DET-AUDIT-002 T7] vería `pendingConfirm != null && !completed` y finalizaría EXACTAMENTE el pin que el usuario acaba de rechazar (CPD:1409-1413); (b) sin quiet period, la misma caminata al asiento del pasajero re-dispara el AR ENTER segundos después y «la detección vuelve sola» | *El veto del usuario es la máxima autoridad: un stop es un FN SOLICITADO.* (1) `ACTION_USER_STOP` en el intake, DISTINTO del interno `ACTION_STOP_TRACKING`: con sesión viva → `onUserStoppedDetection()` estampa el outcome terminal `stopped_by_user`, descarta el confirm retenido y resetea el estado ANTES de que la cancelación llegue al finally; un tap sin sesión viva es stale (dismiss de la notificación, NO abre quiet period). (2) Quiet period de 15 min (`userStopQuietPeriodMs`) aplicado en el ÚNICO embudo de arm (`startParkingDetection`): `isArmSuppressedByUserStop` — MANUAL nunca se suprime y LIMPIA el stamp («Estoy conduciendo» es el mismo usuario retractándose); stamp del futuro (skew de reloj) ⇒ 0, jamás mudez perpetua. Solo el ARM duerme: un EXIT sigue liberando la plaza (aguas arriba del gate), el safety net sigue reconciliando, el carril BT intacto. (3) Todo arm suprimido deja traza remota `ARM_SUPPRESSED_USER_STOP` — no es rama muda | CDS:284 (intake), 375-391 (`handleUserStop`; guard stale-tap :376-380, stamp :383), 1248-1268 (gate de arm: lectura :1254, supresión :1257, limpieza :1267), 1474-1494 (traza remota), 1600/1606 (constantes); CPD:1489-1508 (`onUserStoppedDetection`); `UserStopQuietPeriod.kt:31-54` [KMP, política pura top-level]; `UserStopStore.kt:19-38` [Android]; SentryWakeCooldown.kt:28-32 (`STOPPED_BY_USER`); Config:558 (default), 1035-1037 (require); entrypoints: AppNotificationManagerImpl.kt:298-309 (acción en la notificación FGS) → ParkingConfirmationReceiver.kt:54, 58-61; HomeIntent.kt:61 → HomeViewModel.kt:612-615 → ManualParkingDetectionImpl.kt:37-42 (`stopByUser`, comando propio — NO el `stop()` de DET-MANUAL-CANCEL-001) | Ninguno del state del CPD — stamp durable `user_stopped_at` (prefs del safety-net, un solo slot: parar dos veces re-estampa) + `sessionOutcome = "stopped_by_user"` | ✅ UserStopQuietPeriodTest:22-84 (7 tests: suprime todo trigger automático · MANUAL jamás · lapso · sin stamp · stamp futuro · restante · 0 = deshabilitado); CPDTest:3008 (outcome terminal), :3032 (NO planta el confirm retenido); SentryWakeCooldownTest:76 (el stop RESETEA el streak); 1247 tests totales | TENSIÓN deliberada con «todo trigger dispara siempre» (nota §5 abajo). Familia quiet-period con DET-SENTRY-COOLDOWN-001: misma forma (política pura commonMain aplicada en el gate único de arm del service), causas distintas (sistema vs usuario), sin pisarse. `stopped_by_user` deliberadamente NO es un abort andante: cae en el reset de `nextSentryWakeAbortStreak` (SentryWakeCooldown.kt:41-50) — tercera conducta de membership por convención de nombre, alimenta el bug #3 del doc 11. El drop del hold tuvo que ESQUIVAR a DET-AUDIT-002 T7 — precedente anotado en la propuesta 1 del 09 |

**Adjudicación a los censos del doc (sin reescribirlos):**
- **§2 clases de fallo:** → **G13** «UX deshonesta / estado visible incoherente» (14º miembro): el
  eje del tag es que el botón no mienta — ni plantando el pin rechazado ni «volviendo solo».
  *Cruce:* con G15 (la mitad quiet-period es doctrina de autoridad, no un guard geométrico).
  [Adjudicación de este addendum, NO VERIFICADA con el user.]
- **§3 políticas:** no toca las 5 candidatas. Nace directamente en el patrón
  predicado-compartido-top-level (`UserStopQuietPeriod.kt`, hermana de `SentryWakeCooldown.kt`) —
  segunda instancia de la familia «quiet period como política pura + gate único en el service»; si
  algún día se eleva a política única («ArmGatePolicy»), estas dos son sus miembros.
- **§5 colisiones:** NO se abre colisión nueva, y la tensión merece quedar razonada: la doctrina
  «todo trigger dispara siempre» existe para que el SISTEMA nunca pierda un parking que el usuario
  quería (por eso el sentry-cooldown, ordenado por el sistema, sí es la colisión abierta §5.1).
  Aquí la supresión la ordena el USUARIO sobre su propio viaje: la doctrina cede ante la autoridad
  superior a la que sirve — un FN solicitado no es un parking perdido. Además la rama no es muda:
  cada arm suprimido estampa `ARM_SUPPRESSED_USER_STOP` en remoto, así que un field-test puede
  distinguir «no armó porque lo paraste» de «el OEM se comió el trigger».

---

## Addendum 2026-08-21 — cuatro commits de detección (`26aec131` · `16d3ea69` · `ef03cea6` · `1c292ac8`)

> Cuatro commits entraron en master tras el addendum del 20-08 (línea-base anterior `dd200b84`,
> CPD 2699 líneas). **Nueva línea-base: `bef70ec7`, CPD 2998 líneas** (+299). Mismo patrón que los
> addenda anteriores: filas nuevas SIN reescribir la tabla vieja; todos los line-refs de este
> addendum están verificados contra el árbol actual (deriva detallada en el addendum 21-08 del
> doc 08).
>
> **Censo actualizado** (corrige el addendum 20-08 sin reescribirlo): tags vigentes **136 → 140** ·
> en producción **132 → 136** (solo-test sigue 3: DET-B-03, DET-D-04,
> DET-UNVERIFIED-ARM-DRIVE-PROOF-001) · guard-código vivo **127 → 131** · solo-comentario/doctrinal
> sigue 7 · frontera guard/histórico sigue 1. Los cuatro tags nuevos tienen condición ejecutable en
> producción y test propio, así que los cuatro suman en las tres cifras. Grupos de clase de fallo
> siguen **15** (G5 pasa de 13 a 14 miembros; G6 de 6 a 7; G9 de 16 a 17; G13 de 14 a 15).

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-HANDOFF-NOT-MANUAL-001** | 2026-08-19 22:32 — un paseo en BICI publicó 2 spots fantasma y LIBERÓ 2 coches que no se habían movido (spots borrados y coches re-marcados a mano) (`26aec131`) · backlog `det-handoff-not-manual-001.md` | Dos defectos de forma opuesta. **(A)** el handoff de llegada del safety net reusaba el intent de «Estoy conduciendo»: un arm AUTOMÁTICO entrando por la puerta reservada a la intención humana → provenance falsa (`armEvidence` = `manual`), evidencia fuerte no ganada, exención del strategy-gate Y del quiet period de DET-STOP-BUTTON-001. **(B)** todo departure confirmado comprometía TRES cosas irreversibles a la vez (publicar spot + soltar sesión + borrar geocerca) sin importar qué se hubiera observado | *(A) El handoff arma como ÉL MISMO.* Trigger, evidencia y puerto propios: `ACTION_ARRIVAL_HANDOFF` → `DetectionTrigger.ARRIVAL_HANDOFF` + `ArmEvidence.ArrivalHandoff`; `ACTION_START_TRACKING` vuelve a significar el botón. Gateado por `coordinatorMayArm` como todo nominador automático, débil en `weakLabels` de EvalPD, y sujeto al quiet period. *(B) Los dos compromisos se separan por PRUEBA* (`DepartureProof`): solo un fix fresco a velocidad de conducción creíble es `Witnessed`. En `Deduced` el spot sale **YA** pero PROVISIONAL (TTL 12 min) y la sesión + su geocerca **se QUEDAN** — una plaza libre vale minutos, soltar la sesión propia no le vale nada a nadie. `FinalizeDeducedDepartureUseCase` mueve el commit a la prueba (re-publica el MISMO documento con TTL completo y solo entonces suelta el coche); `RetractDeducedDepartureUseCase` lo RETIRA si el viaje terminó sin medir conducción. *(B.3) Retirar es un ESTADO, no un silencio*: `SpotStatus` (CONFIRMED/PROVISIONAL/RETRACTED) viaja end-to-end y el documento retirado sobrevive `RETRACTION_GRACE_MS` para poder explicarse | `DepartureProof.kt:26-42` [KMP] · `SpotStatus.kt:19-45` + `SpotTtlPolicy.kt:38,50,62-65` [KMP] · `ProcessConfirmedDepartureUseCase.kt:61-129` (gate de doble-publicación `:79-80`, `provisional = proof == Deduced` `:100`, «no se lleva el coche» `:104-128`) · `RunDepartureCheckUseCase.kt:70` (arranca en Deduced), `:111-113` (única rama que MIDE → Witnessed), `:158` · `FinalizeDeducedDepartureUseCase.kt:48-94` · `RetractDeducedDepartureUseCase.kt:59-104` · CPD `:137`/`:141` (inyección opcional), `:998-1006` (one-shot al `driveProven`), `:1556-1565` (retracción en el `finally`, DESPUÉS del cinturón T7) · CDS `:261` (intake), `:357-377` (`handleArrivalHandoff`), `:465` (la vía watchdog sigue `Witnessed`) · `ArrivalHandoffDetection.kt` + `ArrivalHandoffDetectionImpl.kt:16-22` [Android] + `IosArrivalHandoffDetectionImpl` [iOS] · `ArmEvidence.kt:37,59,74` · `DetectionTrigger.kt:32` · `ParkingStrategyResolver.kt:53-57,67-68` · `EvalPD:228-240` (`LABEL_ARRIVAL_HANDOFF` en `weakLabels`) · `UserStopQuietPeriod.kt:18-22` · `PendingNudgeDecision.kt:12-18,26` · PSNW `:414-429` (puerto propio) · Room **v18 → v20** (`AppDatabase.kt:41-45`, `Migrations.kt:230-240`, `UserParkingDao.kt:68`, `UserParkingEntity.kt:45`) · `DetectionEvent.SpotRetracted` `:192-205` + `DepartureProcessed.sessionCleared` `:184-190` | Ninguno del state del CPD (dos locales del `invoke`: el latch `deducedDepartureSettled`). Estado nuevo DURABLE en la sesión: `UserParking.provisionalDepartureAtMs` (`UserParking.kt:49-53`), **local-only** — el mapper de Firestore no lo sube (`ParkingSessionMapper.kt:103-104`) | ✅ `FinalizeDeducedDepartureUseCaseTest:42` (promociona + suelta), `:60`/`:77` (idempotencia sin deducción / sin sesión), `:86` (zona privada: no publica pero sí suelta); `RetractDeducedDepartureUseCaseTest:37` (retira), `:50` (el coche y el marcador SIGUEN), `:65`/`:76` (nada pendiente / zona privada), `:85` (fallo de retirada → 0); `ProcessConfirmedDepartureUseCaseTest`; `ParkingStrategyResolverTest`, `PendingNudgeDecisionTest`, `UserStopQuietPeriodTest`, `RunDepartureCheckUseCaseTest`, `SpotDtoMapperTest`, `HomeSlicesTest`. 1258 tests | **Cierra el agujero de DET-STRATEGY-GATE-001** (el handoff se colaba por la exención MANUAL) y el de **DET-STOP-BUTTON-001** (se colaba por el quiet period). Mantiene **DET-ARRIVAL-HANDOFF-001** (sigue existiendo, ahora con puerto propio). **TENSIÓN de doctrina NUEVA, deliberada**: publicar un spot comunitario sobre evidencia DEDUCIDA es exactamente lo que «solo el movimiento medido confirma» prohíbe — la asimetría se aplica ahora POR COMPROMISO (plaza sí, coche no) en vez de globalmente, acotada por el TTL corto + la retracción. Queda razonada abajo (§5-nota), no abierta como colisión. Deja además la **brecha declarada** por el propio commit: avisar a quien ya iba a la plaza no es construible (`enRouteCount` sin filas por usuario) |
| **DET-HUMAN-POWERED-EARLY-CLOSE-001** | 2026-08-19 tras un paseo en bici, la sesión siguió VIVA 19 min después de llegar a casa (Oppo `1787171533976` / Redmi `1787171592952`), reciclando CANDIDATE↔Notified cada 5 min con FGS y GPS a 2-5 s, y solo murió al expirar el response-timeout de 15 min (`16d3ea69`) · backlog `det-human-powered-early-close-001.md` | La evidencia estaba desde el principio: `humanPoweredRide` solo se consultaba en la rama del response-timeout, así que un veredicto enunciable EN LA PARADA colgaba de un reloj; y `stepCount` se pone a 0 en cada descarte, así que el bucle no habría podido confirmar nunca | *La parada madura ES el veredicto.* `ParkingDecision.CloseHumanPowered` (TERMINAL — cuarta salida de la máquina de decisión) + input `restCertified`. Sin reloj nuevo: el reposo lo certifica el mismo número del tier (`slowPath5MinMs`). La pregunta se hace ANTES de abrir candidate y ANTES de notificar, así que a una bici nunca se le pregunta «¿has aparcado?»; el prompt Low/Medium se SUPRIME dejando la fase en `LowReached` (sin estampar un `shownAt` que nadie vio), de modo que si el veto se levanta (un AR `IN_VEHICLE` ENTER superando el sello) el prompt sale en el fix siguiente con su timeout medido desde `firstReachedAt`. Con evidencia completa de conducción la respuesta sigue siendo Prompt — un tap lo salva. Las tres vías comparten un ÚNICO builder `parkingDecisionInput` | `EvalPD:38` (`CloseHumanPowered`), `:131-135` (`restCertified`), `:287-294` (rama: DESPUÉS de los confirms, ANTES de `windowElapsed`) · CPD `:2064-2100` (el builder único; 3 parámetros son lo único que difiere por vía), `:2102-2126` (`closeHumanPoweredRide`) · las tres vías: fast steps+egress `:1484-1501` (`restCertified = false` — un ciclista parado en un semáforo no se cierra), candidate `:1976-1988` (`true`, High solo llega por el tier de 5 min), reloj de parada `:2785-2801` (`true`) · CPD `:2025-2032` (terminal en el candidate), `:1527-1533` (la sesión TERMINA), `:2844-2857` (supresión del prompt Low/Medium) · `advanceHigh` `:2892-2903` conserva el porqué de la mudanza | Ninguno nuevo en `ParkingDetectionState`: el input `restCertified` es un parámetro de la llamada, no un campo | ✅ `EvaluateParkingDecisionUseCaseTest:338` (parada madura + humanPowered → CloseHumanPowered), `:347-352` (la vía rápida con `restCertified=false` NO cierra), `:364`, `:376` (sin veto humano, `restCertified` es inocuo); `CoordinatorParkingDetectorTest:1821` (la sesión cierra en la parada madura y NUNCA emite `PROMPT_SHOWN`), `:1950` (§B: cierra aunque el AR EXIT tope el scorer en Medium). 1252 tests en su commit | **Consume** DET-BIKE-NOT-A-CAR-001 (+ las 2 fuentes de DET-MOTOR-PROOF-001) como predicado, sin cambiarlo. **Mantiene a propósito** la etiqueta de outcome `aborted_unattended_human_powered` y el `decisionOutcome` `UNATTENDED_HUMAN_POWERED_NUDGE` para que las comparaciones de campo contra trazas anteriores sigan cuadrando → alimenta el **bug #3** del doc 11 (membership por convención de nombre) y **abre el bug #7** (el `pathLabel` `unattended_timeout` viaja también por esta vía nueva, ver doc 11). El `restCertified=false` de la vía rápida es una precondición NUEVA del mismo evaluador que DET-D-03 usa: dos consumidores del mismo veredicto con distinta autoridad sobre su rama terminal. **Superado en parte por `1c292ac8` §B** (la pregunta se mudó de `advanceHigh` al reloj de parada) — ver fila de MOTORWAY |
| **DET-FENCE-REREGISTER-BY-CAUSE-001** | 2026-08-20 — un ✗ rojo «re-registro FALLÓ» sobre una valla demostrablemente VIVA, sin forma de saber por qué. Tirando del hilo: DOS workers re-registrando las mismas vallas por RELOJ, y el que no tenía ninguna salvaguarda no era el que nadie sospechaba (`ef03cea6`) · backlog `det-fence-reregister-by-cause-001.md` | Re-registrar NO es gratis: cada registro EXITOSO resetea el estado INSIDE/OUTSIDE de Play Services a «desconocido» — una ventana ciega en la que una salida no produce EXIT (campo 2026-07-11: salida silenciosa 40 s después de una cura). La cura tenía cuatro gates; **el janitor NINGUNO**, y corría en cada apertura de la app: medido en el Oppo, un solo arranque re-registró la MISMA valla **dos veces, a 4,3 s** (dos encoladores + `REPLACE` que no deduplica una ejecución ya terminada). Y `createGeofence` envolvía todo en `runCatching` y el caller solo leía `isFailure`: la `ApiException` — la respuesta entera — a la basura | **§A medido, no supuesto**: `FenceRegistrationPolicy` + `FenceRegistrationLedger` saltan SOLO lo demostrablemente redundante — lo que ESTE proceso registró hace instantes. Un proceso fresco no tiene registro y SIEMPRE registra (es el caso force-stop, indetectable). ⚠️ Se consideró y **RECHAZÓ** un gate por DISTANCIA: tras un force-stop las vallas están BORRADAS y nada nos lo dice, así que saltar por proximidad dejaría un coche sin valla PERMANENTEMENTE en vez de una ventana ciega que se cierra sola. **§B la cura repara por CAUSA**: `markFenceStatePoisoned` estampa el hecho de que un EXIT falso descartado dejó la valla sorda, sustituyendo un esquema que lo decía BORRANDO una clave de throttle (clave ausente significaba a la vez envenenada Y nunca curada Y recién instalada); la causa vence incluso al guard de frescura, que descansa sobre una suposición que el descarte refuta. Y el suelo cuenta **ÉXITOS, no intentos** (el sello se escribía antes de la llamada y no se revertía: una cura fallida se compraba seis horas de silencio). **§D la razón deja de perderse**: `GeofenceRegistrationFailure` (puro, testeado) nombra los status de GMS y separa `PERMISSION_DENIED` de `UNKNOWN`; el evento gana `source` (cure/janitor) y `failure`, ambos en columnas que el DTO YA tenía; **el janitor emite evento de registro por primera vez** | `FenceRegistrationPolicy.kt:36-59` [KMP] · `GeofenceRegistrationFailure.kt:20-69` [KMP] · `FenceRegistrationLedger.kt:21-56` [Android] · `GeofenceFailureMapping.kt:15-24` [Android] · `GeofenceManagerImpl.kt:46-52` (`recordSuccess` donde convergen TODOS los caminos), `:126-134` (`forget`/`reset`) · `GeofenceJanitorWorker.kt:99-108` (gate del ledger), `:115-141` (razón + evento), `:159-161` (`REGISTRATION_SOURCE_JANITOR`) · PSNW `:288-326` (causa antes que reloj + ledger), `:333-355` (razón + suelo por ÉXITOS + consumo del veneno solo al acertar), `:356-371` (evento con `source`+`failure`), `:733` (rama propia `cure_poisoned_`), `:850-880` (keys + `markFenceStatePoisoned`) · `DepartureDetectionWorker.kt:50-53` (quien envenena) · `EvaluateSafetyNetCheckUseCase.kt:426-457` (`statePoisoned` primero en `shouldReregisterCure`) · `Config:638-646` (`fenceRegisterDedupWindowMs` 5 min), `:653-661` (el suelo de 6 h y su justificación honesta), `:969-973` (requires) · `DetectionEvent.kt:249-262` + `DetectionEventDto.kt:186-193` (`source`/`reason` reusadas) · `PaparcarApp.kt:85` | Ninguno del CPD. Estado nuevo: mapa **EN MEMORIA** por proceso (`FenceRegistrationLedger`, deliberado — persistirlo enseñaría a saltarse justo el caso que nunca debe saltarse) + sello durable `poisoned_<geofenceId>` en las prefs del safety-net | ✅ `FenceRegistrationPolicyTest:18,25,32,38,46` (5: sin registro previo · salta el segundo pase · vuelve al expirar la ventana · **la causa vence a un registro reciente** · sello del futuro); `GeofenceRegistrationFailureTest:14,30,48,61` (4: status conocidos · permiso ≠ desconocido · el permiso gana a cualquier status · etiqueta de wire estable); `EvaluateSafetyNetCheckUseCaseTest:566` (sesión fresca), `:577` (envenenada → cura ya), `:594` (**el veneno vence al guard de frescura**). 1295 tests | **Cambia la PRECEDENCIA interna de DET-CURE-FRESH-001** sin retirarlo: la frescura de la sesión era un veto y ahora `statePoisoned` la precede (`EvaluateSafetyNetCheckUseCase.kt:453-457`). Es el solape más relevante del addendum: hoy hay **DOS throttles sobre la MISMA valla** — el suelo ciego de la cura (`cureReregisterMinIntervalMs`, 6 h) y la ventana de dedup del ledger (`fenceRegisterDedupWindowMs`, 5 min) — con causas distintas y aplicados en cascada (PSNW `:317`). Candidato claro a política única en el refactor (ver §3-g abajo). Toca también GEOF-RESTORE-001 / SESSION-RESTORE-001 (el janitor) y DET-EXIT-WITNESS-001 (la valla testigo cuenta en el presupuesto de 3 vallas por coche que documenta `GeofenceRegistrationFailure.TOO_MANY_GEOFENCES`). Los dos intervalos (6 h / 12 h) se dejan INTACTOS a propósito: son los únicos parámetros aquí que dependen de dato de campo, y la telemetría que los zanjaría acaba de encenderse |
| **DET-MOTORWAY-TRIP-JUDGED-BICYCLE-001** | 2026-08-20, Redmi sesión `1787242874932`: un viaje de AUTOVÍA que sostuvo 40+ km/h durante **361 s** y picó a **131,4 km/h** con 4,6 m de accuracy fue juzgado **BICICLETA** — no guardó nada, colgó 102 min a través de dos viajes y dejó al coche sin geocerca, así que el tercer viaje ni armó. El Oppo pinchó los tres (`1c292ac8`) · backlog `det-motorway-trip-judged-bicycle-001.md` | Dos fallos. **(A)** la doctrina rectora —*el evento nomina, solo el movimiento medido confirma*— se escribió para el lado del CONFIRM y se olvidó en el lado del VETO: un sello de Activity Recognition podía contradecir al stream, y el stream no podía contradecir al sello («gana la última etiqueta» era toda la arbitración). **(B)** el veredicto de cierre esperaba un High que la vía rápida del scorer hace INALCANZABLE siempre que AR entregó un vehicle exit (topa en Medium) — combinado con el prompt suprimido de la bici, la sesión no tenía prompt (ni response-timeout), ni High (ni cierre), ni candidate: nada podía terminarla | **§A el motor MEDIDO refuta toda afirmación de tracción humana**: una banda de motor sostenida (`motorProofSpeedMps` 11,1 m/s ≈ 40 km/h mantenida `sustainedDriveProofMs` = 30 s) vence al sello AR **y** al latch de cadencia — es una REFUTACIÓN, no una prueba de conducción (no licencia ningún pin silencioso por sí sola). Además un `IN_VEHICLE` **EXIT** ahora prueba el embarque que siguió y supersede igual que un ENTER (nadie se baja de un vehículo al que no se subió; en una sesión armada por geocerca el EXIT es a menudo el único que AR entrega). El reloj de banda pasa a función pura compartida (`SpeedBandClock.creditSpeedBand`): un solo acumulador para las dos bandas. **§B el reposo sostenido se MIDE, no se infiere de un score**: el veredicto de cierre lee el reloj de parada (`stoppedDuration >= slowPath5MinMs`) ANTES del dispatch de tiers, así que TODA ruta a un reposo maduro pasa por él exactamente una vez. **§C el carril de EVIDENCIA AR entra en la traza** (`ON_BICYCLE` / `IN_VEHICLE` ENTER como `ACTIVITY_TRANSITION`, llevando lo rancia que era la respuesta de AR) — la señal que decidía una sesión deja de ser invisible en forense | `SpeedBandClock.kt:27-37` [KMP] · `HumanPoweredRide.kt:75-91` (la refutación, colocada ENCIMA del latch de cadencia `:93-100` — el techo de velocidad que la cadencia nunca tuvo), `:109-113` (el EXIT como embarque) · CPD `:405-407` (`motorBandMs`/`lastMotorBandFixTimestampMs`), `:955-968` (reloj de banda + log de cruce), `:991-993` (escritura), `:1593-1612` (`sustainedMotorBandMs` al predicado), `:1614-1627` (`onVehicleExit` sella `vehicleRideAtMs`, solo hacia delante) · §B: CPD `:2761-2801` (la pregunta se muda AQUÍ) y `:2892-2903` (por qué ya no vive en `advanceHigh`) · §C: CPD `:613-615` (latches de dedup), `:1026-1071` (carril AR + `MOTOR_WITNESSED`) · `ActivityTransitionReceiver.kt:61-67` (el EXIT viaja con su tiempo VERDADERO) · `DetectionEvent.ActivityTransition.trueTimeAgeMs` `:64-70` + DTO `:150-153` (cabalga la columna `enterAgeMs`) · `Config:512` (`sustainedDriveProofMs`), `:531` (`motorProofSpeedMps`), `:1018-1028` (requires: `motorProofSpeedMps > minimumTripSpeedMps`) | `motorBandMs`, `lastMotorBandFixTimestampMs` (CPD `:405-407`); 3 locales de dedup de traza (`loggedBicycleRideAtMs`, `loggedVehicleRideAtMs`, `loggedMotorWitnessed`, CPD `:613-615`) | ✅ `SpeedBandClockTest:13,21,31,39,48` (5: fuera de banda · sin par previo · acredita el hueco · hueco mayor que la ventana no acredita · puente sobre agujero de accuracy); `HumanPoweredRideTest` (+71 líneas: refutación por motor · EXIT como embarque · techo de la cadencia); `CoordinatorParkingDetectorTest:1868` (§C: el sello de bici en la traza con su edad), `:1901` (§C: el latch de cadencia trazado aunque su 2º fix llegue tarde), `:1950` (§B: cierra con el scorer topado en Medium — **regresión verificada en ROJO sin el fix**); replay **`Trace_MotorwayRedmi001.kt`** (1.507 líneas, los 1.470 eventos de campo) con sus dos tests: `DetectionTraceReplayTest:561` (con el sello inyectado reproduce 1:1 el fallo: 0 candidates, las mismas 2 decisiones, 0 pines) y `:611` (sin él, el mismo stream confirma). Fixtures de bici corregidas: pedaleaban a 39,6 km/h e inyectaban un vehicle exit que su fuente de campo nunca registró. 1300 tests | **Redefine `isHumanPoweredRide` por tercera vez**: DET-BIKE-NOT-A-CAR-001 (perfil + sello AR) → DET-MOTOR-PROOF-001 (2ª fuente cinemática) → aquí (refutación por motor medido, que vence a las dos). El orden vigente es: perfil registrado (DET-SOLID-001 C2, **inmune** — un perfil BIKE no lo refuta ninguna medición) → banda de motor → cadencia → sellos AR. **Corrige a DET-MOTOR-PROOF-001** sin retirarlo: comparte su `sustainedDriveProofMs` y su ventana `driveProofWindowMaxMs`, y le pone el techo que su latch de cadencia no tenía. **Corrige a DET-HUMAN-POWERED-EARLY-CLOSE-001** (§B: la premisa «High ES la parada certificada» era falsa por la vía rápida del scorer). Cruza con BUG-DETECT-310503 (esa vía rápida que topa en Medium es la causa raíz de §B — y es justo lo que el WIP `DET-EVIDENCE-MUST-NOT-LOWER-CONFIDENCE-001` quiere eliminar, ver doc 11 §WIP) |

**Adjudicación a los censos del doc (sin reescribirlos):**

- **§2 clases de fallo:**
  - **DET-HANDOFF-NOT-MANUAL-001 → G6** «Liberación falsa del pin correcto — el cierre/reconcile
    inventa una salida» (7º miembro: liberó dos coches parados). *Cruces:* con **G4** (el evento
    stale/espurio con autoridad — el handoff llevaba autoridad MANUAL que no había ganado) y con
    **G13** (spot fantasma anunciado a la comunidad). [Adjudicación de este addendum, **NO
    VERIFICADA** con el user.]
  - **DET-HUMAN-POWERED-EARLY-CLOSE-001 → G13** «UX deshonesta / estado visible incoherente»
    (15º miembro: preguntar «¿has aparcado?» a quien iba en bici es la pregunta equivocada en
    pantalla). *Cruces:* con **G10** (19 min de FGS + GPS a 2-5 s colgados) y con **G3** (el veto
    que lo decide es el de movimiento no vehicular). [**NO VERIFICADA** con el user.]
  - **DET-FENCE-REREGISTER-BY-CAUSE-001 → G9** «Pérdida por proceso/OS — Doze, OEM-kill, GMS
    amnésico» (17º miembro: la ventana ciega de INSIDE/OUTSIDE es literalmente GMS olvidando dónde
    está). *Cruces:* con **G14** (§D es observabilidad pura: sin la razón no hay diagnóstico) y con
    **G5** (una valla ciega en el momento de la salida es un FN). [**NO VERIFICADA** con el user.]
  - **DET-MOTORWAY-TRIP-JUDGED-BICYCLE-001 → G5** «Pérdida total del aparcamiento (FN)»
    (14º miembro: viaje de autovía sin pin + coche sin geocerca + tercer viaje sin armar).
    *Cruces:* con **G3 invertido** (G3 es «movimiento no vehicular leído como coche»; esto es su
    espejo — movimiento vehicular leído como no-vehicular: el grupo merece que su enunciado se lea
    en ambas direcciones), con **G7** (sesión zombie: 102 min sin salida) y con **G14** (§C).
    [**NO VERIFICADA** con el user.]
- **§3 políticas:**
  - **3-a «DriveProof»**: DET-MOTORWAY añade una SEGUNDA BANDA al mismo verificador y, al hacerlo,
    **extrae por fin el predicado**: `SpeedBandClock.kt` es el acumulador único que 3-a predijo
    («verificador único parametrizado por momento»), aquí parametrizado por BANDA
    (`minimumTripSpeedMps` vs `motorProofSpeedMps`). Es la primera pieza de 3-a que existe ya en el
    árbol como función pura top-level — la política se sostiene y gana evidencia.
  - **`HumanPoweredRide.kt`** sigue siendo el predicado compartido del patrón ya establecido; ahora
    con CUATRO fuentes ordenadas (perfil · banda de motor · cadencia · sellos AR). No se eleva a
    política nueva: es una función, no una familia.
  - **3-g (NUEVO solape registrado, no elevado a política)** — «cuándo re-registrar una valla»:
    `FenceRegistrationPolicy.shouldRegister` (dedup 5 min por proceso) y
    `EvaluateSafetyNetCheckUseCase.shouldReregisterCure` (suelo ciego 6 h + frescura + veneno)
    responden a la MISMA pregunta sobre la MISMA valla y se aplican en cascada (PSNW `:317`). Ambas
    son puras y testeadas por separado; la unificación es candidata para el `physics/CureThrottle.kt`
    que el 09 §3 ya reservaba — que debería pasar a llamarse `FenceRegistrationDecision` y absorber
    las dos. **NO elevado a política**: son dos causas genuinamente distintas (redundancia
    demostrable vs poisoning no observable) y fundirlas sin demostración caso-por-caso violaría la
    regla de 3-f.
  - **DET-HANDOFF-NOT-MANUAL-001** no toca las 5 candidatas: introduce un EJE nuevo
    (`DepartureProof`) sobre el lado de la SALIDA, que este doc apenas censaba. Nace ya en la forma
    correcta (enum de dominio + dos veredictos propios con sus tests), no como predicado suelto.
  - **DET-HUMAN-POWERED-EARLY-CLOSE-001** aporta algo distinto y valioso para F5: el builder único
    `parkingDecisionInput` (CPD `:2064-2100`) **es el ensamblador único de `ParkingDecisionInput`
    que el 09 §4/§5 planeaba construir** — ya hecho, en master, sin refactor. Anotado en el
    addendum del 09.
- **§4 inalcanzables/dominados:** sin cambios en las cifras. Nota: la nueva rama terminal de EvalPD
  se inserta ENTRE los confirms y `windowElapsed` (`EvalPD:294-296`) sin dominar a ninguna.
- **§5 colisiones de doctrina:** **NO se abre colisión nueva**, y una tensión merece quedar
  razonada. `DET-HANDOFF-NOT-MANUAL-001 §B` publica un spot comunitario sobre evidencia DEDUCIDA —
  que es literalmente lo que «solo el movimiento MEDIDO confirma» prohíbe. No es una violación sino
  una **refinación de la asimetría**: la doctrina «mejor FN que FP» protege al USUARIO de perder su
  coche, y el commit separa por primera vez los dos compromisos que viajaban juntos, aplicando el
  rigor máximo al que tiene dueño (la sesión y su geocerca **no se tocan** sin conducción medida) y
  aceptando riesgo acotado en el que es un bien perecedero de la comunidad (spot PROVISIONAL, TTL
  12 min, retractable y marcado como no confirmado en la UI). Las dos colisiones abiertas de §5.1 y
  §5.2 quedan EXACTAMENTE como estaban.

---

## Addendum 2026-08-22 — cuatro commits de detección (`d7438173` · `1f7e6cde` · `cd7a2cf2` · `4d1d6716`)

> Cuatro commits de detección entraron en master tras el addendum del 21-08 (línea-base anterior
> `bef70ec7`, CPD 2998). **Nueva línea-base: `4d1d6716`, CPD 3052 líneas** (+54), CDS **1735**
> (+41). Dos de ellos venían de los worktrees que el doc 11 registraba como «trabajo en vuelo»
> (`../Paparcar-prompt-reason` → `d7438173`; `../Paparcar-evidence-floor` → `1f7e6cde`): ese
> apartado queda CERRADO (ver doc 11). Mismo patrón que los addenda anteriores: filas nuevas SIN
> reescribir la tabla vieja; todos los line-refs están verificados contra el árbol actual (deriva
> detallada en el addendum 22-08 del doc 08). Dos commits más del rango (`daaf4884`, `5f6fb6d7`)
> son de UI (glifo de ruta del chip · respiración del peek en viaje) y **no tocan detección** —
> revisados fichero a fichero, cero impacto en este censo.
>
> **Censo actualizado** (corrige el addendum 21-08 sin reescribirlo): tags vigentes **140 → 144** ·
> en producción **136 → 140** (solo-test sigue 3: DET-B-03, DET-D-04,
> DET-UNVERIFIED-ARM-DRIVE-PROOF-001) · guard-código vivo **131 → 135**, y **135 → 136** al
> recuperar `BUG-DETECT-310503` su condición ejecutable (§F abajo) · solo-comentario/doctrinal
> sigue 7 · **frontera guard/histórico 1 → 0** (era exactamente `BUG-DETECT-310503`). Los cuatro
> tags nuevos tienen condición ejecutable en producción y test propio, así que suman en las tres
> cifras. Grupos de clase de fallo siguen **15** (G4 pasa de 10 a 11 miembros; G5 de 14 a 16;
> G14 de 6 a 7).

| Tag | Incidente de campo | Síntoma | Regla que lo cierra | Dónde vive hoy | Campos de estado | Test que lo fija | ¿Colisiona con otro tag? |
|---|---|---|---|---|---|---|---|
| **DET-PROMPT-STATES-ITS-REASON-001** | 2026-08-20 23:56, Oppo sesión `1787263007358`: un viaje de 36 min que picó a **63,3 km/h**, paró y caminó **175 pasos** no guardó NADA, y **2 de sus 3 veredictos eran inatribuibles** — el caso solo se pudo cerrar porque el tercero (el timeout desatendido) SÍ nombra su causa vía `UnattendedSaveReason` (`d7438173`) · backlog `det-prompt-states-its-reason-001.md` | **SEIS** causas distintas degradaban un confirm en pregunta y las seis escribían la MISMA cadena `CONFIRM_DEGRADED_PROMPT`: cinco colapsadas en un `OR` de EvalPD (evidencia débil del arm · viaje muscular · egress no nacido en el ancla · ancla entrada andando · ancla entrada por hueco GPS) más el degrade por repark implausible del coordinator. `pathLabel` registraba CÓMO se probó el park; nada registraba **por qué esa prueba no se creyó** | *Un veredicto que no dice su causa no es citable en un diagnóstico.* Enum `PromptReason` (6 claves) + `ParkingDecision.Prompt(pathLabel, reason)`. Tres decisiones de diseño, todas escritas: (a) el `outcome` **NO se renombra** — `CONFIRM_DEGRADED_<CAUSA>` habría invalidado toda traza guardada y toda nota que la cita, la misma razón por la que `UnattendedSaveReason` se negó a renombrar dos etiquetas históricas; (b) la causa **cabalga la columna `reason` que ya existía** (`HonestClose`/`Released`/`GeofenceRegistration` la usan): cero superficie nueva de serializador — la prueba de que nada se rompió es que los 1.312 tests previos siguieron verdes sin tocar un assert; (c) el `OR` de cinco pasa a **first-match ORDENADO** porque con varias causas ciertas a la vez la etiqueta debe ser determinista o la telemetría no agrupa — duda más ANCHA primero: `HUMAN_POWERED` (afirmación sobre TODO el viaje) > `WEAK_EVIDENCE` (sobre el ARM) > las tres que solo dudan de DÓNDE está el ancla. `degradeToPrompt` recibe la razón **sin default** — un default resucitaría en silencio el prompt anónimo que este ticket borra. Rechazado a propósito: meterlo en `pathLabel`, que se compara por igualdad (romper un eje para arreglar el otro) | `EvalPD:19-32` (KDoc del enum: por qué es hermano de `UnattendedSaveReason`), `:33-56` (`PromptReason`, 6 causas con su tag de origen cada una), `:61-63` (`ParkingDecision.Prompt(pathLabel, reason)`), `:305-320` (el first-match ordenado + su rationale), `:322` (la rama 1 del `when` consume `promptReason != null`) · CPD `:1533-1535` (call site del scoring), `:1937-1942` (**el SEXTO productor**: el degrade por repark implausible, `reason = PromptReason.IMPLAUSIBLE_REPARK.key`), `:2057-2059` (call site del candidate), `:2080-2107` (`degradeToPrompt`, firma `reason: PromptReason` sin default `:2085`, log `:2089`, evento `:2104-2106`) · `DetectionEvent.Decision.reason` `:130-138` · `DetectionEventDto.kt:159-163` (`reason = reason` en la rama `Decision`) | **Ninguno** — la razón es un campo del veredicto y una columna del evento, no estado de sesión | ✅ `EvaluateParkingDecisionUseCaseTest:656` (human-powered medido), `:671` (perfil bici → MISMA razón: las dos vías dicen «no es un coche»), `:682` (weak evidence), `:689` (egress no en el ancla), `:698` (ancla andada), `:707` (ancla por hueco), `:714-745` (**el desempate**: con varias causas ciertas el orden es determinista), `:751` (unicidad de claves); `DetectionEventDtoTest` (paridad DTO en ambos sentidos). 1.322 tests en su commit | **Cierra la mitad `prompt` del defecto que el bug #7 del doc 11 denuncia en la mitad `nudge`** — y el #7 **NO queda resuelto** (`CPD:1850` sigue estampando el literal `"unattended_timeout"`, verificado en el árbol actual): son el mismo gesto en dos carriles y ahora hay precedente en master para el que falta. **Vocabulario compartido preservado**: `PromptReason.HUMAN_POWERED` y `UnattendedSaveReason.HUMAN_POWERED` siguen coincidiendo en clave (`human_powered`) — la coincidencia que el doc 11 §W.2 pedía no romper. No abre colisión: no cambia ninguna decisión, solo la nombra |
| **DET-EVIDENCE-MUST-NOT-LOWER-CONFIDENCE-001** | Telemetría 2026-08-20: de todas las sesiones del día, **la ÚNICA con transiciones AR tuvo cero candidates**. Es la firma del viaje de autovía de `1c292ac8` (102 min a través de dos viajes, sin pin) leída desde el otro extremo (`1f7e6cde`) · backlog `det-evidence-must-not-lower-confidence-001.md` | El scorer tenía **dos caminos mutuamente excluyentes** y el rápido retornaba antes de calcular el lento: la MISMA parada de 5 min puntuaba **0,80 sin** vehicle-exit de AR y **0,65 con** él. La evidencia más fuerte que poseemos de que el usuario se bajó del coche **BAJABA** la confianza de que aparcó — y como High es la única ruta a la fase CANDIDATE, un AR exit dejaba esa fase (y el carril de confirm `vehicleExit+window+egress` detrás de ella) **inalcanzable durante toda la sesión** | Dos pasos, y el ORDEN fue load-bearing. **Paso 1 — un veredicto no puede destruir una MEDICIÓN**: descartar un candidate ya no pone `stepCount = 0`; mueve una línea de frescura (`stepsAtLastDiscard`). El total sigue disponible para el bloqueo del ancla, los techos de walk-reach y sobre todo para el veredicto desatendido (que lo lee para justificar guardar el park como ZONA), mientras solo el evaluador de confirm ve el delta FRESCO. La conducción medida sigue siendo lo único que limpia ambos, exactamente como `walkFixesSinceDriving`. **Paso 2 — la rama desaparece**: un solo score ADITIVO (tier de reposo + bonos de velocidad, accuracy y vehicle-exit), con el exit **ACORTANDO la puerta** en vez de abrir un segundo carril. Todo término es ≥ 0, así que la propiedad violada se sostiene por la FORMA de la función, no por un `max()` defensivo. El silencio se preserva exacto; High gana **exactamente un caso** (reposo maduro + AR exit: 0,70+0,15) y deja de depender de dos bonos de 0,05 que un fix urbano mediocre nunca gana. **El orden importó y está medido**: el paso 2 solo, contra los 12 replays de campo, **PERDÍA un park real** (`redmi_late_exit_home_001`, la zona del 27-07) porque levantar el techo metía la sesión en el bucle de candidates cuyo descarte borraba los pasos de egress. Con el paso 1 delante, ni una fixture cambia de veredicto | `CalculateParkingConfidenceUseCase.kt:7-49` (KDoc: la propiedad, qué NO cambió), `:52-69` (**el scorer entero**: puerta `:53-55`, tiers `:57-63` con `briefRestScore` como `else` alcanzable solo por la puerta acortada, bonos `:64-66`) · `Config:14-31` (bloque EARLY GATE nuevo: `fastPathMinStoppedMs` `:20`, `briefRestScore` `:26`, `activityExitBonus` `:31`), `:896-919` (**los cuatro `require` que ahora DECLARAN los techos** que antes se sostenían por accidente aritmético) · **retirados del config**: `fastPathBaseScore` y `fastPathSpeedBonus` (ya no existen) · CPD `:329-338` (`stepsAtLastDiscard`), `:418-421` (`freshStepCount` derivado, `coerceAtLeast(0)`), `:1498-1500` (el fast-confirm lee el FRESCO), `:2036-2051` (el descarte mueve la línea en vez de poner a 0), `:2130-2134` (el builder único pasa `freshStepCount` a EvalPD), `:2765-2767` (solo `effectiveDriving` limpia la línea) | `stepsAtLastDiscard` (CPD `:338`) — NUEVO campo de `ParkingDetectionState`; `freshStepCount` (`:421`) es derivado, no almacenado | ✅ `CalculateParkingConfidenceUseCaseTest:137` (**la PROPIEDAD, no un caso**: barrido 6 duraciones × 2 velocidades × 2 accuracies afirmando que añadir el exit nunca baja el nivel), `:169` (reposo maduro + AR exit → High: el bug de campo), `:183` (**parada breve + AR exit sigue sin auto-confirmar**: 0,35+0,05+0,05+0,15 = 0,60), `:197` (silencio preservado); `CoordinatorParkingDetectorTest:1950` `should_keep_the_egress_steps_on_the_record_when_a_candidate_is_discarded` (**regresión del paso 1 verificada en ROJO sin él**, con la traza de campo del 27-07). 1.317 tests en su commit | **Rehabilita `BUG-DETECT-310503`** — no lo retira: ver §F abajo, es el hallazgo más importante de este addendum. **Cumple la mitad B de `1c292ac8`**: la premisa «High ES la parada certificada» vuelve a ser cierta, pero el reloj de reposo de §B (`CPD:2815+`) **NO debe revertirse** — mide en vez de inferir, que es el punto; el riesgo real es que alguien lo lea como código muerto. **Continúa (no colisiona con) `16d3ea69`**: el paso 1 arregla literalmente el segundo defecto que aquel commit diagnosticó y no arregló («`stepCount` is zeroed on every discard, so the loop could never have confirmed anyway»). ⚠️ **NO VERIFICADO** el efecto neto sobre la etapa CANDIDATE en campo: con High alcanzable teniendo AR exit se abrirán candidates donde hoy no se abren, y la segunda puerta del cierre temprano (`CPD:2025-2032`) pasa de rara a común |
| **DET-BT-DISCONNECT-WITHOUT-RIDE-001** | 2026-08-21, Oppo + Kamiq, **un coche que no se conducía desde hacía días**: ACL CONNECT 14:08:40,2 → DISCONNECT 14:08:53,9 = **13,7 s**, corroborado FUERA de la app por `dumpsys bluetooth_manager` (la `A2dpStateMachine` del Kamiq guarda 7 registros, todos de ese minuto, SBC negociado y caído 11,5 s después). El coche lo había traído un familiar y lo apagó al lado del teléfono. Quince minutos más tarde el carril confirmó `bt_timeout` a 0,85 con una geocerca de 105 m, **cuyo EXIT armó luego al coordinator** cuando el usuario salió andando (`cd7a2cf2`) · backlog `det-bt-disconnect-without-ride-001.md` | El carril BT **nunca comprobó que el coche se hubiera movido**. Leía UN instante —el `ACL_DISCONNECTED`— y entraba directo en debounce, muestreo GPS y vigilancia de walk-away. Y la cadena estaba invertida encima: `evaluateCandidateFix` acepta un candidato **PORQUE el usuario está quieto**, que es exactamente el estado de quien no ha conducido a ninguna parte. Era **el único camino de confirmación de la app sin requisito alguno de conducción medida** | *Una conexión Bluetooth prueba PRESENCIA, no conducción.* La posición era CORRECTA (el coche estaba allí); lo falso era la ATRIBUCIÓN — y una conexión sí lleva información (sin ella la app seguiría mostrando la plaza de ayer después de que otro moviera el coche). Lo que NO puede es distinguir «aparcado a mi lado» de «pasó conduciendo por delante», porque el fix muestreado está en el TELÉFONO y el teléfono no iba en el coche. Así que **la conexión se DIMENSIONA antes de muestrear nada**, y solo una con forma de viaje puede plantar un pin: `Ride` (90 s ≤ duración ≤ 12 h → flujo intacto) · `ProximityOnly` (más corta → pregunta y vuelve) · `Unknown` (sin sello / salto de reloj / sello más viejo que el techo → pregunta y vuelve). Deliberadamente **NO** es un test de distancia ni de velocidad: en ese instante no se ha medido nada todavía; la duración es lo único ya en disco. El gate va **ANTES del debounce**, no en la rama del timeout, por dos razones: cubre AMBOS caminos BT (con 13 s de conexión + 30 m de paseo a la panadería el código viejo también pinchaba, a 0,95) y un blip de proximidad deja de sostener un FGS y el stream GPS durante 15 min. El techo de 12 h no es decoración: un force-stop hace perder broadcasts, y un CONNECT perdido con un DISCONNECT posterior computaría un «viaje» de varios días y devolvería la conducta vieja. **Provenance**: el carril BT no estampaba `armEvidence` **ninguna** (el pin de campo llevaba `null`, por eso el diagnóstico necesitó el cable); ahora los dos confirms estampan `ArmEvidence.BtRide(engagementMs)` | `EvaluateBtParkUseCase.kt:26-44` (`BtEngagement`: `Ride`/`ProximityOnly`/`Unknown`), `:69-100` (`evaluateEngagement`, techo y skew `:94`, umbral `:95-99`) · `BluetoothParkingDetector.kt:63` (la firma gana `connectedAtMs`), `:66-84` (**el gate antes del debounce**, log `:76`, traza `:77`, nudge `:82`, `return` `:83`), `:164` y `:193` (los DOS confirms estampan `BtRide`), `:243-248` (`VERDICT_NO_RIDE_ASK` = `bt_no_ride_ask`, `NUDGE_SOURCE_BT_NO_RIDE`) · `BluetoothDetectionService.kt:148-157` (lee `BtConnectionStore.lastConnectedAt` en la capa Android para que el core de decisión siga sin `Context`) · `ArmEvidence.kt:46-52` (`BtRide(engagementMs)`, con su KDoc: **no** es `isVerifiedDeparture`), `:68` + `:87` (`LABEL_BT_RIDE = "bt_ride"`) · `Config:653-661` (`btMinRideDurationMs` 90 s), `:662-668` (`btMaxRideDurationMs` 12 h), `:1121-1127` (requires) | Ninguno del CPD — el dato es el sello durable `lastConnectedAt` de `BtConnectionStore`, escrito por el receiver de manifiesto desde DET-BT-IDENTITY-GATE-001 y **por tanto ya superviviente a OEM-kills**. *«The data was on disk all along; nobody read it.»* | ✅ `EvaluateBtParkUseCaseTest:108` (**el caso de campo exacto**: 13,7 s → ProximityOnly), `:117` (justo en el mínimo → Ride), `:126` (1 ms por debajo → ProximityOnly), `:135` (sin connect → Unknown), `:141` (sello en el FUTURO → Unknown), `:147` (por encima del techo → Unknown); los 11 tests previos del carril intactos — `DET-BT-TIMEOUT-SAVE-001` (park en casa) **no regresa**. 1.333 tests en su commit | **Cierra el último agujero de la doctrina rectora**: era el único carril de confirm sin requisito de conducción medida, así que *«el evento NOMINA, solo el movimiento MEDIDO confirma»* pasa de ser cierta en el Coordinator a serlo en toda la app. **Toca a DET-STRATEGY-GATE-001** por el lado sano: el pin BT falso generaba una geocerca cuyo EXIT armaba luego al Coordinator — un FP que engendraba un arm; sin pin no hay valla. **Refuerza DET-PIN-PROVENANCE-001** (el carril pasa de `armEvidence = null` a estampar siempre). **Deja una alternativa RECHAZADA con evidencia**: la sonda de permanencia («¿sigue visible la MAC dos minutos después?») se diseñó y se descartó porque el módulo de ESTE coche muere con el contacto, así que un coche aparcado y uno ausente producen el mismo silencio — anotada en `DET-BT-BOARDING-ANCHOR-001` (backlog, **sin código: NO cuenta en el censo**) junto al ancla de embarque, que es la vía superviviente para cerrar el mismo borde por encima del suelo |
| **DET-COOLDOWN-MUST-NOT-BLIND-A-DRIVE-001** | 2026-08-21, **los DOS móviles, dos pérdidas en los mismos dos viajes**: el Redmi no pinchó Covirán en absoluto (22:12:00 → 22:15:38, ciego el viaje entero) y el Oppo pinchó el coche **DENTRO de la casa del usuario** en vez de en el punto de dejada (23:38:55 → 23:46:38, ciego 988 m y 7 min 43 s). Una sola cadena causó ambas (`4d1d6716`) · backlog `det-cooldown-must-not-blind-a-drive-001.md` | Caminar HACIA el coche dispara BUG-FALSE-ENTER-WALKING (8 pasos peatonales antes de alcanzar velocidad de conducción) y termina la sesión; tres de esos ganan un quiet period de DET-SENTRY-COOLDOWN-001; el quiet period **apaga el sensor de movimiento significativo**, y el viaje ocurre dentro. El amortiguador se autolicenciaba a dormir el sensor **con un comentario**: *«la geocerca EXIT, el carril AR ENTER y el safety net periódico siguen vigilando»*. Nunca fue un input, y esa noche fue falso **de tres maneras a la vez**: la valla de Covirán nunca entregó su EXIT; la del Redmi ya se había consumido y el móvil estaba **389 m FUERA** de ella, así que ninguna valla podía volver a disparar; AR entregó `ON_BICYCLE`, jamás `IN_VEHICLE`; y en el Oppo el heartbeat exacto de 5 min llevaba muerto desde las 18:28, dejando una rejilla de 15 min cuya celda **se tragó el viaje** (checks a las 23:15:21 → 23:46:38) | *El amortiguador de tormentas no puede silenciar al último vigilante en pie.* Las dos premisas pasan a ser **INPUTS puros**, ambas en `SentryWakeCooldown.kt`. **(1) Una tormenta es CADENCIA, no recuento**: `nextSentryWakeAbortStreak` recibe `msSinceLastAbort`; pasado `sentryWakeStreakDecayMs` (10 min) un abort **empieza una racha nueva** en vez de extender la vieja. La tormenta del 08-13 disparaba cada ~18 s; los tres aborts que cegaron al Oppo estaban repartidos en **52 minutos** (22:46:56, 22:59:48, 23:38:15) — eso es una persona viviendo su tarde, no un sensor gritando. Skew negativo ⇒ «sin predecesor» ⇒ racha fresca, jamás una cadencia espuriamente apretada. **(2) Silenciar al último vigilante es quedarse ciego**: `sentryWakeRearmCooldownMs` recibe `hasFenceThatCanStillFire`, respondido por el **predicado nuevo `isInsideAnyOwnedFence`** — una geocerca solo puede emitir un EXIT mientras el móvil siga DENTRO de ella, medido con **el MISMO `config.geofenceRadiusFor`** con el que la valla se registró, acolchado por la accuracy del propio fix. Fuera de toda valla propia, el amortiguador se retira **sea cual sea la racha**. **Dos puertas y no una** porque cada incidente lo cura una mitad distinta, y los tests lo dicen: los aborts del Oppo estaban a 39 min pero a 11 m de su pin (solo el decay lo salva), los del Redmi a 25 s pero 389 m fuera de su valla (solo el gate de valla lo salva); y la tormenta del 08-13 —18 s de cadencia, 36 m dentro de una valla de 109 m— **no satisface ninguna escapatoria y sigue amortiguando**. Un fix de abort desconocido **falla ABIERTO** (sin cooldown): un amortiguador que falla abierto cuesta un puñado de despertares, uno que falla cerrado cuesta una plaza. **Agujero residual DECLARADO**: 3 aborts rápidos DENTRO de una valla que luego no entrega su EXIT sigue siendo ciego — la cura es abaratar el despertar, no suprimirlo, y es su propio ticket | `SentryWakeCooldown.kt:22-49` (**las dos premisas, ahora inputs**, con los dos incidentes escritos), `:65-94` (`nextSentryWakeAbortStreak` + `msSinceLastAbort`; el gate de cadencia en `:92`; `null` arranca en 1, `:76-79`), `:96-133` (**`isInsideAnyOwnedFence`**: KDoc que lo declara PREDICADO y no veredicto invocando DET-VERDICT-NOT-PREDICATE-001 `:101-104`, mismo resolver de radio `:106-110` + `:126`, `null ⇒ false` con su asimetría razonada `:112-115` + `:124`, acolchado por accuracy `:131`), `:135-160` (`sentryWakeRearmCooldownMs(hasFenceThatCanStillFire)`, el `return 0L` en `:153`) · CDS `:168-173` (`lastSentryWakeAbortAtMs`, EN MEMORIA junto a la racha que fecha), `:1082-1096` (el hueco medido + skew → null), `:1097` (estampado condicionado a racha > 0), `:1098-1112` (el gate de valla + el cooldown), `:1124-1131` (**la línea de diagnóstico nueva**: la racha ganó su silencio y el gate se lo negó) · `Config:572-583` (`sentryWakeStreakDecayMs` = 10 min, con su razonamiento de orden de magnitud), `:1149-1157` (require: el decay debe caber DENTRO de la escalada) | `lastSentryWakeAbortAtMs` (CDS `:173`) — NUEVO, en memoria por proceso, deliberado: ambos amortiguan una tormenta que solo existe en un proceso residente vivo | ✅ `SentryWakeCooldownTest` **10 → 24 tests**: cadencia `:131` (predecesor más viejo que el decay → racha fresca), `:144` (frontera exacta del decay), `:158` (sin predecesor arranca en 1); geometría `:174` (fix sobre el pin → dentro), `:185` (lejos de toda valla → fuera), `:197` (basta UNA de varias vallas), `:212` (sin sesión aparcada → fuera), `:217` (**sesión sin fix → fuera, el fallo-abierto**), `:230` (acolchado por la accuracy del fix); y **las cuatro decisiones REALES de aquella noche**: `:291` (la tormenta del 08-13 SIGUE amortiguando), `:313` (el paseo del Oppo a las 22:06 junto a su coche SIGUE amortiguando), `:333` (**el Oppo NO se ciega** con sus tres aborts repartidos en 52 min), `:356` (**el Redmi NO se ciega** estando ya fuera de su única valla), más `:274` (sin valla que pueda disparar, jamás cooldown por larga que sea la racha). 1.347 tests | **ACOTA la colisión de doctrina §5.1** («sentry-cooldown vs todo trigger dispara siempre»): la licencia del amortiguador deja de ser una suposición escrita en un comentario y pasa a ser una **precondición comprobada**. La colisión no desaparece —el agujero residual declarado la mantiene viva— pero cambia de naturaleza: ver §5 abajo. **Ambas puertas solo QUITAN quiet periods**, así que la exposición es batería y tasa de telemetría, **nunca un camino nuevo de falso positivo**. Toca a DET-SAFETY-NET-001 por el flanco contrario al esperado: el backstop que justificaba la supresión **también falló** esa noche (heartbeat exacto muerto desde las 18:28 + rejilla de 15 min que se tragó el viaje) — dato que el 09 §14.2 necesita. `isInsideAnyOwnedFence` **duplica literalmente** la expresión de `EvaluateArEnterArmUseCase.kt:93` (distancia ≤ radio + accuracy del fix) y **diverge** de la de `EvaluateSafetyNetCheckUseCase.kt:188-190` (sin acolchado): solape nuevo **3-h**, abajo |

### Adjudicación a los censos del doc (sin reescribirlos)

- **§2 clases de fallo:**
  - **DET-PROMPT-STATES-ITS-REASON-001 → G14** «Observabilidad/forense — sin traza no hay fixture»
    (7º miembro; el enunciado del grupo es literalmente su ticket). *Cruces:* con **G5** (el viaje
    del 20-08 que lo motiva PERDIÓ el parking, y el veto que lo perdió no tenía nombre) y con
    **G15** (cumple una regla permanente del proyecto, no una geometría). [Adjudicación de este
    addendum, **NO VERIFICADA** con el user.]
  - **DET-EVIDENCE-MUST-NOT-LOWER-CONFIDENCE-001 → G5** «Pérdida total del aparcamiento (FN)»
    (15º miembro: la sesión no podía puntuar, luego no podía preguntar, luego no podía terminar).
    *Cruces:* con **G7** (sesión zombie de 102 min) y con **G14** (el hallazgo lo produjo la
    telemetría: «la única sesión con AR tuvo cero candidates»). [**NO VERIFICADA** con el user.]
  - **DET-BT-DISCONNECT-WITHOUT-RIDE-001 → G4** «Evento stale/espurio con autoridad — el evento
    nomina, nunca confirma» (11º miembro, y el caso más puro del grupo: un `ACL_DISCONNECTED`
    CONFIRMABA). *Cruces:* con **G8** (BT como árbitro de atribución — la posición era correcta y
    la atribución falsa), con **G10** (un blip de 13 s sostenía FGS + GPS 15 minutos) y con **G13**
    (una sesión a 0,85 de un viaje que nadie hizo). [**NO VERIFICADA** con el user.]
  - **DET-COOLDOWN-MUST-NOT-BLIND-A-DRIVE-001 → G5** (16º miembro: dos FN el mismo día en dos
    móviles). *Cruces:* con **G9** «Pérdida por proceso/OS» (el heartbeat exacto muerto desde las
    18:28 y la rejilla de 15 min son OS, no algoritmo) y con **G1** «El ancla sigue al peatón» (el
    pin del Oppo acabó DENTRO de la casa: al recuperarse tarde, el cuerpo ya era el ancla).
    [**NO VERIFICADA** con el user.]
- **§3 políticas:**
  - **3-a «DriveProof»** — gana una cláusula por vía inesperada: `DET-BT-DISCONNECT-WITHOUT-RIDE-001`
    mete el carril **Bluetooth** bajo el mismo enunciado («solo el movimiento medido confirma»),
    pero con una métrica que ningún otro miembro usa: **duración de conexión**, no cinemática.
    Eso NO lo hace miembro de 3-a — el verificador único de 3-a habla de fixes y bandas de
    velocidad — sino que registra que la doctrina tiene ahora **dos implementaciones legítimamente
    distintas** según lo que el carril pueda medir. Anotado para que el refactor no intente fundir
    `evaluateEngagement` dentro de `DriveProof`.
  - **`isInsideAnyOwnedFence` NO se eleva a política**: es una función, no una familia — el patrón
    ya establecido (`SentryWakeCooldown.kt`, `UserStopQuietPeriod.kt`, `VehicleFenceOwnershipPolicy.kt`,
    `HumanPoweredRide.kt`, `SpeedBandClock.kt`). Pero **sí abre solape**:
  - **3-h (NUEVO solape registrado, no elevado a política)** — «¿está el móvil dentro de su propia
    valla?». Tres sitios lo calculan hoy, con **dos fórmulas distintas**:
    (i) `EvaluateArEnterArmUseCase.kt:91-95` → `distancia <= radio + fix.accuracy`;
    (ii) `isInsideAnyOwnedFence` (`SentryWakeCooldown.kt:126-131`) → **la MISMA expresión, escrita
    otra vez**; (iii) `EvaluateSafetyNetCheckUseCase.kt:186-190` → `distancia <= radio`, **sin
    acolchado**, con su puerta «lejos» aparte (`:198`, `distancia − accuracy > watchdogFarThreshold`).
    (i) y (ii) son duplicación literal y deben fundirse; (iii) **puede** ser deliberada (curar una
    valla es barato, armar no) pero **NO VERIFICADO**: no hay comentario que lo declare. Los tres
    comparten ya el mismo resolver de radio (`config.geofenceRadiusFor`), que es la mitad difícil.
    Candidato al `domain/detection/` del 09 — ver el addendum 22-08 del doc 09, C.1.
  - Las **5 políticas candidatas siguen sosteniéndose**; ninguna cambia de dictamen.
- **§4 inalcanzables/dominados:** los 4 inalcanzables demostrados **se mantienen en 4** (la
  condición ORIGINAL de `BUG-DETECT-310503` —el bono de accuracy gateado en STILL— sigue purgada:
  lo que cambia es que el tag ha ganado una condición NUEVA, no que la vieja resucite). Sin cambios
  en dominados. **Nota**: `fastPathBaseScore` y `fastPathSpeedBonus` **desaparecen del config** —
  primera vez en estos addenda que se BORRAN parámetros; su conducta la absorben `briefRestScore`
  + `activityExitBonus` + los cuatro `require`.
- **§5 colisiones de doctrina:** **no se abre ninguna nueva**, y la §5.1 CAMBIA DE FORMA. La
  colisión era «el sentry-cooldown duerme un nominador, contra *todo trigger dispara SIEMPRE*», y
  su defensa era la división exacta: *solo* duerme `SIGNIFICANT_MOTION` porque otros carriles
  vigilan. `4d1d6716` **no revoca la división** — la CONVIERTE EN COMPROBACIÓN: el amortiguador ya
  no afirma que otro carril vigila, lo verifica, y si no puede se retira. La colisión queda
  **ACOTADA a un agujero residual declarado por escrito** (3 aborts rápidos dentro de una valla que
  luego no entrega su EXIT). Contabilidad honesta: **§5.1 sigue ABIERTA pero deja de descansar en
  una premisa no medida**; §5.2 (watchdog T7 sin re-validación) queda **exactamente** como estaba —
  ninguno de los cuatro commits toca `CPD:829-863`.

### §F · `BUG-DETECT-310503` — el desenlace: **RETIRADA NO; REHABILITACIÓN CON DEMOSTRACIÓN**

Este era el riesgo señalado antes de leer el árbol: `1f7e6cde` borra la vía rápida del scorer, y la
condición ejecutable que el censo atribuía a `BUG-DETECT-310503` era literalmente *«la vía rápida
topa en Medium 0,65 por aritmética (`fastPathBaseScore` 0,50 + `fastPathSpeedBonus` 0,15) y por
tanto nunca auto-confirma»*. Verificado en el árbol actual, esto es lo que pasó — y **no es una
retirada**:

1. **Los cuatro line-refs del censo están MUERTOS.** El tag vivía en `CPC:14-16` (KDoc), `CPC:41`,
   `Config:21` y `Config:27`. Hoy `CalculateParkingConfidenceUseCase.kt` **no contiene el tag en
   ninguna línea** (grep limpio) y los dos parámetros que lo sostenían ya no existen.
2. **La condición vieja se fue de verdad.** El techo «la vía rápida nunca llega a High» **ha sido
   levantado a propósito**: un reposo maduro de 5 min CON vehicle-exit de AR alcanza hoy High
   (0,70 + 0,15 = 0,85). Eso es exactamente lo que el ticket existe para arreglar.
3. **Pero el ESCENARIO DE CAMPO del tag sigue cerrado, y por una condición MÁS fuerte.** El
   incidente era una parada BREVE (entrada de hospital, 2026-05-31: exit + GPS excelente + 8 pasos
   → 0,75 → CANDIDATE → confirm antes de poder descartar). Bajo el scorer nuevo esa parada cae en
   el tier `briefRestScore` y su máximo absoluto es `0,35 + 0,05 + 0,05 + 0,15 = 0,60` — Medium,
   abre el prompt, jamás auto-confirma.
4. **La condición ejecutable NUEVA está declarada, no es un accidente aritmético.** Vive en dos
   sitios y ambos llevan el tag o su razón por escrito:
   - `Config:21-26` — el KDoc de `briefRestScore` **cita `[BUG-DETECT-310503]` explícitamente**:
     *«low enough that even with every bonus it cannot reach `highConfidenceThreshold` (asserted in
     `init`): a brief stop opens the prompt, never an auto-confirm»*.
   - `Config:901-909` — `require(briefRestScore + allBonuses < highConfidenceThreshold)`, con el
     mensaje de fallo *«It opens the prompt, never an auto-confirm»*; y su hermano
     `require(slowPath3MinScore + allBonuses < highConfidenceThreshold)` («la fase candidate
     pertenece a un reposo MADURADO»). El propio comentario `Config:896-899` declara el cambio de
     régimen: *«They used to hold by arithmetic accident (0.50 + 0.15 < 0.75) with nothing to
     defend them, so a weight tweak could have turned a brief drop-off stop into a silent
     auto-confirm»*.
   - Ejecutable en producción: el `init` corre en **cada** construcción de `ParkingDetectionConfig`,
     que Koin hace en el arranque real, no solo en tests.
5. **La demostración está escrita y es reproducible.** Test propio nuevo:
   `CalculateParkingConfidenceUseCaseTest:183` *«a brief stop with an AR vehicle exit still cannot
   auto-confirm»*, cuyo comentario dice *«The behaviour the old fast path protected, kept»* y
   escribe la aritmética entera (0,60). Más el barrido de propiedad de `:137`.

**Dictamen para el censo**: `BUG-DETECT-310503` **sale de la fila «frontera guard/histórico»
(1 → 0) y entra en «guard-código vivo» (135 → 136)**. La regla sagrada del plan —ninguna condición
desaparece sin demostración escrita— **se cumple**: la condición cambió de forma (de un techo
estructural por accidente a un invariante declarado con `require` + test), y el cambio viene con su
razonamiento en el KDoc, en el mensaje del `require`, en el test y en el cuerpo del commit. Lo que
SÍ hay que corregir en la ficha del §1.2 son sus cuatro line-refs y la frase «SUPERSEDIDO PARCIAL
por DET-SOLID-001 C1: la condición que el tag guardaba YA NO EXISTE» — hoy vuelve a existir, con
otro cuerpo.

⚠️ **Doc-rot heredado, verificado y NO arreglado por el commit** (registrado como bug **#8** en el
doc 11): los tres tests viejos de la sección «Fast path» del mismo fichero
(`CalculateParkingConfidenceUseCaseTest:22-32`, `:34-44`, `:46-57`) **siguen pasando pero sus
comentarios ya no describen el código**. El primero —justo el que el censo del §1.2 cita como «el
test que fija el tag», con la anotación `0.50 + 0.15 = 0.65 → Medium: the fast path's ceiling by
design [BUG-DETECT-310503]`— evalúa hoy `0,35 + 0,05 + 0,05 + 0,15 = 0,60`, y su comentario in-line
*«good GPS — irrelevant: no accuracy bonus exists in the fast path»* es directamente falso (el bono
de accuracy **sí** se aplica ahora). Impacto: **documental, no de conducta** — pero la línea que el
censo usa como demostración del guard cita aritmética inexistente, que es precisamente el modo de
fallo contra el que este documento existe.
