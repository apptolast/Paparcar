# 11 — Bugs encontrados durante el refactor (registro, NO fixes)

> 📌 **Citas de línea ancladas a master `2288468e` (2026-08-24), el commit de arranque de F6.**
> A partir de aquí la Fase 1 mueve símbolos y los números se desplazan: son *a fecha de*, no punteros
> vivos. Para resolver cualquiera con exactitud: `git show 2288468e:<fichero>`. [P0.5]

> Regla del plan (§7.4): los bugs hallados durante el refactor se anotan aquí y se arreglan
> APARTE — ticket propio (`nuevo-ticket`), nunca mezclados con commits de refactor.
> Estado: `ABIERTO` (sin adjudicar) · `ADJUDICADO-BUG` (el user confirma que es bug) ·
> `ADJUDICADO-INTENCIÓN` (comportamiento deseado, se documenta) · `FIXED` (con hash).
>
> **Línea-base de los line-refs: master `4d1d6716` (2026-08-22), CPD 3052 líneas** — verificados
> extrayendo ese commit con `git show`, no leyendo el árbol de trabajo. Revisión 22-08 de los siete
> bugs previos: **ninguno arreglado, ninguno invalidado**; el #1 sigue sin deriva, el #6 deriva por
> primera vez, y el **#7 se comprobó explícitamente contra `d7438173` y NO está resuelto**. Dos
> bugs nuevos verificados: **#8** (la demostración escrita de `BUG-DETECT-310503` cita aritmética
> inexistente) y **#9** (tres cálculos de «dentro de mi valla» con dos fórmulas y sin declarar la
> divergencia).

| # | Fase | Hallazgo | Estado |
|---|------|----------|--------|
| 1 | F3 | `ReleaseActiveParkingSessionUseCase` no comprueba `privateZoneId` antes de publicar el spot comunitario ni resetea `DepartureEventBus`; su gemelo `ProcessConfirmedDepartureUseCase` hace ambas cosas (el reset del bus vive DENTRO de Process, confirmado por el user 19-08). Consecuencia: soltar desde la UI una sesión aparcada en zona privada publicaría la plaza. Bloquea la parametrización del par P1 (07 §1). **Re-verificado 21-08**: `Process` gatea `privateZoneId == null` en TRES puntos (`:84`, `:123`, `:147`) y resetea el bus (`:139`); `Release` no hace ninguna de las dos (grep sin coincidencias en todo el fichero). **Ampliación 21-08 (post-`26aec131`, line-refs SIN deriva pese a que el fichero creció de 95 a 154 líneas)**: (i) `Process` tiene ahora DOS ramas por `DepartureProof` y el `departureEventBus.reset()` vive SOLO en la rama `Witnessed` (`:139`, dentro de `:131-151`) — una salida `Deduced` (`:104-128`) no lo resetea, lo cual es coherente (nada se ha cerrado aún) pero deja el invariante repartido por proof; (ii) **TERCERA instancia de la MISMA omisión**: `FinalizeDeducedDepartureUseCase` completa exactamente la transición de la rama `Witnessed` —publica con TTL completo (`:61-73`), `clearActiveParkingSession` (`:75-81`), `removeGeofence` (`:82`)— y **NO resetea el bus**: ni siquiera lo recibe en el constructor (`:35-40`). Sí gatea `privateZoneId` (`:61`), así que le falta solo la mitad «bus». El «par» Release↔Process es hoy una **familia de cuatro** (Process×2 ramas · Release · Finalize · Retract) y el núcleo común debe parametrizarse por `(sesión, razón, proof)`. **Re-verificado 22-08 (master `4d1d6716`): INTACTO y sin deriva alguna** — ninguno de los cuatro commits del rango toca `ProcessConfirmedDepartureUseCase`, `ReleaseActiveParkingSessionUseCase`, `FinalizeDeducedDepartureUseCase` ni `RetractDeducedDepartureUseCase`, así que `:84`/`:123`/`:147`/`:139`, las dos ramas `:104-128`/`:131-151` y `Finalize:35-40`/`:61-73`/`:75-81`/`:82` siguen exactos. Es el único de los siete bugs cuyos line-refs no se han movido en tres addenda seguidos. | **ADJUDICADO-BUG** (user, 21-08) — fix = núcleo común CON gate de zona privada (09 §14.6); ticket propio. **Alcance ampliado 21-08**: 4 llamadores, no 2 (09 addendum 21-08 §C.5) |
| 2 | F3→F4 | `EvaluateHonestCloseUseCase.kt:371` calcula su zona sin techo, y NINGÚN consumidor lo aplica después — **verificado en F4 y RE-verificado 19-08 tras `bf92070c`** (el commit desplazó la escalera ~+52 líneas pero no tocó el cálculo): cadena `EvaluateHonestCloseUseCase.kt:371-372` → `RunHonestCloseUseCase.kt:105/115` → `ConfirmParkingUseCase.kt:100/255` → Room. `unattendedZoneMaxRadiusMeters` (250 m, `ParkingDetectionConfig:580`) solo se aplica en `CoordinatorParkingDetector.kt:1592-1595` (`saveUnattendedZone`), camino por el que el honest-close nunca pasa. Radio efectivo = `max(accuracy_abortFix, honestCloseMinZoneRadius)` SIN cota superior: un fix con accuracy de p.ej. 800 m guardaría una zona de 800 m. **El gate nuevo DET-UNWITNESSED-DISPLACEMENT-001 (`EvaluateHonestCloseUseCase.kt:267-286`) NO mitiga este caso**: refuta por velocidad IMPLÍCITA, no por accuracy — y las accuracies SUMAN al allowance (`acc_testigo + acc_abort + edad×15 m/s`, `:277-278`), así que un fix de accuracy enorme ENSANCHA la tolerancia en vez de estrecharla: con velocidad implícita baja (fix indoor pegado al testigo) o sin testigo estampado (instalación fresca / skew) sigue colando la zona de 800 m. La rama measured-driving (`:244-246`) tampoco tiene techo (hoy defensivamente inalcanzable). **Evidencia añadida 21-08**: (i) el daño es de UI, NO operativo — el radio de zona no alimenta la geocerca, que usa `geofenceRadiusFor(sizeCategory, accuracy)` con su propio tope (`ParkingDetectionConfig:1129-1138`); (ii) el techo se concibió GLOBAL y el config lo demuestra: `require(unattendedZoneMaxRadiusMeters >= honestCloseMinZoneRadiusMeters)` (`:1008-1010`) ata el techo al suelo DEL CIERRE HONESTO, lo que solo tiene sentido si ambos acotan el mismo radio. **Re-anclaje 21-08 (master `bef70ec7`, CPD 2998)**: ninguno de los cuatro commits nuevos toca `EvaluateHonestCloseUseCase` (383 líneas, **delta 0**), así que `EvalHC:371-372`, `:267-286`, `:277-278` y `:244-246` siguen EXACTOS y el bug está intacto. Los refs de `Config` SÍ derivan (+44 líneas): `unattendedZoneMaxRadiusMeters` `:580`/`:590` → **`:609`** (KDoc **`:604-608`**) · el `require` `:1008-1010` → **`:1052-1054`** · `geofenceRadiusFor` `:1129-1138` → **`:1173-1182`** · `honestCloseMinZoneRadiusMeters` sigue en **`:588`**. Y el ÚNICO caller que aplica el techo, `saveUnattendedZone`, de `CPD:1501-1504`/`:1592-1595`/`:1606-1615` → **`CPD:1777-1780`**. **Re-anclaje 22-08 (master `4d1d6716`, CPD 3052)**: `EvaluateHonestCloseUseCase` sigue en **383 líneas, delta 0 por SEGUNDO addendum consecutivo**, así que `EvalHC:244-246`, `:267-286`, `:371-372` y `:374-380` siguen EXACTOS y el bug está intacto. Derivan los de `Config` (+72) y el único caller que aplica el techo: `honestCloseMinZoneRadiusMeters` `:588` → **`:604`** · `honestCloseMaxImpliedTravelSpeedMps` `:603` → **`:619`** · `unattendedZoneMaxRadiusMeters` `:609` (KDoc `:604-608`) → **`:625`** (KDoc **`:620-624`**) · el `require` `:1052-1054` → **`:1109-1111`** · `geofenceRadiusFor` `:1173-1182` → **`:1245-1254`** · `saveUnattendedZone` `CPD:1777-1780` → **`CPD:1793-1796`** (`minOf` en `:1793`, `maxOf` en `:1795`). **Refuerzo del dictamen**: `1f7e6cde` acaba de hacer EXACTAMENTE lo que este fix pide, en el fichero de al lado — `Config:896-919` convierte cuatro techos que se sostenían «por accidente aritmético» en `require` declarados con su razón escrita en el mensaje de fallo. Ya no hay que inventar la forma del fix: hay plantilla en master. ⚠️ **AVISO DE CADUCIDAD**: `f42e393b` [DET-CLOSE-ZONE-WHEN-THE-BODY-WALKED-001], ya en master pero POSTERIOR a esta línea-base, **modifica `EvaluateHonestCloseUseCase` (383 → 465 líneas)**. Todos los refs de EvalHC de esta fila caducan al leer ese commit, y **NO VERIFICADO** si lo arregla, lo agrava o no lo toca. | **ADJUDICADO-BUG** (user, 21-08) — fix = `physics/HonestZoneRadius.kt` única (suelo+techo) en TODOS los caminos + marca de recorte (09 §14.5); ticket propio. **Re-leer contra `f42e393b` antes de abrirlo** |
| 3 | F3 | `sessionOutcome` se discrimina por PREFIJO/literal de string en vez de por el owner `DetectionSessionOutcomes` — contrato frágil: un outcome nuevo que no siga la convención de nombre cambia el comportamiento en silencio. **Actualización 20-08 (post-`1d8f7264`, line-refs re-verificados)**: el contrato por convención codifica ya TRES conductas repartidas en tres consumidores sin dueño común — (a) `confirmed_*` por PREFIJO: `savedOk = ended && sessionOutcome.startsWith("confirmed_")` (`CoordinatorParkingDetector.kt:1633`; la línea `:1520` citada originalmente ya iba desplazada); (b) `aborted_false_enter`/`aborted_no_movement` por igualdad EXACTA contra las constantes: extienden el streak del sentry (`SentryWakeCooldown.kt:47-48`, fold en `CoordinatorDetectionService.kt:1045-1053`) y disparan el honest-close (filtro `CoordinatorDetectionService.kt:856`, aliases del companion `:1626-1629`); (c) `stopped_by_user` (nuevo, `DetectionSessionOutcomes.STOPPED_BY_USER`): RESETEA el streak cayendo en el else del fold (`SentryWakeCooldown.kt:49`; deliberado, KDoc `:28-32`; test `SentryWakeCooldownTest:76`) y NO dispara honest-close — no está en el par de `:856`, y además su vía termina en cancelación, que ni siquiera invoca `maybeRunHonestClose()` (solo corre tras retorno normal, `CoordinatorDetectionService.kt:1392-1406`). Cada conducta nueva obliga a auditar a mano los tres consumidores — refuerza la adjudicación: tipar `SessionOutcome` (09 §13/§B.3) con membership declarada (`isConfirmed`/`triggersHonestClose`/`extendsSentryStreak`/`resetsSentryStreak`). **Re-anclaje 21-08 (master `bef70ec7`)**: (a) `savedOk` `CPD:1633` → **`CPD:1796`**; (b) `SentryWakeCooldown.kt:47-48` **sin deriva**, fold en `CDS:1045-1053` → **`CDS:1071-1082`**, filtro del honest-close `CDS:856` → **`CDS:882`**, aliases del companion `CDS:1626-1629` → **`CDS:1664-1667`**; (c) la vía cancelada que ni invoca `maybeRunHonestClose` `CDS:1392-1406` → **`CDS:1427-1433`**. **Refuerzo nuevo (`16d3ea69`)**: el outcome `aborted_unattended_human_powered` gana un SEGUNDO productor (el cierre temprano, `CPD:2125`, junto a los dos del response-timeout `CPD:1421`/`:1426`) sin ninguna marca que los distinga — la convención de nombre pasa de codificar tres conductas a codificar además la PROCEDENCIA, que es la fuga del bug #7. **Re-anclaje 22-08 (master `4d1d6716`)**: (a) `savedOk` `CPD:1796` → **`CPD:1812`**; (b) **`SentryWakeCooldown.kt` se desplaza mucho por primera vez** (65 → 160 líneas): el `when` del fold `:41-50` → **`:87-94`** y las dos igualdades EXACTAS contra las constantes `:47-48` → **`:89-90`**; el objeto `DetectionSessionOutcomes` → **`:54-63`** (el KDoc de `STOPPED_BY_USER` `:28-32` → **`:58-62`**); el fold en el service `CDS:1071-1082` → **`CDS:1079-1096`**; el filtro del honest-close `CDS:882` → **`CDS:890`**; los aliases del companion `CDS:1664-1667` → **`CDS:1705-1708`**; (c) la vía cancelada que ni invoca `maybeRunHonestClose` `CDS:1427-1433` → **`CDS:1466-1474`**. **Hallazgo nuevo (`4d1d6716`)**: el fold pasa de 3 ramas a **4**, y la cuarta —el gate de cadencia, `SentryWakeCooldown.kt:92`— **NO es membership por nombre**: compara tiempos. El `when` mezcla ahora dos criterios de naturaleza distinta. Eso **no agrava** el bug (no añade una cuarta conducta atada a un string) pero refuerza el fix por TIPADO: al declarar la membership, el `when` queda partido en «pertenencia declarada» + «cadencia medida», que es lo que hoy hay que deducir leyendo. | ABIERTO — reforzado post-`1d8f7264`, post-`16d3ea69` y post-`4d1d6716` |
| 4 | F3 | Nota de diseño (no bug de comportamiento): `creepWindow`/`recentFixes` viven sueltos en el estado del coordinator y son absorbibles por el concepto DriveProof (07 §3). Se resuelve en la arquitectura objetivo (09), no como fix. **Actualización 21-08**: `DriveProof` tendrá que poseer más de lo previsto — a los dos rings se suman ahora DOS relojes de banda (`drivingBandMs`/`lastBandFixTimestampMs` de DET-MOTOR-PROOF-001 y `motorBandMs`/`lastMotorBandFixTimestampMs` de DET-MOTORWAY, `CPD:405-407`), que ya comparten su aritmética en la función pura `SpeedBandClock.creditSpeedBand` (`SpeedBandClock.kt:27-37`). Eso es evidencia A FAVOR del dueño único, no en contra: el predicado ya salió del coordinator por su cuenta. **Re-anclaje 22-08 (master `4d1d6716`)**: `recentFixes` → **`CPD:365`** · `drivingBandMs`/`lastBandFixTimestampMs` → **`CPD:386`**/**`:390`** · `motorBandMs`/`lastMotorBandFixTimestampMs` `CPD:405-407` → **`CPD:407-416`** (KDoc `:407-413`, campos `:414` y `:416`) · `creepWindow` → **`CPD:616`** · `SpeedBandClock.creditSpeedBand` **`:27-37`, sin deriva**. **Ampliación 22-08**: a `DriveProof` hay que sumarle ahora un cuarto tipo de inquilino, porque `1f7e6cde` mete un contador de FRESCURA (`stepsAtLastDiscard`, `CPD:333-338`, con su derivado `freshStepCount` en `:418-421`) cuyo criterio de limpieza es **el mismo que el de los relojes de banda y el de `walkFixesSinceDriving`: solo la conducción medida** (`CPD:2765-2767`). Es decir: la frontera real no es «rings vs relojes» sino **«todo lo que la conducción medida resetea»**, y eso incluye estado que hoy vive repartido entre `DriveProof` y `EgressEvidence`. El diseño F4 tiene que decidir explícitamente quién posee el CRITERIO de reseteo, no solo los campos. | ABIERTO — diseño F4 |
| 5 | F4 | Instancia concreta del contrato frágil del #3: el outcome `aborted_no_movement_jam` (`CoordinatorParkingDetector:987`) no casa con el patrón `ABORTED_NO_MOVEMENT`, así que el fold por atasco NI dispara el honest-close (`CoordinatorDetectionService:818`) NI incrementa el streak del sentry-cooldown (`SentryWakeCooldown.kt:41-42`). **RESUELTO el "NO VERIFICADO" (21-08)**: la etiqueta distinta es DELIBERADA y provisional — `CoordinatorParkingDetector.kt:1071-1074` lo dice literal (*«Distinct outcome + telemetry when the extension ran: field data sizes this cohort … before deciding whether it deserves a nudge»*). **Y la cohorte se midió**: 0 casos de `aborted_no_movement_jam` en 1.359 sesiones / 4 uids / 13-08→21-08 (COUNT agregado; control de validez: 28 `aborted_no_movement` en OPPO cuadran con el recuento manual). Las 55 `aborted_no_movement` pliegan todas a ~4,1 min o ~1,4 min, ninguna a ~10 min ⇒ la extensión nunca llegó a armarse. ⚠️ **Caveat NO VERIFICADO**: una extensión que SÍ rescató el viaje no deja evento remoto (solo log local), así que «0 folds» no prueba «la extensión nunca ayudó» — para dimensionarlo hay que emitir telemetría al EXTENDER, no solo al plegar (requisito anotado en la propuesta 3, 09 §14.4-bis). **Re-anclaje 21-08 (master `bef70ec7`)**: el outcome `aborted_no_movement_jam` `CPD:987` → **`CPD:1215`**; su rationale «Distinct outcome + telemetry when the extension ran» `CPD:1071-1074` → **`CPD:1212-1216`**; el filtro del honest-close `CDS:818` → **`CDS:882`**; el fold `SentryWakeCooldown.kt:41-42` → **`:47-48`** (la rama que lo excluye sigue siendo el `else` de `:49`). Ninguno de los cuatro commits nuevos puebla la cohorte ni toca el fold. **Re-anclaje 22-08 (master `4d1d6716`)**: el outcome `aborted_no_movement_jam` `CPD:1215` → **`CPD:1229`** · su rationale `CPD:1212-1216` → **`CPD:1226-1230`** · el filtro del honest-close `CDS:882` → **`CDS:890`** · el fold `SentryWakeCooldown.kt:47-48` → **`:89-90`** (la rama que lo excluye sigue siendo la de esas dos igualdades, que devuelve 0). **La cohorte sigue VACÍA**: ninguno de los cuatro commits del rango la puebla, y el caveat sigue sin cerrarse (una extensión que SÍ rescató el viaje no deja evento remoto). Lo único que cambia es que el fold gana una rama de cadencia — ver bug #3. | **ADJUDICADO** (user, 21-08) — la exclusión se MANTIENE (indecidible con datos); el fix es por TIPADO (membership declarada del outcome, bug #3), no por conducta |
| 6 | F4-adj | **Asimetría del testigo cinemático en el egress-birth** (hallazgo al adjudicar la propuesta 7). El sabor MÓVIL graba el nacimiento con `stepCount > 0` **o** `newKinematicEgressFixes > 0` (`CoordinatorParkingDetector.kt:2693-2694`); el sabor PARADO exige `stepCount > 0` a secas (`:2503`). Con el contador de pasos MUDO o CONGELADO ([DET-STEP-SENSOR-REDMI-001], [DET-FROZEN-COUNTER-001]) el testigo cinemático acumulado en fixes móviles NO graba el nacimiento si el siguiente fix entra clasificado como parado: el nacimiento espera al próximo fix móvil y queda MÁS LEJOS del coche, debilitando `egressBornAtAnchor` y pudiendo desplazar el pin. La otra diferencia entre sabores (`!shouldClearBestStop`) SÍ es explicable — solo un fix móvil puede resolver como COCHE y borrar el ancla. **Re-verificación 21-08 contra `bef70ec7` (CPD 2699 → 2998, +299): los line-refs NO han derivado ni una línea.** Sabor MÓVIL en **`CPD:2693-2694`** (aplica en `:2734-2743`, ramas `shouldClearBestStop -> null / 0`), sabor PARADO en **`CPD:2503`** (aplica en `:2550-2551`), `newKinematicEgressFixes` en `:2683-2684`, `shouldClearBestStop` en `:2668`. Motivo: las +299 líneas de los cuatro commits caen íntegramente FUERA de ese tramo (el grueso está en el collect, `:868-1533`, y en `evaluateConfidence`, `:2761+`). El bug está intacto y sigue sin dato que lo confirme ni lo descarte. **Re-anclaje 22-08 (master `4d1d6716`, CPD 3052): por primera vez en tres addenda los line-refs SÍ derivan**, porque `1f7e6cde` inserta la contabilidad de frescura por encima del tramo (+51): sabor **PARADO** `CPD:2503` → **`CPD:2554`** (aplica `:2550-2551` → **`:2601-2602`**) · sabor **MÓVIL** `CPD:2693-2694` → **`CPD:2744-2745`** (aplica `:2734-2743` → **`:2788-2796`**) · `newKinematicEgressFixes` `:2683-2684` → **`:2734-2735`** · `shouldClearBestStop` `:2668` → **`:2719`**. **La asimetría es idéntica**: PARADO sigue exigiendo `stepCount > 0` a secas (`:2554`), MÓVIL sigue aceptando `stepCount > 0` **o** `newKinematicEgressFixes > 0` (`:2744-2745`). **Matiz nuevo verificado**: los dos sabores leen `stepCount` **TOTAL**, no el `freshStepCount` que `1f7e6cde` introdujo — y es CORRECTO que así sea (el nacimiento del egress es un hecho histórico, no una alegación de confirm), pero significa que el mismo contador tiene ya **tres semánticas** en el árbol (total para el ancla y el veredicto desatendido · fresco para el confirm · total otra vez para el nacimiento del egress). `state/EgressEvidence` tendrá que declararlas, no heredarlas. | ABIERTO — **NO VERIFICADO** que la combinación se dé en la práctica ni que no esté dominada por otras ramas; resolver con replay dirigido o dato de campo. La unificación adjudicada (09 §14.7) preserva la asimetría como parámetro `acceptsKinematicWitness`, así que el refactor NO la arregla ni la esconde |
| 7 | F4-adj (21-08) | **El veredicto de cierre temprano se hace pasar por el timeout de 15 minutos.** `16d3ea69` creó una vía TERMINAL nueva —la parada madura de un viaje muscular, que resuelve a los ~5 min— y la conectó al efecto que ya existía: `closeHumanPoweredRide` (`CPD:2115-2126`) llama a `nudgeUnattended(UnattendedSaveReason.HUMAN_POWERED, …)` en `CPD:2125`. Pero `nudgeUnattended` (`CPD:1820-1839`) estampa un `pathLabel` **literal**: `DetectionEvent.Decision(outcome = reason.decisionOutcome, pathLabel = "unattended_timeout")` (`CPD:1834`). Sus otros dos llamadores (`CPD:1421`, `:1426`) SÍ están dentro de la rama del response-timeout (`CPD:1343-1455`). Resultado: en telemetría remota las dos vías emiten **exactamente el mismo par** `outcome = UNATTENDED_HUMAN_POWERED_NUDGE` + `pathLabel = "unattended_timeout"`, y el `sessionOutcome` también coincide (`aborted_unattended_human_powered`, `CPD:1829` vía `UnattendedSaveReason:24`). **El KDoc del cierre temprano (`CPD:2102-2114`) declara deliberado el reuso del OUTCOME y del `decisionOutcome` —comparabilidad de campo con trazas antiguas, razón legítima— y no dice NADA del `pathLabel`**: la fuga es colateral, no adjudicada. Consecuencias: (i) viola la regla permanente del proyecto «identificar SIEMPRE qué path produjo cada veredicto»; (ii) y es la medición que hace falta para validar el propio fix en campo — «¿cerró de verdad en la parada madura o volvió a colgar hasta el timeout?» es hoy irrespondible desde `diagnostics`. ⚠️ **NO VERIFICADO** que la edad de sesión desambigüe: la segunda puerta del cierre temprano (`CPD:2025-2032`, para el sello AR que llega tarde) puede dispararse pasados los 15 min, así que el reloj tampoco separa las dos vías con garantía. Impacto: **observabilidad, NO conducta** — ninguna decisión cambia. **COMPROBADO 22-08 contra master `4d1d6716`: `d7438173` NO lo resuelve.** Se verificó explícitamente porque era el candidato obvio: `nudgeUnattended` (`CPD:1820-1839` → **`CPD:1836-1855`**) **sigue estampando el literal**, hoy en **`CPD:1850`** (`pathLabel = "unattended_timeout"`), y sus tres llamadores siguen produciendo trazas indistinguibles: `CPD:1421`/`:1426` → **`CPD:1435`**/**`:1440`** (response-timeout) y `CPD:2125` → **`CPD:2176`** (cierre temprano). `closeHumanPoweredRide` `CPD:2102-2126` → **`CPD:2166-2190`**; su KDoc `CPD:2102-2114` → **`CPD:2154-2165`**. La ironía queda anotada: el KDoc de `nudgeUnattended` (**`CPD:1826-1835`**) razona que el `sessionOutcome` se deriva de `UnattendedSaveReason` *«so a future branch cannot invent a fourth spelling»* — el eje que SÍ se protegió — dos líneas por encima del `pathLabel` que sí es un literal. **Lo que SÍ cambia es la fuerza del dictamen**: el defecto gemelo está resuelto en master con su diseño escrito (`d7438173`: la causa cabalga la columna `reason`, el `outcome` NO se renombra porque invalidaría trazas guardadas, el `pathLabel` NO se contamina porque se compara por igualdad). El fix del #7 deja de ser una propuesta y pasa a ser **la aplicación del mismo patrón al otro carril**, con precedente en el árbol. | ABIERTO — re-verificado contra `4d1d6716`; **NO resuelto por `d7438173`**. Fix natural: un `pathLabel` propio (`human_powered_rest` / `unattended_timeout`) como parámetro de `nudgeUnattended`, SIN tocar `outcome` ni `sessionOutcome`. Precedente en master: `d7438173` |
| 8 | F5-adj (22-08) | **La demostración escrita de `BUG-DETECT-310503` cita aritmética que ya no existe.** `1f7e6cde` sustituyó el scorer de dos ramas por uno aditivo, pero **no tocó los tres tests viejos de la sección «Fast path»** de `CalculateParkingConfidenceUseCaseTest`, que **siguen pasando con comentarios falsos**: (a) `:22-32` —justo el test que el censo del 06 §1.2 cita como «el que fija el tag»— anota `0.50 + 0.15 = 0.65 → Medium: the fast path's ceiling by design [BUG-DETECT-310503]` cuando el valor real hoy es `0,35 + 0,05 + 0,05 + 0,15 = 0,60`, y su comentario in-line *«good GPS — irrelevant: no accuracy bonus exists in the fast path»* es **directamente falso** (el bono de accuracy sí se aplica ahora, `CPC:65`); (b) `:34-44` acierta el valor (`0,50 → Low`) por coincidencia, con la derivación equivocada; (c) `:46-57` describe el mecanismo como «fast path skipped → slow path», y ya no hay dos paths. Además el KDoc de clase (`:10-14`) sigue firmado solo por `[DET-SOLID-001][C1]`. Impacto: **documental, NO de conducta** — los tres asserts son correctos. Pero la línea que el censo usa como PRUEBA de un guard cita números inexistentes, que es exactamente el modo de fallo contra el que existe el doc 06 (y el motivo por el que su §F tuvo que reconstruir el desenlace del tag a mano). Fix: reescribir los tres comentarios y renombrar la sección; cero cambios de assert. | ABIERTO — verificado por lectura de `4d1d6716`. Trivial, sin riesgo; candidato a viajar dentro del ticket que arregle el #2 (mismo fichero de config, mismo patrón de `require`) |
| 9 | F5-adj (22-08) | **Tres cálculos de «¿estoy dentro de mi propia valla?» con dos fórmulas distintas, y la divergencia no está declarada.** `4d1d6716` añade `isInsideAnyOwnedFence` (`SentryWakeCooldown.kt:119-133`) → `distancia <= radio + fix.accuracy`. Es **literalmente la misma expresión** que `EvaluateArEnterArmUseCase.kt:91-95` (`distanceMeters <= radiusMeters + fix.accuracy` → `ArmAtCar`), escrita otra vez. Y **diverge** de la tercera: `EvaluateSafetyNetCheckUseCase.kt:186-190` usa `distanceMeters <= radiusMeters`, **sin acolchado por accuracy**, con su puerta «lejos» aparte y con acolchado propio (`:198`, `distanceMeters - fix.accuracy > watchdogFarThresholdMeters`). Los tres resuelven el radio con el MISMO `config.geofenceRadiusFor(sizeCategory, accuracy)` — la mitad difícil ya está compartida; lo que no lo está es la mitad de una línea. Consecuencia potencial: con un fix mediocre a poca distancia del pin, el carril de arm y el amortiguador se consideran DENTRO y el safety net FUERA, sobre la misma valla y en el mismo instante. Puede ser deliberado (curar una valla es barato, armar no) pero **NINGÚN comentario lo dice** — y ese silencio es lo que convierte una asimetría razonada en una discrepancia. | ABIERTO — la divergencia está **verificada** por lectura de `4d1d6716`; **NO VERIFICADO** que produzca daño en campo ni que la asimetría sea intencionada. Registrado también como solape **3-h** en el doc 06 y como casa `domain/detection/FenceContainment.kt` en el 09 (E.1). El fix mínimo es fundir (i) y (ii) y **escribir** por qué (iii) no se acolcha |

---

## ~~Trabajo EN VUELO sobre el mismo terreno~~ — **CERRADO 2026-08-22: los dos están en master**

> ⛔ **Apartado histórico. No hay trabajo en vuelo sobre este terreno a 2026-08-22.** Los dos
> worktrees que se describen abajo se mergearon: `../Paparcar-evidence-floor` →
> **`1f7e6cde`** y `../Paparcar-prompt-reason` → **`d7438173`**, ambos en master por debajo de
> la línea-base `4d1d6716`. Se conserva el texto original porque los riesgos que anticipó son
> ahora hechos verificables, y así se puede contrastar la lectura del diff con el desenlace real:
>
> | Riesgo anticipado | Desenlace verificado en `4d1d6716` |
> |---|---|
> | W.1 punto 1 — el WIP invalidaría los characterization tests que el 09 §12.1 exige ANTES del move | **Se hizo en el orden correcto**: el ticket entró primero y trajo tests propios, incluido uno de **PROPIEDAD** (`CalculateParkingConfidenceUseCaseTest:137`), que fija el invariante en vez de una tabla de valores. El move de F5 arranca desde una base más firme |
> | W.1 punto 2 — «la condición ejecutable de BUG-DETECT-310503 desaparece y el tag hay que readjudicarlo» | **Readjudicado, y en la dirección contraria a la temida**: el tag NO se retira. Su escenario de campo (parada breve en la entrada de un hospital) sigue cerrado por una condición **más fuerte** —`briefRestScore` + `require(briefRestScore + allBonuses < highConfidenceThreshold)`, `Config:26` y `:901-909`— y con test propio (`:183`). Pasa de «frontera guard/histórico» a **guard-código vivo**. Desenlace completo y demostración: **doc 06, §F del addendum 22-08** |
> | W.1 punto 3 — riesgo de que alguien lea el §B de `1c292ac8` como código muerto | **No ocurrió**: el reloj de reposo de §B sigue vivo (`CPD:2837-2855`). Sigue siendo el punto a vigilar en F5 |
> | W.2 punto 1 — cambio de firma de `ParkingDecision.Prompt` + barrido de consumidores | Hecho: `degradeToPrompt` recibe la razón **sin default** (`CPD:2085`), y el §1.7 del 09 se respeta (no se renombra ninguna cadena existente) |
> | W.2 punto 2 — «ambos worktrees modifican el CPD: conflicto garantizado» | **No se materializó**: entraron en commits separados y ordenados (`d7438173` antes que `1f7e6cde`) |
> | W.2 punto 3 — no romper la coincidencia de clave `human_powered` entre las dos familias | **Respetada**: `PromptReason.HUMAN_POWERED` y `UnattendedSaveReason.HUMAN_POWERED` siguen compartiendo clave |
> | W.2 — «son el MISMO defecto en dos sitios; el #7 es el `pathLabel` del nudge» | **Solo se arregló la mitad `prompt`**. El bug **#7 sigue ABIERTO**: `CPD:1850` conserva el literal `"unattended_timeout"`. Lo que gana es precedente: ver la fila #7 |
>
> **Inventario de worktrees a 22-08** (`git worktree list`): quedan `Paparcar-auth-providers`,
> `Paparcar-ios-f0` y `Paparcar-ios-social`, **ninguno sobre el subsistema de detección**.
>
> ⚠️ Nota de corte: master avanzó cuatro commits MÁS durante la redacción de este addendum
> (`f42e393b`, `865f0f8a`, `64e1def0`, `0a0832cf`, 22-08 01:43-01:53) y **no están incorporados**.
> El que más afecta a este documento es `f42e393b`, que modifica `EvaluateHonestCloseUseCase`
> (383 → 465 líneas) — el fichero del bug #2; ver el aviso en su fila.

---

### Texto original del apartado (2026-08-21), conservado como registro

> Dos worktrees con cambios **sin commitear** sobre `bef70ec7` tocan ficheros del subsistema de
> detección. No son parte del refactor ni de estos cuatro commits, pero colisionan con ambos y
> conviene que quede escrito antes de que F5 empiece a mover ficheros. Solo se ha LEÍDO su estado
> (`git status` / `git diff`); no se ha escrito nada en ellos.

### W.1 · `DET-EVIDENCE-MUST-NOT-LOWER-CONFIDENCE-001` — `../Paparcar-evidence-floor`

- **Rama** `bugfix/DET-EVIDENCE-MUST-NOT-LOWER-CONFIDENCE-001-floor-not-ceiling`, al día con master
  (`bef70ec7`), **5 ficheros modificados, 0 commits** (+263/−52).
- **Ticket**: `docs/backlog/det-evidence-must-not-lower-confidence-001.md` — abierto POR `1c292ac8`
  («Follow-up measured and deliberately left out»), ya en master con 79 líneas; el worktree le añade
  otras 66 sin commitear.
- **Qué toca**: `CalculateParkingConfidenceUseCase.kt` · `ParkingDetectionConfig.kt` ·
  `CoordinatorParkingDetector.kt` · `CoordinatorParkingDetectorTest.kt`.
- **Qué hace** (leído del diff): (a) el scorer deja de tener dos caminos mutuamente excluyentes y
  pasa a **un solo score ADITIVO** — el AR vehicle-exit deja de TOPAR el resultado en Medium (0,65)
  y pasa a acortar el GATE y sumar un bono, de modo que «más evidencia» nunca puede bajar la
  confianza; (b) `stepCount` deja de ponerse a 0 al descartar un candidate: se introduce
  `stepsAtLastDiscard` + `freshStepCount`, y EvalPD recibe el FRESCO mientras el veredicto
  desatendido sigue leyendo el TOTAL.
- **Riesgo de colisión — ALTO, y por partida triple:**
  1. **Con el refactor**: `CalculateParkingConfidenceUseCase` es **la clase de la etapa
     `ConfidenceScoringStage`** (09 §3:177 y fila `stages/` del §9). El WIP no la mueve pero la
     reescribe entera, así que los characterization tests que el 09 §12.1 exige ANTES del move
     cambian de valores esperados. Orden correcto: primero este ticket, después el move.
  2. **Con el censo (06 §6)**: la condición ejecutable de **BUG-DETECT-310503** —hoy la frontera
     guard/histórico del censo— es precisamente «la vía rápida topa en Medium y nunca auto-confirma».
     Si el WIP entra, esa condición desaparece y el tag hay que readjudicarlo.
  3. **Con `1c292ac8 §B`**: §B existe PORQUE la vía rápida topaba en Medium (era la puerta que una
     sesión con prompt suprimido no podía cruzar). Con el WIP dentro, la premisa original de
     `16d3ea69` («High ES la parada certificada») vuelve a ser cierta — pero **el reloj de reposo de
     §B NO debe revertirse**: mide en vez de inferir, que es el punto. El riesgo real es que alguien
     lea §B como código muerto y lo quite. ⚠️ **NO VERIFICADO** el efecto neto sobre la etapa
     CANDIDATE: con High alcanzable teniendo AR exit, se abrirán candidates en sesiones donde hoy no
     se abren, y la segunda puerta del cierre temprano (`CPD:2025-2032`) pasa de rara a común.
  - *Continuidad, no colisión*: el punto (b) del WIP arregla literalmente el segundo defecto que
    `16d3ea69` diagnosticó y no arregló («`stepCount` is zeroed on every discard, so the loop could
    never have confirmed anyway»).
- **Adjudicaciones §14 afectadas**: ninguna cambia de DECISIÓN. Sí cambia material del §9/§3 (la
  fila de `ConfidenceScoringStage` pasa a llevar un guard MODIFICADO, no solo mudado) y del §12.1.

### W.2 · `DET-PROMPT-STATES-ITS-REASON-001` — `../Paparcar-prompt-reason`

- **Rama** `bugfix/DET-PROMPT-STATES-ITS-REASON-001-verdict-names-its-cause`; el **ticket ya está
  commiteado** (`ad570156`, encima de `bef70ec7`), el **código es WIP sin commitear** (4 ficheros,
  +102/−18).
- **Qué toca**: `EvaluateParkingDecisionUseCase.kt` · `CoordinatorParkingDetector.kt` ·
  `DetectionEvent.kt` · `DetectionEventDto.kt`.
- **Qué hace**: enum `PromptReason` con **seis causas** (`weak_evidence`, `human_powered`,
  `egress_not_at_anchor`, `anchor_walk_entered`, `anchor_gap_entered`, `implausible_repark`) y
  `ParkingDecision.Prompt(pathLabel, reason)`; el `||` de cinco causas de `EvalPD:265-267` pasa a un
  **first-match ORDENADO** para que dos sesiones de la misma forma reporten siempre la misma razón;
  la clave cabalga la columna `reason` que `HonestClose`/`Released`/`GeofenceRegistration` ya usan y
  el `outcome` sigue siendo `CONFIRM_DEGRADED_PROMPT` a propósito. Campo que lo motiva: 2026-08-20
  23:56, Oppo sesión `1787263007358` — un viaje a 63 km/h que no guardó nada y en el que 2 de sus 3
  veredictos eran inatribuibles.
- **Riesgo de colisión — MEDIO:**
  1. **Con el refactor**: `EvaluateParkingDecisionUseCase` es uno de los **veredictos MANTENER** del
     09 §1.1, y el WIP le cambia la FIRMA de `ParkingDecision.Prompt` — barrido obligatorio de
     consumidores (`degradeToPrompt` en el CPD y sus tests). Es exactamente el tipo de cambio que el
     09 §1.7 protege (contrato de trazas): aquí lo respeta, porque no renombra ninguna cadena
     existente.
  2. **Con W.1**: **ambos worktrees modifican `CoordinatorParkingDetector.kt`** — conflicto
     garantizado si se mergean sin rebasear el segundo sobre el primero.
  3. **Con el bug #7 de arriba**: son el MISMO defecto en dos sitios (la causa de un veredicto que
     no llega a la traza). El #7 es el `pathLabel` del nudge desatendido; este ticket es el `reason`
     del prompt degradado. Merecen el mismo gesto y, si se hace en dos tickets, hay que asegurar que
     usan el mismo vocabulario: `PromptReason.HUMAN_POWERED` y
     `UnattendedSaveReason.HUMAN_POWERED` ya coinciden en clave (`human_powered`) — no romper esa
     coincidencia.
- **Adjudicaciones §14 afectadas**: refuerza **§14.3** (propuesta 3, aprobada entera) — es el cuarto
  precedente vivo del patrón «disposición/causa en una columna que ya existe», tras
  `HonestClose` ampliado, `ARM_SUPPRESSED_USER_STOP` y `GeofenceRegistration(source, failure)`.
  Ninguna otra decisión cambia.
