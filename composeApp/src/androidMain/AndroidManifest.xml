<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos de localización -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Activity Recognition -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Reinicio al arrancar dispositivo (para reactivar acelerómetro) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".PaparcarApp"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="Paparcar"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ParkingDetectionService -->
        <service
            android:name=".detection.service.ParkingDetectionService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- ActivityTransitionReceiver -->
        <receiver
            android:name=".detection.receiver.ActivityTransitionReceiver"
            android:exported="true"
            android:permission="com.google.android.gms.permission.ACTIVITY_RECOGNITION">
        </receiver>

        <!-- BootCompletedReceiver -->
        <receiver
            android:name=".detection.receiver.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- GeofenceBroadcastReceiver — Play Services delivers via PendingIntent (external process).
             exported=true is required on Android 12+ so the OS allows cross-process delivery.
             The PendingIntent is explicit + FLAG_IMMUTABLE, limiting spoofing risk. -->
        <receiver
            android:name=".detection.receiver.GeofenceBroadcastReceiver"
            android:exported="true" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBpOJ6G-AVGiEJLFl1acyjo82C2r0MIIP8" />

        <!-- ParkingConfirmationReceiver — notification action buttons -->
        <receiver
            android:name=".detection.receiver.ParkingConfirmationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.apptolast.paparcar.ACTION_PARKING_CONFIRMED" />
                <action android:name="io.apptolast.paparcar.ACTION_PARKING_DENIED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
